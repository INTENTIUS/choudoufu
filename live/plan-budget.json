{
  "n": 200,
  "resource_type": "aws_s3_bucket",
  "calls_total": 3403,
  "calls_by_api": {
    "GET /{bucket}/...": 400,
    "GET ?accelerate": 200,
    "GET ?acl": 200,
    "GET ?bucket-region&max-buckets&x-id": 1,
    "GET ?cors": 200,
    "GET ?encryption": 200,
    "GET ?lifecycle": 200,
    "GET ?logging": 200,
    "GET ?object-lock": 200,
    "GET ?policy": 200,
    "GET ?replication": 200,
    "GET ?requestPayment": 200,
    "GET ?versioning": 200,
    "GET ?website": 200,
    "GetCallerIdentity": 1,
    "GetUser": 1,
    "HEAD /{bucket}": 600
  },
  "tolerance": 0.2,
  "wall_clock_bucket": "a few seconds (2-4s measured locally against floci on the machine that recorded this artifact)",
  "measured_at": "2026-08-13",
  "measured_against": "ghcr.io/lex00/floci@sha256:4753246c0260a22af1056c65993f4d73b0a907729a9580b9baba5d628b6dad34",
  "note": "Issue #64's committed call-count budget. Generated by internal/live/discovery.TestPlanCallBudgetAgainstFloci (go test -run TestPlanCallBudgetAgainstFloci, TF_FLOCI_TEST=1), which reads this file back and fails if a fresh measurement at the same n exceeds calls_total by more than tolerance. calls_total covers discovery.Discover plus projection.BuildFrom for an N=200 aws_s3_bucket estate (tools/estate-gen -count 200) with no prior state - the read cost live-plan pays end to end, not just the discovery/list half. wall_clock_bucket and measured_at are informational only, never gated: floci's performance on whatever machine runs the test, not this repository's code, is what a wall-clock assertion would actually be grading. calls_by_api's per-bucket shape (~17 calls/instance: 3 HEAD /{bucket} plus one GET per S3 subresource the aws_s3_bucket Read covers - acl, cors, encryption, lifecycle, logging, object-lock, policy, replication, requestPayment, versioning, website, accelerate - plus 2 more folded into 'GET /{bucket}/...', which is this proxy's catch-all for any deeper-path or S3-Control-routed call such as bucket tags) is a property of the AWS provider's own aws_s3_bucket Read, not of anything this fork adds; it is recorded here as the baseline this ratchet protects, not as a target to optimize down. See internal/live/discovery/scale_bench_test.go's doc comment for the full method, and internal/live/cloudcontrol/doc.go's 'Retries' section for the backoff policy this same issue asked for."
}
