# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires only name and sns_topic; the provider validates sns_topic is a well-formed ARN (validate: "is an invalid ARN"), and no aws_sns_topic is part of this cohort to reference
resource "aws_db_event_subscription" "app" {
  name      = "tofu-rds-cohort-db-event-subscription"
  sns_topic = "arn:aws:sns:us-east-1:000000000000:tofu-rds-cohort-events"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_db_event_subscription.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires only identifier and instance_class; the provider's create-time logic also requires allocated_storage, engine, username and one of password/password_wo/manage_master_user_password (validate does not catch any of these - they are enforced only once Create actually runs, confirmed by hand against floci during this batch's verification), and instance_class needs a real instance type, not an arbitrary string
resource "aws_db_instance" "app" {
  identifier          = "tofu-rds-cohort-db-instance"
  instance_class      = "db.t3.micro"
  allocated_storage   = 10
  engine              = "mysql"
  username            = "admin"
  password            = "tofu-rds-cohort-pw"
  skip_final_snapshot = true
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_db_instance.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires db_instance_identifier, feature_name and role_arn; the provider validates role_arn is a well-formed ARN (validate: "is an invalid ARN"), and db_instance_identifier is a bare string the generic pass has no parentRef alias for (its own name differs from aws_db_instance's "identifier" identity argument), so it defaults to a placeholder that names no real instance in this cohort
resource "aws_db_instance_role_association" "app" {
  db_instance_identifier = aws_db_instance.app.identifier
  feature_name           = "tofu-rds-cohort-db-instance-role-association"
  role_arn               = "arn:aws:iam::000000000000:role/tofu-rds-cohort-role"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: major_engine_version, option_group_description, option.option_name, option.option_settings.name, option.option_settings.value
resource "aws_db_option_group" "app" {
  name                     = "tofu-rds-cohort-db-option-group"
  engine_name              = "tofu-rds-cohort-db-option-group"
  major_engine_version     = "11.00"
  option_group_description = "Terraform Option Group"
  option {
    option_name = "Timezone"
    option_settings {
      name  = "TIME_ZONE"
      value = "UTC"
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_db_option_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: family, parameter.name, parameter.value
resource "aws_db_parameter_group" "app" {
  name   = "tofu-rds-cohort-db-parameter-group"
  family = "mysql5.6"
  parameter {
    name  = "character_set_server"
    value = "utf8"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_db_parameter_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires name, engine_family, role_arn and vpc_subnet_ids; the provider validates engine_family is one of MYSQL/POSTGRESQL/SQLSERVER (validate: "expected engine_family to be one of ...") and role_arn is a well-formed ARN (validate: "is an invalid ARN")
resource "aws_db_proxy" "app" {
  name           = "tofu-rds-cohort-db-proxy"
  engine_family  = "MYSQL"
  role_arn       = "arn:aws:iam::000000000000:role/tofu-rds-cohort-role"
  vpc_subnet_ids = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_db_proxy.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires only db_proxy_name; the generic pass's parentRef alias does not fire because this type's own identity argument (db_proxy_name, per internal/live/identity/table.go) has the same name as its own Required argument, so it fills its own identity placeholder instead of referencing the sibling aws_db_proxy this batch's cohort also admits - db_proxy_name is set to aws_db_proxy.app's own "name" argument by hand instead
resource "aws_db_proxy_default_target_group" "app" {
  db_proxy_name = aws_db_proxy.app.name
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_db_proxy_endpoint" "app" {
  db_proxy_name          = aws_db_proxy_default_target_group.app.db_proxy_name
  db_proxy_endpoint_name = "tofu-rds-cohort-db-proxy-endpoint"
  vpc_subnet_ids         = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_db_proxy_endpoint.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_db_subnet_group" "app" {
  name       = "tofu-rds-cohort-db-subnet-group"
  subnet_ids = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_db_subnet_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires cluster_identifier and engine; the provider validates engine against a fixed enum (validate: "expected engine to be one of [aurora-mysql aurora-postgresql mysql postgres]"; a second, independent validator on the same attribute also rejects it: "invalid value for engine (must begin with custom-)"); skip_final_snapshot also defaults to false, and the provider refuses a destroy without it or a final_snapshot_identifier (found only by exercising a destroy against floci during this batch's verification, not by validate)
resource "aws_rds_cluster" "app" {
  cluster_identifier  = "tofu-rds-cohort-cluster"
  engine              = "aurora-mysql"
  skip_final_snapshot = true
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rds_cluster.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: engine, instance_class
resource "aws_rds_cluster_instance" "app" {
  identifier         = "tofu-rds-cohort-cluster-instance"
  cluster_identifier = aws_rds_cluster.app.cluster_identifier
  engine             = aws_rds_cluster.app.engine
  instance_class     = "db.r4.large"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rds_cluster_instance.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description, family, parameter.name, parameter.value
resource "aws_rds_cluster_parameter_group" "app" {
  name        = "tofu-rds-cohort-cluster-parameter-group"
  family      = "aurora5.6"
  description = "RDS default cluster parameter group"
  parameter {
    name  = "character_set_server"
    value = "utf8"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rds_cluster_parameter_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires db_cluster_identifier, feature_name and role_arn; the provider validates role_arn is a well-formed ARN (validate: "is an invalid ARN"), and db_cluster_identifier is a bare string the generic pass has no parentRef alias for (its own name differs from aws_rds_cluster's "cluster_identifier" identity argument), so it defaults to a placeholder that names no real cluster in this cohort
resource "aws_rds_cluster_role_association" "app" {
  db_cluster_identifier = aws_rds_cluster.app.cluster_identifier
  role_arn              = "arn:aws:iam::000000000000:role/tofu-rds-cohort-role"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires engine, database_installation_files_s3_bucket_name, database_installation_files_s3_prefix and engine_version; the provider validates engine must begin with "custom-" (validate: "invalid value for engine (must begin with custom-)"), the same shape as aws_rds_cluster's engine but a disjoint enum
resource "aws_rds_custom_db_engine_version" "app" {
  engine         = "custom-oracle-ee-cdb"
  engine_version = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rds_custom_db_engine_version.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: engine, engine_version
resource "aws_rds_global_cluster" "app" {
  global_cluster_identifier = "tofu-rds-cohort-global-cluster"
  engine                    = "aurora"
  engine_version            = "5.6.mysql_aurora.1.22.2"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rds_global_cluster.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires integration_name, source_arn and target_arn; the provider validates both are well-formed ARNs (validate: "Invalid ARN Value") - source_arn references the cohort's own aws_rds_cluster, target_arn names a Redshift Serverless namespace no type in this cohort covers
resource "aws_rds_integration" "app" {
  integration_name = "tofu-rds-cohort-integration"
  source_arn       = aws_rds_cluster.app.arn
  target_arn       = "arn:aws:redshift-serverless:us-east-1:000000000000:namespace/tofu-rds-cohort-namespace"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rds_integration.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires db_cluster_identifier, db_shard_group_identifier and max_acu; db_cluster_identifier is a bare string the generic pass has no parentRef alias for (its own name differs from aws_rds_cluster's "cluster_identifier" identity argument), so it defaults to a placeholder that names no real cluster in this cohort - no provider-side validation catches the mismatch, but the fix keeps this cohort's shard group pointed at the real cluster it admits alongside it
resource "aws_rds_shard_group" "app" {
  db_shard_group_identifier = "tofu-rds-cohort-shard-group"
  db_cluster_identifier     = aws_rds_cluster.app.cluster_identifier
  max_acu                   = 0
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rds_shard_group.app"
  }
}
