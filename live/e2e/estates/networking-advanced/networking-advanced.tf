# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_globalaccelerator_accelerator" "app" {
  name = "tofu-networking-advanced-cohort-globalaccelerator-accelerator"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_globalaccelerator_accelerator.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_globalaccelerator_cross_account_attachment" "app" {
  name = "tofu-networking-advanced-cohort-globalaccelerator-cross-account-attachment"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_globalaccelerator_cross_account_attachment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: listener_arn is Required but a generic-string placeholder is not a valid ARN (validate: "is an invalid ARN") - overridden to this cohort's own aws_globalaccelerator_listener.app.arn
resource "aws_globalaccelerator_endpoint_group" "app" {
  listener_arn = aws_globalaccelerator_listener.app.arn
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: accelerator_arn is Required but a generic-string placeholder is not a valid ARN (validate: "is an invalid ARN") - overridden to this cohort's own aws_globalaccelerator_accelerator.app.arn; protocol is Required and validated against a closed enum (validate: "expected protocol to be one of [\"TCP\" \"UDP\"]"); port_range is a Required (min_items 1) block the generic pass emits empty, but the provider requires from_port and to_port once the block exists
resource "aws_globalaccelerator_listener" "app" {
  accelerator_arn = aws_globalaccelerator_accelerator.app.arn
  protocol        = "TCP"
  port_range {
    from_port = 80
    to_port   = 80
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: the schema marks transit_gateway_id and vpc_id both Optional, but the provider requires exactly one (validate: "Invalid combination of arguments" x2); firewall_policy_arn is Required but a generic-string placeholder is not a valid ARN (validate: "is an invalid ARN") - overridden to this cohort's own aws_networkfirewall_firewall_policy.app.arn. subnet_mapping carries no schema-level minimum, but a VPC-scoped firewall needs at least one subnet in practice, so one is added for behavioral realism even though validate does not require it.
resource "aws_networkfirewall_firewall" "app" {
  firewall_policy_arn = aws_networkfirewall_firewall_policy.app.arn
  name                = "tofu-networking-advanced-cohort-networkfirewall-firewall"
  vpc_id              = "vpc-0123456789abcdef0"
  subnet_mapping {
    subnet_id = "subnet-0123456789abcdef0"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkfirewall_firewall.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: stateless_default_actions and stateless_fragment_default_actions are both Required sets of strings the schema does not constrain (no closed-enum validate error - Network Firewall validates these only once Create actually runs), but the generic pass's own "placeholder" element is not a real action name; overridden to the documented built-in aws:pass for behavioral realism during a floci apply, not to silence validate
resource "aws_networkfirewall_firewall_policy" "app" {
  name = "tofu-networking-advanced-cohort-networkfirewall-firewall-policy"
  firewall_policy {
    stateless_default_actions          = ["aws:pass"]
    stateless_fragment_default_actions = ["aws:pass"]
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkfirewall_firewall_policy.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: firewall_arn is Required; the generic pass's own cross-reference guess (this type shares no identity-table shape with any sibling, so parentRef never fires) left a literal, non-ARN string in place (validate: "is an invalid ARN") - overridden to this cohort's own aws_networkfirewall_firewall.app.arn. log_destination_config's log_destination_type and log_type are both Required strings validated against closed enums (validate: "expected ... to be one of [...]" x2); log_destination is a Required map the generic pass leaves empty, but CloudWatchLogs destinations need a logGroup key in practice
resource "aws_networkfirewall_logging_configuration" "app" {
  firewall_arn = aws_networkfirewall_firewall.app.arn
  logging_configuration {
    log_destination_config {
      log_destination      = { logGroup = "tofu-networking-advanced-cohort-firewall-logs" }
      log_destination_type = "CloudWatchLogs"
      log_type             = "FLOW"
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: type is Required and validated against a closed enum (validate: "expected type to be one of [\"STATELESS\" \"STATEFUL\" \"STATEFUL_DOMAIN\"]"); rules and rule_group are both Optional in the schema (either supplies the rule content), but a rule group with neither has nothing for the STATELESS type it declares, so a minimal valid stateless rules_source is added for behavioral realism, not because validate requires it
resource "aws_networkfirewall_rule_group" "app" {
  capacity = 100
  name     = "tofu-networking-advanced-cohort-networkfirewall-rule-group"
  type     = "STATELESS"
  rule_group {
    rules_source {
      stateless_rules_and_custom_actions {
        stateless_rule {
          priority = 1
          rule_definition {
            actions = ["aws:pass"]
            match_attributes {
              source {
                address_definition = "10.0.0.0/8"
              }
              destination {
                address_definition = "10.0.0.0/8"
              }
            }
          }
        }
      }
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkfirewall_rule_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: tls_inspection_configuration is a Required block the generic pass emits empty, but the provider requires at least one of server_certificate_configuration[0].certificate_authority_arn/server_certificate once it exists (validate: "At least one of these attributes must be configured" and "Block ... must have a configuration value"); server_certificate_configuration's own scope block is likewise required once that block exists (validate: "Block ... scope must have a configuration value"), and scope's protocols is Required
resource "aws_networkfirewall_tls_inspection_configuration" "app" {
  name = "tofu-networking-advanced-cohort-networkfirewall-tls-inspection-configuration"
  tls_inspection_configuration {
    server_certificate_configuration {
      certificate_authority_arn = "arn:aws:acm-pca:us-east-1:123456789012:certificate-authority/12345678-1234-1234-1234-123456789012"
      scope {
        protocols = [6]
        source {
          address_definition = "10.0.0.0/8"
        }
        destination {
          address_definition = "10.0.0.0/8"
        }
      }
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkfirewall_tls_inspection_configuration.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: firewall_arn is Required; the generic pass's own cross-resource guess pointed it at this cohort's aws_networkfirewall_logging_configuration.app.firewall_arn (that type happens to share an argument name, but is not this type's real parent) - overridden to this cohort's own aws_networkfirewall_firewall.app.arn instead. vpc_id is Required with no ARN validation, but is nonetheless a real EC2 VPC id this batch's scope does not add coverage for, so it stays a literal placeholder (the same choice aws_volume_attachment's own comment above makes for aws_ebs_volume). subnet_mapping carries no schema-level minimum, but the provider requires at least one once the block exists in practice (validate: "Block subnet_mapping must have a configuration value")
resource "aws_networkfirewall_vpc_endpoint_association" "app" {
  firewall_arn = aws_networkfirewall_firewall.app.arn
  vpc_id       = "vpc-0123456789abcdef0"
  subnet_mapping {
    subnet_id = "subnet-0123456789abcdef0"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkfirewall_vpc_endpoint_association.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: core_network_id is Required and the provider validates its shape (validate: "Must start with core-network and then have 8 to 17 characters") - overridden to this cohort's own aws_networkmanager_core_network.app.id; transport_attachment_id is Required and shape-validated the same way (validate: "Must start with attachment- and then have 8 to 17 characters") - overridden to this cohort's own aws_networkmanager_vpc_attachment.app.id, itself a real "attachment-..." id, making this Connect attachment's transport a VPC attachment already in this cohort; options is an Optional (max_items 1) block the generic pass still emits empty, given a protocol for behavioral realism
resource "aws_networkmanager_connect_attachment" "app" {
  core_network_id         = aws_networkmanager_core_network.app.id
  edge_location           = "us-east-1"
  transport_attachment_id = aws_networkmanager_vpc_attachment.app.id
  options {
    protocol = "GRE"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_connect_attachment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: connect_attachment_id is Required and shape-validated (validate: "Must start with attachment and then have 8 to 17 characters") - overridden to this cohort's own aws_networkmanager_connect_attachment.app.id; peer_address is Required with no schema-level format constraint, given a real IP literal for behavioral realism
resource "aws_networkmanager_connect_peer" "app" {
  connect_attachment_id = aws_networkmanager_connect_attachment.app.id
  peer_address          = "10.0.0.1"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_connect_peer.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: global_network_id is Required but a generic-string placeholder, not a reference - global_network is server-assigned so identityArgName never wires this automatically (issue #56's own note on the mechanism); overridden to this cohort's own aws_networkmanager_global_network.app.id
resource "aws_networkmanager_core_network" "app" {
  global_network_id = aws_networkmanager_global_network.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_core_network.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: global_network_id and device_id are both Required but generic-string placeholders, not references - overridden to this cohort's own aws_networkmanager_global_network.app.id and aws_networkmanager_device.app.id. customer_gateway_arn is Required but a generic-string placeholder is not a valid ARN (validate: "is an invalid ARN"); the real target is an EC2 customer gateway, outside this batch's own scope (aws_customer_gateway is explicitly not this agent's to touch), so it stays a literal placeholder ARN
resource "aws_networkmanager_customer_gateway_association" "app" {
  customer_gateway_arn = "arn:aws:ec2:us-east-1:123456789012:customer-gateway/cgw-0123456789abcdef0"
  device_id            = aws_networkmanager_device.app.id
  global_network_id    = aws_networkmanager_global_network.app.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: global_network_id is Required but a generic-string placeholder, not a reference - overridden to this cohort's own aws_networkmanager_global_network.app.id
resource "aws_networkmanager_device" "app" {
  global_network_id = aws_networkmanager_global_network.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_device.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: core_network_id is Required but a generic-string placeholder, not a reference - overridden to this cohort's own aws_networkmanager_core_network.app.id. direct_connect_gateway_arn is Required and the provider validates it is a well-formed ARN (validate: "The provided value cannot be parsed as an ARN"); the real target is an EC2 Direct Connect gateway, outside this batch's own scope, so it stays a literal placeholder ARN
resource "aws_networkmanager_dx_gateway_attachment" "app" {
  core_network_id            = aws_networkmanager_core_network.app.id
  direct_connect_gateway_arn = "arn:aws:directconnect::123456789012:dx-gateway/11223344-1122-1122-1122-112233445566"
  edge_locations             = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_dx_gateway_attachment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_networkmanager_global_network" "app" {
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_global_network.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: global_network_id and site_id are both Required but generic-string placeholders, not references - overridden to this cohort's own aws_networkmanager_global_network.app.id and aws_networkmanager_site.app.id. bandwidth is a Required (min_items 1) block the generic pass emits empty; both its attributes are schema-Optional, but a link with neither speed set is not a realistic fixture, so both are given values
resource "aws_networkmanager_link" "app" {
  global_network_id = aws_networkmanager_global_network.app.id
  site_id           = aws_networkmanager_site.app.id
  bandwidth {
    download_speed = 100
    upload_speed   = 100
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_link.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: global_network_id, link_id and device_id are all Required but generic-string placeholders, not references - overridden to this cohort's own aws_networkmanager_global_network.app.id, aws_networkmanager_link.app.id and aws_networkmanager_device.app.id
resource "aws_networkmanager_link_association" "app" {
  device_id         = aws_networkmanager_device.app.id
  global_network_id = aws_networkmanager_global_network.app.id
  link_id           = aws_networkmanager_link.app.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: core_network_id is Required but a generic-string placeholder, not a reference - overridden to this cohort's own aws_networkmanager_core_network.app.id. prefix_list_arn is Required and the provider validates it is a well-formed ARN (validate: "The provided value cannot be parsed as an ARN"); the real target is an EC2 managed prefix list, outside this batch's own scope, so it stays a literal placeholder ARN
resource "aws_networkmanager_prefix_list_association" "app" {
  core_network_id   = aws_networkmanager_core_network.app.id
  prefix_list_alias = "placeholder"
  prefix_list_arn   = "arn:aws:ec2:us-east-1:123456789012:prefix-list/pl-0123456789abcdef0"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: global_network_id is Required but a generic-string placeholder, not a reference - overridden to this cohort's own aws_networkmanager_global_network.app.id
resource "aws_networkmanager_site" "app" {
  global_network_id = aws_networkmanager_global_network.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_site.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: core_network_id is Required but a generic-string placeholder, not a reference - overridden to this cohort's own aws_networkmanager_core_network.app.id. vpn_connection_arn is Required and the provider validates its shape (validate: "Must be valid VPN ARN"); the real target is an EC2 VPN connection, outside this batch's own scope, so it stays a literal placeholder ARN
resource "aws_networkmanager_site_to_site_vpn_attachment" "app" {
  core_network_id    = aws_networkmanager_core_network.app.id
  vpn_connection_arn = "arn:aws:ec2:us-east-1:123456789012:vpn-connection/vpn-0123456789abcdef0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_site_to_site_vpn_attachment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: core_network_id is Required but a generic-string placeholder, not a reference - overridden to this cohort's own aws_networkmanager_core_network.app.id. transit_gateway_arn is Required and the provider validates it is a well-formed ARN (validate: "is an invalid ARN"); the real target is an EC2 transit gateway, outside this batch's own scope, so it stays a literal placeholder ARN
resource "aws_networkmanager_transit_gateway_peering" "app" {
  core_network_id     = aws_networkmanager_core_network.app.id
  transit_gateway_arn = "arn:aws:ec2:us-east-1:123456789012:transit-gateway/tgw-0123456789abcdef0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_transit_gateway_peering.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: global_network_id is Required but a generic-string placeholder, not a reference - overridden to this cohort's own aws_networkmanager_global_network.app.id. transit_gateway_arn is Required and the provider validates it is a well-formed ARN (validate: "is an invalid ARN"); the real target is an EC2 transit gateway, outside this batch's own scope, so it stays a literal placeholder ARN, the same one aws_networkmanager_transit_gateway_peering above uses
resource "aws_networkmanager_transit_gateway_registration" "app" {
  global_network_id   = aws_networkmanager_global_network.app.id
  transit_gateway_arn = "arn:aws:ec2:us-east-1:123456789012:transit-gateway/tgw-0123456789abcdef0"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: peering_id is Required but a generic-string placeholder, not a reference - overridden to this cohort's own aws_networkmanager_transit_gateway_peering.app.id. transit_gateway_route_table_arn is Required and the provider validates it is a well-formed ARN (validate: "is an invalid ARN"); the real target is an EC2 transit gateway route table, outside this batch's own scope, so it stays a literal placeholder ARN
resource "aws_networkmanager_transit_gateway_route_table_attachment" "app" {
  peering_id                      = aws_networkmanager_transit_gateway_peering.app.id
  transit_gateway_route_table_arn = "arn:aws:ec2:us-east-1:123456789012:transit-gateway-route-table/tgw-rtb-0123456789abcdef0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_transit_gateway_route_table_attachment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: core_network_id is Required but a generic-string placeholder, not a reference - overridden to this cohort's own aws_networkmanager_core_network.app.id. subnet_arns and vpc_arn are both Required and the provider validates each is a well-formed ARN (validate: "is an invalid ARN" x2); the real targets are EC2 subnets and a VPC, outside this batch's own scope, so both stay literal placeholder ARNs
resource "aws_networkmanager_vpc_attachment" "app" {
  core_network_id = aws_networkmanager_core_network.app.id
  subnet_arns     = ["arn:aws:ec2:us-east-1:123456789012:subnet/subnet-0123456789abcdef0"]
  vpc_arn         = "arn:aws:ec2:us-east-1:123456789012:vpc/vpc-0123456789abcdef0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_networkmanager_vpc_attachment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53recoveryreadiness_cell" "app" {
  cell_name = "tofu-networking-advanced-cohort-route53recoveryreadiness-cell"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53recoveryreadiness_cell.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53recoveryreadiness_readiness_check" "app" {
  readiness_check_name = "tofu-networking-advanced-cohort-route53recoveryreadiness-readiness-check"
  resource_set_name    = aws_route53recoveryreadiness_resource_set.app.resource_set_name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53recoveryreadiness_readiness_check.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53recoveryreadiness_recovery_group" "app" {
  recovery_group_name = "tofu-networking-advanced-cohort-route53recoveryreadiness-recovery-group"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53recoveryreadiness_recovery_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: resource_set_type is Required with no closed-enum validation (it names an arbitrary supported AWS resource type string), given a real one for behavioral realism; resources is a Required (min_items 1) block the generic pass emits empty - given a resource_arn for behavioral realism, matching the resource_set_type given
resource "aws_route53recoveryreadiness_resource_set" "app" {
  resource_set_name = "tofu-networking-advanced-cohort-route53recoveryreadiness-resource-set"
  resource_set_type = "AWS::EC2::Subnet"
  resources {
  }
  resources {
    resource_arn = "arn:aws:ec2:us-east-1:123456789012:subnet/subnet-0123456789abcdef0"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53recoveryreadiness_resource_set.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: resource_identifier is Required; the generic pass's own cross-resource guess (parentRef matched this argument's name against aws_vpclattice_auth_policy's own identity argument, which happens to share the name "resource_identifier" but is not this type's real target) left this pointed at a sibling resource's own opaque placeholder string - overridden to this cohort's own aws_vpclattice_service_network.app.arn, an actual VPC Lattice resource a log subscription can target. destination_arn is Required with no ARN-format validate error surfaced yet, given a real S3 ARN shape for behavioral realism
resource "aws_vpclattice_access_log_subscription" "app" {
  destination_arn     = "arn:aws:s3:::tofu-networking-advanced-cohort-firewall-logs"
  resource_identifier = aws_vpclattice_service_network.app.arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_access_log_subscription.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: resource_identifier is Required; the generic pass rendered its own identity-table placeholder name (a self-reference to no real resource) rather than an actual VPC Lattice resource - overridden to this cohort's own aws_vpclattice_service_network.app.arn. policy is Required and the provider validates it is well-formed JSON (validate: "\"policy\" contains an invalid JSON")
resource "aws_vpclattice_auth_policy" "app" {
  resource_identifier = aws_vpclattice_service_network.app.arn
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = "*"
      Action    = "vpc-lattice-svcs:Invoke"
      Resource  = "*"
    }]
  })
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: domain_name is Required with no length/format validate error surfaced against this cohort's own generic naming convention, given a real-looking domain name for behavioral realism
resource "aws_vpclattice_domain_verification" "app" {
  domain_name = "tofu-networking-advanced-cohort.example.com"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_domain_verification.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name is Required and the provider validates its length (3-40 characters); this cohort's own generic tofu-COHORT-cohort-SUFFIX naming convention exceeds it, so a short literal name is given instead. protocol is Required and validated against a closed enum (validate: "expected protocol to be one of [\"HTTP\" \"HTTPS\" \"TLS_PASSTHROUGH\"]"). service_arn and service_identifier are both Optional in the schema, but the provider requires exactly one (validate: "Missing required argument" x2) - service_identifier is set to this cohort's own aws_vpclattice_service.app.id. default_action is a Required (min_items 1) block the generic pass emits empty; given a fixed_response for behavioral realism
resource "aws_vpclattice_listener" "app" {
  name     = "tofu-na-listener"
  protocol = "HTTPS"
  default_action {
    fixed_response {
      status_code = 404
    }
  }
  service_identifier = aws_vpclattice_service.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_listener.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name is Required and length-validated the same way aws_vpclattice_listener's is above, given a short literal name for the same reason. listener_identifier and service_identifier are both Required but generic-string placeholders, not references - overridden to this cohort's own aws_vpclattice_listener.app.id and aws_vpclattice_service.app.id. priority is Required and the provider validates its range (validate: "expected priority to be in the range (1 - 100), got 0"). action is a Required (min_items 1) block the generic pass emits empty; given a fixed_response for behavioral realism, the same shape as the listener's own default_action. match.http_match is a Required (min_items 1) block whose own contents (header_matches, method, path_match) are all Optional, but the provider requires at least one (validate: "Missing required argument"); given a path_match
resource "aws_vpclattice_listener_rule" "app" {
  listener_identifier = aws_vpclattice_listener.app.id
  name                = "tofu-na-listener-rule"
  priority            = 10
  service_identifier  = aws_vpclattice_service.app.id
  action {
    fixed_response {
      status_code = 404
    }
  }
  match {
    http_match {
      path_match {
        match {
          exact = "/"
        }
      }
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_listener_rule.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name is Required and length-validated the same way aws_vpclattice_listener's is above, given a short literal name for the same reason. resource_gateway_identifier and resource_configuration_group_id are both Optional, but the provider requires exactly one (validate: "No attribute specified when one ... is required") - resource_gateway_identifier is set to this cohort's own aws_vpclattice_resource_gateway.app.id. protocol, resource_configuration_definition[0].arn_resource and resource_configuration_group_id are all Optional, but the provider requires at least one (validate: "At least one attribute out of [...] must be specified") - protocol is set
resource "aws_vpclattice_resource_configuration" "app" {
  name                        = "tofu-na-resource-config"
  protocol                    = "TCP"
  resource_gateway_identifier = aws_vpclattice_resource_gateway.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_resource_configuration.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name is Required and length-validated the same way aws_vpclattice_listener's is above, given a short literal name for the same reason. subnet_ids and vpc_id are both Required with no ARN-format validate error surfaced, but the real targets are an EC2 VPC and its subnets, outside this batch's own scope, so both stay literal placeholder ids
resource "aws_vpclattice_resource_gateway" "app" {
  name       = "tofu-na-resource-gateway"
  subnet_ids = ["subnet-0123456789abcdef0"]
  vpc_id     = "vpc-0123456789abcdef0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_resource_gateway.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: resource_arn is Required; the generic pass's own identity-table placeholder (a self-reference to no real resource) left a non-ARN string in place (validate: "is an invalid ARN") - overridden to this cohort's own aws_vpclattice_service_network.app.arn. policy is Required and the provider validates it is well-formed JSON (validate: "\"policy\" contains an invalid JSON")
resource "aws_vpclattice_resource_policy" "app" {
  resource_arn = aws_vpclattice_service_network.app.arn
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = "*"
      Action    = "vpc-lattice-svcs:Invoke"
      Resource  = "*"
    }]
  })
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name is Required and length-validated the same way aws_vpclattice_listener's is above, given a short literal name for the same reason
resource "aws_vpclattice_service" "app" {
  name = "tofu-na-service"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_service.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name is Required and length-validated the same way aws_vpclattice_listener's is above, given a short literal name for the same reason
resource "aws_vpclattice_service_network" "app" {
  name = "tofu-na-service-network"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_service_network.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: resource_configuration_identifier and service_network_identifier are both Required but generic-string placeholders, not references - overridden to this cohort's own aws_vpclattice_resource_configuration.app.id and aws_vpclattice_service_network.app.id
resource "aws_vpclattice_service_network_resource_association" "app" {
  resource_configuration_identifier = aws_vpclattice_resource_configuration.app.id
  service_network_identifier        = aws_vpclattice_service_network.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_service_network_resource_association.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: service_identifier and service_network_identifier are both Required but generic-string placeholders, not references - overridden to this cohort's own aws_vpclattice_service.app.id and aws_vpclattice_service_network.app.id
resource "aws_vpclattice_service_network_service_association" "app" {
  service_identifier         = aws_vpclattice_service.app.id
  service_network_identifier = aws_vpclattice_service_network.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_service_network_service_association.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: service_network_identifier is Required but a generic-string placeholder, not a reference - overridden to this cohort's own aws_vpclattice_service_network.app.id. vpc_identifier is Required with no ARN-format validate error surfaced, but the real target is an EC2 VPC, outside this batch's own scope, so it stays a literal placeholder id
resource "aws_vpclattice_service_network_vpc_association" "app" {
  service_network_identifier = aws_vpclattice_service_network.app.id
  vpc_identifier             = "vpc-0123456789abcdef0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_service_network_vpc_association.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name is Required and length-validated the same way aws_vpclattice_listener's is above, given a short literal name for the same reason. type is Required and validated against a closed enum (validate: "expected type to be one of [\"IP\" \"LAMBDA\" \"INSTANCE\" \"ALB\"]"); an IP-type target group also needs its config block's vpc_identifier in practice (not caught by validate, enforced at Create), so config is given even though the schema marks it Optional
resource "aws_vpclattice_target_group" "app" {
  name = "tofu-na-target-group"
  type = "IP"
  config {
    vpc_identifier = "vpc-0123456789abcdef0"
    port           = 80
    protocol       = "HTTP"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpclattice_target_group.app"
  }
}
