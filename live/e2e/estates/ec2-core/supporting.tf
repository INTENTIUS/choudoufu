# Supporting, not coverage - added only so aws_ebs_volume.ec2-core's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: availability_zone, size
resource "aws_ebs_volume" "ec2-core" {
  availability_zone = "us-west-2a"
  size              = 40
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ebs_volume.ec2-core"
  }
}

# Supporting, not coverage - added only so aws_security_group.ec2-core's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_security_group" "ec2-core" {
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_security_group.ec2-core"
  }
}

# Supporting, not coverage - added only so aws_subnet.ec2-core's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_subnet" "ec2-core" {
  vpc_id = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_subnet.ec2-core"
  }
}
