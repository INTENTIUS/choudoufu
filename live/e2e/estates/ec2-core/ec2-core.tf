# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: state is Required but Optional-shaped in nothing else - the provider validates it against a closed enum (validate: "expected state to be one of [...]"), and the generic pass's "placeholder" string is not a member
resource "aws_ebs_snapshot_block_public_access" "app" {
  state = "block-new-sharing"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: instance_platform is Required and the provider validates it against a closed enum (validate: "expected instance_platform to be one of [...]"); the generic placeholder string is not a member
resource "aws_ec2_capacity_reservation" "app" {
  availability_zone = "placeholder"
  instance_count    = 0
  instance_platform = "Linux/UNIX"
  instance_type     = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_capacity_reservation.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: launch_template_config is a required block, but its own launch_template_specification child is optional in the schema while the provider requires it in practice (validate: "Invalid combination of arguments" on an empty launch_template_config); target_capacity_specification.default_target_capacity_type is Required and validated against a closed enum, and the generic placeholder string is not a member
resource "aws_ec2_fleet" "app" {
  launch_template_config {
    launch_template_specification {
      launch_template_id = "lt-0123456789abcdef0"
      version            = "$Latest"
    }
  }
  target_capacity_specification {
    default_target_capacity_type = "on-demand"
    total_target_capacity        = 0
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_fleet.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: instance_family and instance_type are both Optional in the schema, but the provider requires exactly one (validate: "Invalid combination of arguments": "one of instance_family,instance_type must be specified"), and the generic required-only pass sets neither
resource "aws_ec2_host" "app" {
  availability_zone = "placeholder"
  instance_type     = "c5.xlarge"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_host.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: every argument is Optional in the schema, so the generic pass renders an empty body, but the provider requires exactly one of instance_id/network_interface_id (validate: "Invalid combination of arguments"); allocation_id is documented as required in practice too (legacy EC2-Classic exception in the Argument Reference), so both are set here rather than just enough to silence validate. instance_id references this same cohort's aws_instance.app - the cross-resource reference issue #56 asks for - since identityArgName only wires that automatically for client-named types, and aws_instance is server-assigned.
resource "aws_eip_association" "app" {
  instance_id   = aws_instance.app.id
  allocation_id = "eipalloc-0123456789abcdef0"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: ami and instance_type are both Optional in the schema (a launch_template can supply either instead), but the provider requires ami and instance_type when no launch_template is set (validate: "Missing required argument" x3), and the generic required-only pass sets neither since the schema alone does not say so
resource "aws_instance" "app" {
  ami           = "ami-0123456789abcdef0"
  instance_type = "t3.micro"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_instance.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: public_key
resource "aws_key_pair" "app" {
  key_name   = "tofu-ec2-core-cohort-key-pair"
  public_key = "ssh-rsa  email@example.com"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_key_pair.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_network_interface" "app" {
  subnet_id = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_network_interface.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: instance_id and network_interface_id are both Required but generic-string placeholders, not references - overridden to point at this cohort's own aws_instance.app and aws_network_interface.app for the same cross-resource-reference reason as aws_eip_association above (validate does not require this; it is a fixture-quality improvement, not a constraint fix)
resource "aws_network_interface_attachment" "app" {
  device_index         = 0
  instance_id          = aws_instance.app.id
  network_interface_id = aws_network_interface.app.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: aws_account_id is Required and the provider validates it is a well-formed 12-digit account ID (validate: "must be a valid AWS account ID"); permission is Required and validated against a closed enum (INSTANCE-ATTACH, EIP-ASSOCIATE); network_interface_id is overridden to reference this cohort's own aws_network_interface.app for the same reason as aws_network_interface_attachment above
resource "aws_network_interface_permission" "app" {
  aws_account_id       = "123456789012"
  network_interface_id = aws_network_interface.app.id
  permission           = "INSTANCE-ATTACH"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: strategy is Required and the provider validates it against a closed enum (validate: "expected strategy to be one of [...]"); the generic placeholder string is not a member
resource "aws_placement_group" "app" {
  name     = "tofu-ec2-core-cohort-placement-group"
  strategy = "cluster"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_placement_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: launch_specification and launch_template_config are both Optional in the schema, but the provider requires exactly one (validate: "Invalid combination of arguments"), and the generic pass sets neither; iam_fleet_role is Required and the provider validates it is a well-formed ARN (validate: "is an invalid ARN"), and the generic placeholder string is not one
resource "aws_spot_fleet_request" "app" {
  iam_fleet_role  = "arn:aws:iam::123456789012:role/tofu-ec2-core-cohort-spot-fleet"
  target_capacity = 0
  launch_specification {
    ami           = "ami-0123456789abcdef0"
    instance_type = "t3.micro"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_spot_fleet_request.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: instance_id is Required but a generic-string placeholder, not a reference - overridden to point at this cohort's own aws_instance.app for the same cross-resource-reference reason as aws_eip_association above. volume_id stays a literal placeholder: aws_ebs_volume is already admitted and covered by live/e2e/estate, not part of this cohort's own coverage, so there is no sibling aws_ebs_volume resource in this run to point at (validate does not require this either; it is a fixture-quality note, not a constraint fix).
resource "aws_volume_attachment" "app" {
  device_name = "tofu-ec2-core-cohort-volume-attachment"
  instance_id = aws_instance.app.id
  volume_id   = "placeholder"
}
