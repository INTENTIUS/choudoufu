# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: type is Required and the provider validates it against a closed enum (validate: "expected type to be one of [\"ipsec.1\"]"); the generic placeholder string is not a member
resource "aws_customer_gateway" "app" {
  type = "ipsec.1"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_customer_gateway.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: authentication_options is a required block whose "type" argument the provider validates against a closed enum (validate: "expected type to be one of [...]"); the certificate-authentication member also needs root_certificate_chain_arn in practice, which the schema leaves Optional; server_certificate_arn is Required and the provider validates it is a well-formed ARN (validate: "is an invalid ARN")
resource "aws_ec2_client_vpn_endpoint" "app" {
  server_certificate_arn = "arn:aws:acm:us-east-1:000000000000:certificate/tofu-ec2-networking-cohort-server-cert"
  authentication_options {
    type                       = "certificate-authentication"
    root_certificate_chain_arn = "arn:aws:acm:us-east-1:000000000000:certificate/tofu-ec2-networking-cohort-root-cert"
  }
  connection_log_options {
    enabled = false
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_client_vpn_endpoint.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: destination_cidr_block is Required and the provider validates it is a well-formed CIDR (validate: "is not a valid CIDR block"); the generic placeholder string is not one
resource "aws_ec2_client_vpn_route" "app" {
  client_vpn_endpoint_id = aws_ec2_client_vpn_endpoint.app.id
  destination_cidr_block = "10.1.0.0/24"
  target_vpc_subnet_id   = "placeholder"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: address_family, max_entries, entry.cidr, entry.description
resource "aws_ec2_managed_prefix_list" "app" {
  address_family = "IPv4"
  max_entries    = 5
  name           = "tofu-ec2-networking-cohort-ec2-managed-prefix-list"
  entry {
    cidr        = aws_vpc.ec2-networking.cidr_block
    description = "Primary"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_managed_prefix_list.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: cidr is Required and the provider validates it is a well-formed CIDR (validate: "to be a valid CIDR Value"); the generic placeholder string is not one
resource "aws_ec2_managed_prefix_list_entry" "app" {
  prefix_list_id = aws_ec2_managed_prefix_list.app.id
  cidr           = "10.0.3.0/24"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description
resource "aws_ec2_transit_gateway" "app" {
  description = "example"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_transit_gateway.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: transport_attachment_id
resource "aws_ec2_transit_gateway_connect" "app" {
  transit_gateway_id      = aws_ec2_transit_gateway.app.id
  transport_attachment_id = aws_ec2_transit_gateway_vpc_attachment.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_transit_gateway_connect.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: peer_address is Required and the provider validates it is a well-formed IP (validate: "expected peer_address to contain a valid IP"); inside_cidr_blocks is Required and the provider validates each element is a well-formed CIDR inside a fixed range, 169.254.0.0/16 for an IPv4 element (validate: "is not a valid CIDR block", then "IPv4 range must be from range 169.254.0.0/16"); the generic pass's single "placeholder" element satisfies neither check
resource "aws_ec2_transit_gateway_connect_peer" "app" {
  inside_cidr_blocks            = ["169.254.6.0/29"]
  peer_address                  = "10.0.0.1"
  transit_gateway_attachment_id = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_transit_gateway_connect_peer.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_ec2_transit_gateway_metering_policy" "app" {
  transit_gateway_id = aws_ec2_transit_gateway.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_transit_gateway_metering_policy.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: metered_account is Required and the provider validates it against a closed enum (validate: "Invalid String Enum Value", valid values: source-attachment-owner, destination-attachment-owner, transit-gateway-owner); the generic placeholder string is not a member
resource "aws_ec2_transit_gateway_metering_policy_entry" "app" {
  transit_gateway_metering_policy_id = aws_ec2_transit_gateway_metering_policy.app.transit_gateway_metering_policy_id
  metered_account                    = "source-attachment-owner"
  policy_rule_number                 = 0
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: static_sources_support
resource "aws_ec2_transit_gateway_multicast_domain" "app" {
  transit_gateway_id     = aws_ec2_transit_gateway.app.id
  static_sources_support = "enable"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_transit_gateway_multicast_domain.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: peer_transit_gateway_id
resource "aws_ec2_transit_gateway_peering_attachment" "app" {
  peer_region             = "placeholder"
  peer_transit_gateway_id = aws_ec2_transit_gateway.app.id
  transit_gateway_id      = aws_ec2_transit_gateway.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_transit_gateway_peering_attachment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_ec2_transit_gateway_policy_table" "app" {
  transit_gateway_id = aws_ec2_transit_gateway.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_transit_gateway_policy_table.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_ec2_transit_gateway_policy_table_association" "app" {
  transit_gateway_policy_table_id = aws_ec2_transit_gateway_policy_table.app.id
  transit_gateway_attachment_id   = "placeholder"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: destination_cidr_block
resource "aws_ec2_transit_gateway_route" "app" {
  transit_gateway_route_table_id = aws_ec2_transit_gateway_route_table.app.id
  destination_cidr_block         = "0.0.0.0/0"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_ec2_transit_gateway_route_table" "app" {
  transit_gateway_id = aws_ec2_transit_gateway.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_transit_gateway_route_table.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: transit_gateway_attachment_id
resource "aws_ec2_transit_gateway_route_table_association" "app" {
  transit_gateway_route_table_id = aws_ec2_transit_gateway_route_table.app.id
  transit_gateway_attachment_id  = aws_ec2_transit_gateway_vpc_attachment.app.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: transit_gateway_attachment_id
resource "aws_ec2_transit_gateway_route_table_propagation" "app" {
  transit_gateway_route_table_id = aws_ec2_transit_gateway_route_table.app.id
  transit_gateway_attachment_id  = aws_ec2_transit_gateway_vpc_attachment.app.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_ec2_transit_gateway_vpc_attachment" "app" {
  subnet_ids         = [aws_subnet.ec2-networking.id]
  transit_gateway_id = aws_ec2_transit_gateway.app.id
  vpc_id             = aws_vpc_dhcp_options_association.app.vpc_id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_transit_gateway_vpc_attachment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: every argument naming what the flow log watches (vpc_id, subnet_id, eni_id, transit_gateway_id, transit_gateway_attachment_id, regional_nat_gateway_id) is Optional in the schema, so the generic pass renders an empty body, but the provider requires exactly one (validate: "Invalid combination of arguments": "one of ... must be specified")
resource "aws_flow_log" "app" {
  vpc_id = "vpc-0123456789abcdef0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_flow_log.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: connectivity_type, subnet_id
resource "aws_nat_gateway" "app" {
  connectivity_type = "private"
  subnet_id         = aws_subnet.ec2-networking.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_nat_gateway.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: allocation_id
resource "aws_nat_gateway_eip_association" "app" {
  nat_gateway_id = aws_nat_gateway.app.id
  allocation_id  = aws_eip.ec2-networking.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_network_acl" "app" {
  vpc_id = aws_vpc_dhcp_options_association.app.vpc_id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_network_acl.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: cidr_block and ipv6_cidr_block are both Optional in the schema, but the provider requires exactly one (validate: "Invalid combination of arguments": "one of cidr_block,ipv6_cidr_block must be specified"); protocol is Required and the provider validates it against its own protocol-number/name table (validate: "unsupported NACL protocol"); rule_action is Required and validated against a closed enum (validate: "expected rule_action to be one of [...]")
resource "aws_network_acl_rule" "app" {
  network_acl_id = aws_network_acl.app.id
  egress         = false
  protocol       = "-1"
  rule_action    = "allow"
  rule_number    = 0
  cidr_block     = "10.0.0.0/16"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: #175's demand-head batch: aws_security_group_rule, ADMITTED despite provider deprecation under the type-parity ruling. type is Required and the provider validates it against a closed enum (validate: "expected type to be one of [\"egress\" \"ingress\"]"); protocol is Required and the provider validates it against its own protocol-number/name table, same as aws_network_acl_rule's own protocol above; cidr_blocks is a list of strings in the schema, but the provider validates each element is a well-formed CIDR (validate: "is not a valid CIDR block"); the generic pass's placeholder strings satisfy none of the three
resource "aws_security_group_rule" "app" {
  security_group_id = aws_security_group.ec2-networking.id
  cidr_blocks       = ["10.0.3.0/24"]
  from_port         = 0
  protocol          = "tcp"
  to_port           = 0
  type              = "ingress"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: domain_name, domain_name_servers, ipv6_address_preferred_lease_time, netbios_name_servers, netbios_node_type and ntp_servers are all Optional in the schema, so the generic pass renders an empty body, but the provider requires at least one (validate: "Missing required argument": "one of ... must be specified")
resource "aws_vpc_dhcp_options" "app" {
  domain_name = "example.com"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc_dhcp_options.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_vpc_dhcp_options_association" "app" {
  vpc_id          = aws_vpc.ec2-networking.id
  dhcp_options_id = aws_vpc_dhcp_options.app.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_vpc_endpoint" "app" {
  vpc_id = aws_vpc_dhcp_options_association.app.vpc_id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc_endpoint.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: policy
resource "aws_vpc_endpoint_policy" "app" {
  vpc_endpoint_id = aws_vpc_endpoint.app.id
  policy          = "{\"Statement\":[{\"Action\":[\"dynamodb:*\"],\"Effect\":\"Allow\",\"Principal\":{\"AWS\":\"*\"},\"Resource\":\"*\",\"Sid\":\"AllowAll\"}],\"Version\":\"2012-10-17\"}"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: private_dns_enabled
resource "aws_vpc_endpoint_private_dns" "app" {
  vpc_endpoint_id     = aws_vpc_endpoint.app.id
  private_dns_enabled = true
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_vpc_endpoint_route_table_association" "app" {
  vpc_endpoint_id = aws_vpc_endpoint.app.id
  route_table_id  = aws_route_table.ec2-networking.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_vpc_endpoint_security_group_association" "app" {
  vpc_endpoint_id   = aws_vpc_endpoint.app.id
  security_group_id = aws_security_group.ec2-networking.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: acceptance_required, network_load_balancer_arns
resource "aws_vpc_endpoint_service" "app" {
  acceptance_required        = false
  network_load_balancer_arns = [aws_lb.ec2-networking.arn]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc_endpoint_service.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_vpc_endpoint_subnet_association" "app" {
  vpc_endpoint_id = aws_vpc_endpoint.app.id
  subnet_id       = aws_subnet.ec2-networking.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: operating_regions is a required block, but its own region_name argument is validated by the provider as a well-formed AWS region (validate: "doesn't look like AWS Region"), and the generic placeholder string is not one
resource "aws_vpc_ipam" "app" {
  operating_regions {
    region_name = "us-east-1"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc_ipam.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: address_family
resource "aws_vpc_ipam_pool" "app" {
  address_family = "ipv4"
  ipam_scope_id  = aws_vpc_ipam_scope.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc_ipam_pool.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: cidr
resource "aws_vpc_ipam_pool_cidr" "app" {
  cidr         = "172.20.0.0/16"
  ipam_pool_id = aws_vpc_ipam_pool.app.id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: operating_regions is a required block, the same shape as aws_vpc_ipam above; region_name is validated as a well-formed AWS region and the generic placeholder string is not one
resource "aws_vpc_ipam_resource_discovery" "app" {
  operating_regions {
    region_name = "us-east-1"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc_ipam_resource_discovery.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_vpc_ipam_resource_discovery_association" "app" {
  ipam_id                    = aws_vpc_ipam.app.id
  ipam_resource_discovery_id = aws_vpc_ipam_resource_discovery.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc_ipam_resource_discovery_association.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description
resource "aws_vpc_ipam_scope" "app" {
  ipam_id     = aws_vpc_ipam.app.id
  description = "Another Scope"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc_ipam_scope.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: peer_vpc_id, accepter.allow_remote_vpc_dns_resolution, requester.allow_remote_vpc_dns_resolution
resource "aws_vpc_peering_connection" "app" {
  peer_vpc_id = aws_vpc.ec2-networking.id
  vpc_id      = aws_vpc_dhcp_options_association.app.vpc_id
  accepter {
    allow_remote_vpc_dns_resolution = true
  }
  requester {
    allow_remote_vpc_dns_resolution = true
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc_peering_connection.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: static_routes_only, type, vpn_gateway_id
resource "aws_vpn_connection" "app" {
  customer_gateway_id = aws_customer_gateway.app.id
  type                = "ipsec.1"
  static_routes_only  = true
  vpn_gateway_id      = aws_vpn_gateway.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpn_connection.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: vpc_id
resource "aws_vpn_gateway" "app" {
  vpc_id = aws_vpc.ec2-networking.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpn_gateway.app"
  }
}
