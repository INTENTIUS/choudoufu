# Supporting, not coverage - added only so aws_vpc.ec2-networking's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: cidr_block
resource "aws_vpc" "ec2-networking" {
  cidr_block = "10.0.0.0/16"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc.ec2-networking"
  }
}
