# Supporting, not coverage - added only so aws_api_gateway_rest_api.lambda's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: body, endpoint_configuration.types
resource "aws_api_gateway_rest_api" "lambda" {
  name = "tofu-lambda-cohort-api-gateway-rest-api"
  body = "{\"info\":{\"title\":\"example\",\"version\":\"1.0\"},\"openapi\":\"3.0.1\",\"paths\":{\"/path1\":{\"get\":{\"x-amazon-apigateway-integration\":{\"httpMethod\":\"GET\",\"payloadFormatVersion\":\"1.0\",\"type\":\"HTTP_PROXY\",\"uri\":\"https://ip-ranges.amazonaws.com/ip-ranges.json\"}}}}}"
  endpoint_configuration {
    types = ["REGIONAL"]
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_api_gateway_rest_api.lambda"
  }
}

# Supporting, not coverage - added only so aws_iam_role.lambda's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires "assume_role_policy" as a plain string, but the provider validates it is well-formed JSON (validate: "\"assume_role_policy\" contains an invalid JSON"); the generic string placeholder is not JSON
resource "aws_iam_role" "lambda" {
  name = "tofu-lambda-cohort-iam-role"
  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = { Service = "lambda.amazonaws.com" }
      Action    = "sts:AssumeRole"
    }]
  })
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iam_role.lambda"
  }
}

# Supporting, not coverage - added only so aws_s3_bucket.lambda's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_s3_bucket" "lambda" {
  bucket = "tofu-lambda-cohort-s3-bucket"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_s3_bucket.lambda"
  }
}
