# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "sns_topic_arn" is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "\"sns_topic_arn\" (placeholder) is an invalid ARN: arn: invalid prefix"); no aws_sns_topic is part of this cohort to reference
resource "aws_docdb_event_subscription" "app" {
  name          = "tofu-databases-cohort-docdb-event-subscription"
  sns_topic_arn = "arn:aws:sns:us-east-1:000000000000:tofu-databases-cohort-events"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_docdb_event_subscription.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" was mis-wired to aws_docdb_event_subscription.app.name (this type is server-assigned per internal/live/identity/table.go, so identityArgName never claims "name" as its own, and parentRef's same-name search picks the alphabetically-first sibling that does); corrected to a literal name. "auth_type" is a required argument the provider validates against a closed enum (PLAIN_TEXT, SECRET_ARN per the provider's own Argument Reference), and the generic placeholder string is neither. "shard_capacity" and "shard_count" are both required integers the generic pass leaves at their zero value, which the provider's own documented Argument Reference says is below the minimum in practice (not caught by validate, found by reading the provider's example usage) - set to the documented example's own values
resource "aws_docdbelastic_cluster" "app" {
  admin_user_name     = "tofu-databases-cohort-docdbelastic-cluster"
  admin_user_password = "placeholder"
  auth_type           = "PLAIN_TEXT"
  name                = "tofu-databases-docdbelastic-cluster"
  shard_capacity      = 2
  shard_count         = 1
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_docdbelastic_cluster.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "domain_name" is a required string the schema does not constrain, but the provider validates it against a closed shape (validate: "must start with a lowercase alphabet and be at least 3 and no more than 28 characters long. Valid characters are a-z (lowercase letters), 0-9, and - (hyphen)"); the generic tofu-<cohort>-cohort-<type> placeholder is 44 characters and carries no uppercase, but is otherwise disqualified purely on length
resource "aws_elasticsearch_domain" "app" {
  domain_name = "tofu-db-es-domain"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_elasticsearch_domain.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" is a required string the schema does not constrain, but the provider validates it against a closed shape (validate: "The name can have up to 48 characters. It must begin with an alpha-numeric character and can only contain alpha-numeric characters and underscores."); the generic placeholder is hyphenated
resource "aws_keyspaces_keyspace" "app" {
  name = "tofu_databases_cohort_keyspaces_keyspace"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_keyspaces_keyspace.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "keyspace_name" and "table_name" are both required strings the schema does not constrain, but the provider validates both against the same closed shape as aws_keyspaces_keyspace's own "name" above (validate: "The keyspace/table name can have up to 48 characters..."), and the generic pass's placeholders are both hyphenated; keyspace_name is also wired to the sibling aws_keyspaces_keyspace this cohort renders rather than left as an unrelated literal, since a table genuinely belongs to a keyspace. schema_definition.column.type is required and the provider validates it against its own lower-case CQL type-name shape (validate: "The type must consist of lower case alphanumerics and an optional list of upto two lower case alphanumerics enclosed in angle brackets '<>'."); the generic placeholder is neither lower-case nor a real type, set to "text"
resource "aws_keyspaces_table" "app" {
  keyspace_name = aws_keyspaces_keyspace.app.name
  table_name    = "tofu_databases_cohort_keyspaces_table"
  schema_definition {
    column {
      name = "id"
      type = "text"
    }
    partition_key {
      name = "id"
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_keyspaces_table.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_memorydb_acl" "app" {
  name = "tofu-databases-cohort-memorydb-acl"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_memorydb_acl.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: node_type
resource "aws_memorydb_cluster" "app" {
  name      = "tofu-databases-cohort-memorydb-cluster"
  acl_name  = "tofu-databases-cohort-memorydb-cluster"
  node_type = "db.t4g.small"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_memorydb_cluster.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: multi_region_cluster_name_suffix, node_type
resource "aws_memorydb_multi_region_cluster" "app" {
  multi_region_cluster_name_suffix = "example"
  node_type                        = "db.r7g.xlarge"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_memorydb_multi_region_cluster.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: family
resource "aws_memorydb_parameter_group" "app" {
  name   = "tofu-databases-cohort-memorydb-parameter-group"
  family = "memorydb_redis6"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_memorydb_parameter_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_memorydb_subnet_group" "app" {
  name       = "tofu-databases-cohort-memorydb-subnet-group"
  subnet_ids = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_memorydb_subnet_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: authentication_mode.type is a required argument the provider validates against a closed enum (validate: "expected type to be one of [\"password\" \"iam\"], got placeholder"); set to "password", which the provider's own documented example pairs with a "passwords" list the generic pass never sets (Optional in the schema, but the API rejects a password-mode user with none) - added here for the same apply-time reason aws_backup_restore_testing_plan's recovery_point_selection above is
resource "aws_memorydb_user" "app" {
  user_name     = "tofu-databases-cohort-memorydb-user"
  access_string = "placeholder"
  authentication_mode {
    type      = "password"
    passwords = ["TofuDatabasesCohortPassw0rd2026"]
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_memorydb_user.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description, family
resource "aws_neptune_cluster_parameter_group" "app" {
  name        = "tofu-databases-cohort-neptune-cluster-parameter-group"
  family      = "neptune1"
  description = "neptune cluster parameter group"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_neptune_cluster_parameter_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: family
resource "aws_neptune_parameter_group" "app" {
  name   = "tofu-databases-cohort-neptune-parameter-group"
  family = "neptune1"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_neptune_parameter_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_neptune_subnet_group" "app" {
  name       = "tofu-databases-cohort-neptune-subnet-group"
  subnet_ids = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_neptune_subnet_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: Same "domain_name" shape constraint as aws_elasticsearch_domain above (validate: "must start with a lowercase alphabet and be at least 3 and no more than 28 characters long..."); the generic placeholder is 40 characters
resource "aws_opensearch_domain" "app" {
  domain_name = "tofu-db-os-domain"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_opensearch_domain.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" was mis-wired to aws_docdb_event_subscription.app.name (this type's identity is the composite name+type pair, more than one Component, so identityArgName never claims "name" as its own single-component identity - the same shape gen.go:116's "len(entry.Components) != 1" comment describes); corrected to a literal name. "type" is required and the provider validates it against a one-member closed enum (must be "data", per the provider's own Argument Reference); the generic placeholder is not. "policy" is a required string the provider validates as well-formed JSON matching its own access-policy shape (Rules/Principal), confirmed against the provider's documented example
resource "aws_opensearchserverless_access_policy" "app" {
  name = "tofu-db-access-policy"
  policy = jsonencode([
    {
      Rules = [
        {
          ResourceType = "collection"
          Resource     = ["collection/${aws_opensearchserverless_collection.app.name}"]
          Permission   = ["aoss:*"]
        }
      ]
      Principal = ["arn:aws:iam::000000000000:root"]
    }
  ])
  type = "data"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" mis-wired the same way as aws_opensearchserverless_access_policy above; corrected to a literal name. AWS itself refuses to create a collection with no matching encryption security policy (not caught by validate; found by reading the provider's own documented example, which sequences an aws_opensearchserverless_security_policy before its collection with an explicit depends_on) - wired to this cohort's own aws_opensearchserverless_security_policy the same way
resource "aws_opensearchserverless_collection" "app" {
  name       = "tofu-db-collection"
  depends_on = [aws_opensearchserverless_security_policy.app]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_opensearchserverless_collection.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" mis-wired the same way as aws_opensearchserverless_access_policy above; corrected to a literal name. "standby_replicas" is required and the provider validates it against a closed enum (ENABLED, DISABLED per the provider's own Argument Reference); the generic placeholder is neither
resource "aws_opensearchserverless_collection_group" "app" {
  name             = "tofu-db-collection-group"
  standby_replicas = "ENABLED"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_opensearchserverless_collection_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" mis-wired the same way as aws_opensearchserverless_access_policy above; corrected to a literal name. "type" is required and the provider validates it against a one-member closed enum (must be "retention", per the provider's own Argument Reference); the generic placeholder is not. "policy" is a required string the provider validates as well-formed JSON matching its own lifecycle-policy shape (Rules with ResourceType/Resource/MinIndexRetention), confirmed against the provider's documented example
resource "aws_opensearchserverless_lifecycle_policy" "app" {
  name = "tofu-db-lifecycle-policy"
  policy = jsonencode({
    Rules = [
      {
        ResourceType      = "index"
        Resource          = ["index/tofu-db-collection/*"]
        MinIndexRetention = "30d"
      }
    ]
  })
  type = "retention"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" mis-wired the same way as aws_opensearchserverless_access_policy above; corrected to a literal name. "type" is required and the provider validates it against a closed enum (encryption, network per the provider's own Argument Reference); the generic placeholder is neither. "policy" is a required string the provider validates as well-formed JSON matching its own encryption-policy shape (Rules/AWSOwnedKey), confirmed against the provider's documented example - the policy's Resource pattern targets this cohort's own aws_opensearchserverless_collection by name, since it is the encryption policy that type's own override depends_on
resource "aws_opensearchserverless_security_policy" "app" {
  name = "tofu-db-security-policy"
  policy = jsonencode({
    Rules = [
      {
        ResourceType = "collection"
        Resource     = ["collection/tofu-db-collection"]
      }
    ]
    AWSOwnedKey = true
  })
  type = "encryption"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" is a required string the schema does not constrain, but the provider validates its length (validate: "expected length of name to be in the range (1 - 32), got tofu-databases-cohort-qldb-ledger"), 34 characters against a 32-character limit. "permissions_mode" is required and the provider validates it against a closed enum (ALLOW_ALL, STANDARD per the provider's own Argument Reference); the generic placeholder is neither, and STANDARD is the value the provider's own docs recommend over the legacy ALLOW_ALL
resource "aws_qldb_ledger" "app" {
  name             = "tofu-db-qldb-ledger"
  permissions_mode = "STANDARD"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_qldb_ledger.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: neither "manage_master_password" nor "master_password" is Required in the wire schema (the provider accepts either), so the generic required-only pass sets neither, and validate does not catch the gap - but the provider's own plan-time logic refuses the combination outright (apply: "one of \"manage_master_password\" or \"master_password\" is required"), found only by exercising a real apply against floci
resource "aws_redshift_cluster" "app" {
  cluster_identifier = "tofu-databases-cohort-redshift-cluster"
  node_type          = "placeholder"
  master_password    = "TofuDatabasesCohortPassw0rd"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_redshift_cluster.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: family
resource "aws_redshift_parameter_group" "app" {
  name   = "tofu-databases-cohort-redshift-parameter-group"
  family = "redshift-1.0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_redshift_parameter_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "definitions" is a list of schedule expressions the schema types as unconstrained strings; the generic placeholder is neither a documented cron nor rate expression (not caught by validate, found by reading the provider's own documented example) - set to that same example's "rate(12 hours)"
resource "aws_redshift_snapshot_schedule" "app" {
  identifier  = "tofu-databases-cohort-redshift-snapshot-schedule"
  definitions = ["rate(12 hours)"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_redshift_snapshot_schedule.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_redshift_subnet_group" "app" {
  name       = "tofu-databases-cohort-redshift-subnet-group"
  subnet_ids = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_redshift_subnet_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_redshiftserverless_namespace" "app" {
  namespace_name = "tofu-databases-cohort-redshiftserverless-namespace"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_redshiftserverless_namespace.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_redshiftserverless_workgroup" "app" {
  workgroup_name = "tofu-databases-cohort-redshiftserverless-workgroup"
  namespace_name = aws_redshiftserverless_namespace.app.namespace_name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_redshiftserverless_workgroup.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" mis-wired the same way as aws_docdbelastic_cluster above (this type is also server-assigned); corrected to a literal name. "db_instance_type" is required and the plugin-framework schema validates it against a closed enum (validate: "Invalid String Enum Value" - db.influx.medium, db.influx.large, ...); the generic placeholder is not a member. "vpc_security_group_ids" and "vpc_subnet_ids" are both required lists of strings the framework schema validates by regular expression (^sg-[a-z0-9]+$ and ^subnet-[a-z0-9]+$ respectively); the generic placeholder string matches neither. "allocated_storage", "bucket", "organization", "password" and "username" are all Optional in the wire schema, so the generic required-only pass never sets them, but the provider's own plan-time business logic requires all five for a V2 cluster (not caught by validate; found by exercising a real apply against floci: "Missing Required Configuration for InfluxDB V2": "allocated_storage/bucket/organization/password/username is required for InfluxDB V2 clusters") - added by hand the same way aws_timestreaminfluxdb_db_instance's own allocated_storage already is. "password" also has its own regular-expression shape (validate: "Attribute password value must match regular expression '^[a-zA-Z0-9]+$'"), found the same apply-time way; the generic cohort-derived literal that would otherwise land here is hyphenated, so this one is alphanumeric-only instead
resource "aws_timestreaminfluxdb_db_cluster" "app" {
  db_instance_type       = "db.influx.medium"
  name                   = "tofu-db-influxdb-cluster"
  vpc_security_group_ids = ["sg-0123456789abcdef0"]
  vpc_subnet_ids         = ["subnet-0123456789abcdef0", "subnet-0123456789abcdef1"]
  allocated_storage      = 20
  bucket                 = "tofu-databases-cohort-influxdb"
  organization           = "tofu-databases-cohort"
  password               = "TofuDatabasesCohortPassw0rd2026"
  username               = "admin"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_timestreaminfluxdb_db_cluster.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: Same "name" mis-wiring, "db_instance_type" enum, and "vpc_security_group_ids"/"vpc_subnet_ids" regular-expression shapes as aws_timestreaminfluxdb_db_cluster above. "allocated_storage" is a required integer the framework schema validates as 20-16384 (validate: "Attribute allocated_storage value must be between 20 and 16384, got: 0"); the generic pass's zero value is below the minimum
resource "aws_timestreaminfluxdb_db_instance" "app" {
  allocated_storage      = 20
  bucket                 = "placeholder"
  db_instance_type       = "db.influx.medium"
  name                   = "tofu-db-influxdb-instance"
  organization           = "placeholder"
  password               = "placeholder"
  username               = "placeholder"
  vpc_security_group_ids = ["sg-0123456789abcdef0"]
  vpc_subnet_ids         = ["subnet-0123456789abcdef0", "subnet-0123456789abcdef1"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_timestreaminfluxdb_db_instance.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" mis-wired the same way as aws_docdbelastic_cluster above (this type is also server-assigned). schedule_configuration, error_report_configuration, notification_configuration and target_configuration are all required blocks the wire schema marks optional-in-shape while the plugin framework requires each present in practice (validate: "Block ... must have a configuration value as the provider has marked it as required"); the generic required-only pass never visits any of the four since none is Required at the top level, so all four - and every one of their own required nested fields - are added by hand here, following the provider's own documented example verbatim
resource "aws_timestreamquery_scheduled_query" "app" {
  execution_role_arn = aws_iam_role.databases.arn
  name               = "tofu-db-scheduled-query"
  query_string       = "placeholder"
  schedule_configuration {
    schedule_expression = "rate(1 hour)"
  }
  error_report_configuration {
    s3_configuration {
      bucket_name = "tofu-databases-cohort-scheduled-query-errors"
    }
  }
  notification_configuration {
    sns_configuration {
      topic_arn = "arn:aws:sns:us-east-1:000000000000:tofu-databases-cohort-scheduled-query"
    }
  }
  target_configuration {
    timestream_configuration {
      database_name = aws_timestreamwrite_database.app.database_name
      table_name    = aws_timestreamwrite_table.app.table_name
      time_column   = "time"
      dimension_mapping {
        name                 = "region"
        dimension_value_type = "VARCHAR"
      }
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_timestreamquery_scheduled_query.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_timestreamwrite_database" "app" {
  database_name = "tofu-databases-cohort-timestreamwrite-database"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_timestreamwrite_database.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_timestreamwrite_table" "app" {
  database_name = aws_timestreamwrite_database.app.database_name
  table_name    = "tofu-databases-cohort-timestreamwrite-table"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_timestreamwrite_table.app"
  }
}
