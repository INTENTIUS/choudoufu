# iam-ecr cohort: generator facts

Generated by `tools/estate-gen`; rewritten in full on every run. Hand
narrative belongs in README.md, which the generator never rewrites.

Cohort: `iam-ecr`. Generated by `tools/estate-gen` (issue #56) from hashicorp/aws 6.59.0's schema and `internal/live/identity/table.go`'s identity table.

Regenerate with:

```
go run ./tools/estate-gen -cohort iam-ecr -types aws_ecr_registry_policy,aws_ecr_registry_scanning_configuration,aws_ecr_replication_configuration,aws_ecr_repository,aws_iam_group,aws_iam_instance_profile,aws_iam_service_linked_role,aws_iam_user -out live/e2e/estates/iam-ecr
```

## Provenance

| Resource | Kind | Overrides |
|---|---|---|
| `aws_ecr_registry_policy.app` | coverage | schema requires "policy" as a plain string, but the provider validates it is well-formed JSON (validate: "\"policy\" contains an invalid JSON"); set to the hand-written cohort's own registry policy - an allow-pull statement for the account root principal, kept verbatim (the Sid charset permits no cohort-derived hyphens) |
| `aws_ecr_registry_scanning_configuration.app` | coverage | scan_type is a required argument the provider validates as an enum (validate: "expected scan_type to be one of [\"BASIC\" \"ENHANCED\"], got placeholder"); set to "BASIC", the member the hand-written cohort used |
| `aws_ecr_replication_configuration.app` | coverage | replication_configuration is optional in the wire schema (MinItems 0), so the generic pass renders an empty resource - but a replication configuration with no rule configures nothing, and the destination's region and registry_id carry provider-side shape validation (a region name and a 12-digit account ID) no generic placeholder satisfies. The hand-written block's literal us-west-2/000000000000 destination is kept: a placeholder region and registry ID rather than a second real account, the same "keep the block out of the emulator's boundary" choice live/e2e/estates/lambda's placeholder ARNs make |
| `aws_ecr_repository.app` | coverage | none |
| `aws_iam_group.app` | coverage | none |
| `aws_iam_instance_profile.app` | coverage | none |
| `aws_iam_service_linked_role.app` | coverage | aws_service_name is a required argument the provider validates by shape (validate: "must be a full service hostname e.g. elasticbeanstalk.amazonaws.com"); set to elasticbeanstalk.amazonaws.com, the provider's own documented example service, as the hand-written cohort chose |
| `aws_iam_user.app` | coverage | none |
| `aws_iam_role.iam-ecr` | supporting, not coverage | schema requires "assume_role_policy" as a plain string, but the provider validates it is well-formed JSON (validate: "\"assume_role_policy\" contains an invalid JSON"); the generic string placeholder is not JSON |

## Requested types

- `aws_ecr_registry_policy`
- `aws_ecr_registry_scanning_configuration`
- `aws_ecr_replication_configuration`
- `aws_ecr_repository`
- `aws_iam_group`
- `aws_iam_instance_profile`
- `aws_iam_service_linked_role`
- `aws_iam_user`

## Files

| File | Contents |
|---|---|
| `versions.tf` | `terraform`/`provider "aws"` blocks, identical in shape to `live/e2e/estate/versions.tf`. |
| `locals.tf` | `estate_tag` — "iam-ecr-cohort", distinct from every other cohort's own tag. |
| `iam-ecr.tf` | Every requested (coverage) resource. |
| `supporting.tf` | Resources this generator added on its own so a required argument had something to reference - not a coverage row (see the Provenance table above). |

## Gating

Nothing here runs against a live or emulated cloud. `go test ./internal/live/lint/... ./internal/live/identity/...` picks this
directory up through `internal/live/flocitest.FixtureDirs` (#48's union pin).

`terraform validate` passes against the real provider release (6.59.0) this fixture pins.
