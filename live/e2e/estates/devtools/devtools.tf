# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: domain
resource "aws_codeartifact_domain" "app" {
  region = "placeholder"
  domain = "example"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codeartifact_domain.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_codeartifact_domain_permissions_policy" "app" {
  region = "placeholder"
  domain = "example"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: repository
resource "aws_codeartifact_repository" "app" {
  region     = "placeholder"
  domain     = "example"
  repository = "example"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codeartifact_repository.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: "policy_document" is a required string the schema does not constrain, but the provider validates it is well-formed JSON (validate: "\"policy_document\" contains an invalid JSON"); the generic placeholder string is not
resource "aws_codeartifact_repository_permissions_policy" "app" {
  region = "placeholder"
  domain = "example"
  policy_document = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = "*"
      Action    = "codeartifact:ReadFromRepository"
      Resource  = "*"
    }]
  })
  repository = "example"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: base_capacity is Required and the provider validates it is at least 1 (validate: "expected base_capacity to be at least (1), got 0"), but the schema types it only as a number, so the generic pass's zero placeholder fails; compute_type and environment_type are both Required strings the schema does not constrain to an enum, but the provider validates each against a fixed set (validate: "expected ... to be one of [...]")
resource "aws_codebuild_fleet" "app" {
  name             = "tofu-devtools-cohort-codebuild-fleet"
  base_capacity    = 1
  compute_type     = "BUILD_GENERAL1_SMALL"
  environment_type = "LINUX_CONTAINER"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codebuild_fleet.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: "service_role" is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN"), the same isRoleArg gap aws_codedeploy_deployment_group's own service_role_arn does not have (this argument name lacks the "_role_arn" suffix the alias matches); artifacts.type, environment.compute_type, environment.type and source.type are all required strings the schema does not constrain to an enum, but the provider validates each against a fixed set (validate: "expected ... to be one of [...]"); source.buildspec is Optional in the schema, but the provider requires it when source.type is NO_SOURCE (apply-time only: "buildspec must be set when source's type is NO_SOURCE", not caught by "terraform validate")
resource "aws_codebuild_project" "app" {
  name         = "tofu-devtools-cohort-codebuild-project"
  service_role = aws_iam_role.devtools.arn
  artifacts {
    type = "NO_ARTIFACTS"
  }
  environment {
    compute_type = "BUILD_GENERAL1_SMALL"
    image        = "placeholder"
    type         = "LINUX_CONTAINER"
  }
  source {
    type      = "NO_SOURCE"
    buildspec = "version: 0.2"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codebuild_project.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: type is a required string the provider validates against a fixed enum (validate: "expected type to be one of [\"TEST\" \"CODE_COVERAGE\"]"); export_config.type is likewise a required, enum-validated string (validate: "expected type to be one of [\"S3\" \"NO_EXPORT\"]")
resource "aws_codebuild_report_group" "app" {
  region = "placeholder"
  name   = "tofu-devtools-cohort-codebuild-report-group"
  type   = "TEST"
  export_config {
    type = "NO_EXPORT"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codebuild_report_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: build_type, filter_group.filter.pattern, filter_group.filter.type
resource "aws_codebuild_webhook" "app" {
  project_name = "tofu-devtools-cohort-codebuild-webhook"
  build_type   = "BUILD"
  filter_group {
    filter {
      pattern = "PUSH"
      type    = "EVENT"
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description
resource "aws_codecommit_repository" "app" {
  repository_name = "tofu-devtools-cohort-codecommit-repository"
  description     = "This is the Sample App Repository"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codecommit_repository.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" is a required string; the generic pass's cross-resource-reference heuristic points it at this cohort's own aws_codebuild_fleet.app.name (both arguments happen to be named "name"), but the provider validates connection names to at most 32 characters (apply: "Attribute name string length must be between 1 and 32") and the fleet's own identity-argument placeholder is longer than that - not caught by "terraform validate" itself (the referenced value is already known at plan time, but the provider's ValidateFunc only runs at apply), only surfaced at apply
resource "aws_codeconnections_connection" "app" {
  name = "tofu-devtools-connection"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codeconnections_connection.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: compute_platform
resource "aws_codedeploy_app" "app" {
  name             = "tofu-devtools-cohort-codedeploy-app"
  compute_platform = "ECS"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codedeploy_app.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: minimum_healthy_hosts.type, minimum_healthy_hosts.value
resource "aws_codedeploy_deployment_config" "app" {
  deployment_config_name = "tofu-devtools-cohort-codedeploy-deployment-config"
  minimum_healthy_hosts {
    type  = "HOST_COUNT"
    value = 2
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: app_name is a required string, but this type's identity is a two-attribute composite (internal/live/identity/table.go's Components: attr("app_name"), sep(":"), attr("deployment_group_name")), so identityArgName's own-identity convention does not fire (it only names a single-component identity's argument) and the generic pass left app_name as its own tofu-<cohort>-<type> placeholder instead of the sibling aws_codedeploy_app.app's real name - not a validate error (both are plain strings), but a plan-then-apply-time one (apply: "ApplicationDoesNotExistException: Application does not exist: ..."), so this override wires the cross-resource reference issue #56 asks for by hand.
resource "aws_codedeploy_deployment_group" "app" {
  app_name              = aws_codedeploy_app.app.name
  deployment_group_name = "tofu-devtools-cohort-codedeploy-deployment-group"
  service_role_arn      = aws_iam_role.devtools.arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codedeploy_deployment_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: artifact_store.type is a required string the provider validates against a one-member enum (validate: "expected type to be one of [\"S3\"]"); "role_arn" is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN"), the same isRoleArg gap as aws_codebuild_project's service_role above ("role_arn" alone does not end "_role_arn"); each stage's action.category, action.owner and action.version are required strings the schema does not constrain to an enum or a length range, but the provider validates each (validate: "expected category to be one of [...]", "expected owner to be one of [...]", "expected length of ... version to be in the range (1 - 9)")
resource "aws_codepipeline" "app" {
  name     = "tofu-devtools-cohort-codepipeline"
  role_arn = aws_iam_role.devtools.arn
  artifact_store {
    location = "placeholder"
    type     = "S3"
  }
  stage {
    name = "placeholder"
    action {
      category = "Source"
      name     = "placeholder"
      owner    = "AWS"
      provider = "S3"
      version  = "1"
    }
  }
  stage {
    name = "placeholder"
    action {
      category = "Approval"
      name     = "placeholder"
      owner    = "AWS"
      provider = "Manual"
      version  = "1"
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codepipeline.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: category is a required string the provider validates against a fixed enum (validate: "expected category to be one of [...]"); provider_name is this type's own identity argument (internal/live/identity/table.go), but the generic pass's tofu-<cohort>-<type> placeholder exceeds the provider's documented 35-character limit (validate: "expected length of provider_name to be in the range (1 - 35)"); version is a required string the provider validates is 1-9 characters (validate: "expected length of version to be in the range (1 - 9)")
resource "aws_codepipeline_custom_action_type" "app" {
  category      = "Build"
  provider_name = "tofu-devtools-action"
  version       = "1"
  input_artifact_details {
    maximum_count = 0
    minimum_count = 0
  }
  output_artifact_details {
    maximum_count = 0
    minimum_count = 0
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codepipeline_custom_action_type.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: authentication is a required string the provider validates against a fixed enum (validate: "expected authentication to be one of [...]"); UNAUTHENTICATED needs no matching auth_configuration block, unlike GITHUB_HMAC (a secret_token) or IP (an allowed_ip_range), keeping this override to the one attribute. "name" is also overridden away from the generic pass's cross-reference to aws_codebuild_fleet.app.name: CodeBuild::Fleet has no working create handler at all against the pinned floci image (see this cohort's README, "Verifying by hand"), so that reference made every plan against this fixture depend on a resource guaranteed to fail create, hiding this type's own result behind an unrelated one.
resource "aws_codepipeline_webhook" "app" {
  region          = "placeholder"
  authentication  = "UNAUTHENTICATED"
  name            = "tofu-devtools-webhook"
  target_action   = "placeholder"
  target_pipeline = "placeholder"
  filter {
    json_path    = "placeholder"
    match_equals = "placeholder"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codepipeline_webhook.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: Same cross-reference-length gap as aws_codeconnections_connection above, its CodeStarConnections predecessor - the provider validates this type's name to the same 32-character limit.
resource "aws_codestarconnections_connection" "app" {
  name = "tofu-devtools-starconn"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codestarconnections_connection.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: detail_type is a required string the provider validates against a fixed enum (validate: "expected detail_type to be one of [\"BASIC\" \"FULL\"]"); "resource" is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN") - overridden to this cohort's own aws_codebuild_project.app.arn, the cross-resource reference issue #56 asks for, since this argument names an arbitrary already-admitted resource to watch rather than matching identityArgName's own-identity convention
resource "aws_codestarnotifications_notification_rule" "app" {
  detail_type    = "BASIC"
  event_type_ids = ["placeholder"]
  name           = "tofu-devtools-cohort-codestarnotifications-notification-rule"
  resource       = aws_codebuild_project.app.arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codestarnotifications_notification_rule.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_ecrpublic_repository" "app" {
  repository_name = "tofu-devtools-cohort-ecrpublic-repository"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ecrpublic_repository.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: "policy" is a required string the schema does not constrain, but the provider validates it is well-formed JSON (validate: "\"policy\" contains an invalid JSON"); the generic placeholder string is not
resource "aws_ecrpublic_repository_policy" "app" {
  repository_name = aws_ecrpublic_repository.app.repository_name
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = "*"
      Action    = "ecr-public:DescribeRepositories"
    }]
  })
}
