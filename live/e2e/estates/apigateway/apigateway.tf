# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_api_gateway_account" "app" {
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_api_gateway_api_key" "app" {
  name = "tofu-apigateway-cohort-api-gateway-api-key"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_api_gateway_api_key.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: stage_name
resource "aws_api_gateway_base_path_mapping" "app" {
  domain_name = aws_api_gateway_domain_name.app.domain_name
  api_id      = aws_api_gateway_rest_api.app.id
  base_path   = "placeholder"
  stage_name  = aws_api_gateway_stage.app.stage_name
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description
resource "aws_api_gateway_client_certificate" "app" {
  description = "My client certificate"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_api_gateway_client_certificate.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: rest_api_id was mis-wired to aws_api_gateway_rest_api_policy.app (parentRef's only candidate whose identity self-names "rest_api_id" - the real parent, aws_api_gateway_rest_api, is server-assigned and never a parentRef candidate); corrected to the REST API this cohort renders
resource "aws_api_gateway_documentation_version" "app" {
  rest_api_id = aws_api_gateway_rest_api.app.id
  version     = "tofu-apigateway-cohort-docs-v1"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_api_gateway_domain_name" "app" {
  domain_name = "tofu-apigateway-cohort-api-gateway-domain-name"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_api_gateway_domain_name.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: access_association_source_type is a fixed enum (validate: "Invalid String Enum Value", valid values: VPCE); domain_name_arn is validated as a well-formed ARN (validate: "Invalid ARN Value") - both need real-shaped values, not the generic placeholder
resource "aws_api_gateway_domain_name_access_association" "app" {
  access_association_source      = "vpce-0123456789abcdef0"
  access_association_source_type = "VPCE"
  domain_name_arn                = "arn:aws:apigateway:us-east-1::/domainnames/tofu-apigateway-cohort-api-gateway-domain-name"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_api_gateway_domain_name_access_association.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: rest_api_id mis-wired the same way as aws_api_gateway_documentation_version above; response_type is a fixed enum the provider validates server-side (terraform validate does not catch "placeholder", but it is not one of the documented values), set to the provider docs' own example value
resource "aws_api_gateway_gateway_response" "app" {
  rest_api_id   = aws_api_gateway_rest_api.app.id
  response_type = "UNAUTHORIZED"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: rest_api_id/resource_id/http_method mis-wired or left as the generic placeholder the same way aws_api_gateway_method's were, for the same reason - corrected to the same REST API root resource and GET method aws_api_gateway_method.app already targets, so this integration is the method's own; type is a fixed enum (validate: "expected type to be one of [...]"), set to MOCK, the shape floci's PutIntegration/GetIntegration round-trip cleanly (verified by hand)
resource "aws_api_gateway_integration" "app" {
  rest_api_id = aws_api_gateway_rest_api.app.id
  http_method = "GET"
  resource_id = aws_api_gateway_rest_api.app.root_resource_id
  type        = "MOCK"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: rest_api_id/resource_id/http_method mis-wired or left as the generic placeholder the same way aws_api_gateway_integration's were above, and for the same reason - status_code is left schema-Required-but-unvalidated by terraform validate, but the provider expects a real HTTP status string, set to the aws_api_gateway_method_response.app row below's own value so the two agree
resource "aws_api_gateway_integration_response" "app" {
  rest_api_id = aws_api_gateway_rest_api.app.id
  http_method = "GET"
  resource_id = aws_api_gateway_rest_api.app.root_resource_id
  status_code = "200"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: rest_api_id mis-wired the same way as aws_api_gateway_documentation_version above; resource_id has no identity-table candidate at all because aws_api_gateway_resource is not admitted this batch (rejected), and that type cannot be added as supporting infrastructure either - every fixture resource, coverage or supporting, has to be an admitted type (TestAdmissionTableCoversEstate, TestTableCoversFixtureTypes) - so this method attaches to the REST API's own root_resource_id instead of a child resource, which needs no unadmitted type at all
resource "aws_api_gateway_method" "app" {
  rest_api_id   = aws_api_gateway_rest_api.app.id
  authorization = "NONE"
  http_method   = "GET"
  resource_id   = aws_api_gateway_rest_api.app.root_resource_id
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: rest_api_id/resource_id/http_method/status_code, the same corrections as aws_api_gateway_integration_response above and for the same reason
resource "aws_api_gateway_method_response" "app" {
  rest_api_id = aws_api_gateway_rest_api.app.id
  http_method = "GET"
  resource_id = aws_api_gateway_rest_api.app.root_resource_id
  status_code = "200"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: rest_api_id mis-wired the same way as aws_api_gateway_documentation_version above; stage_name has no identity-table candidate to auto-wire from (aws_api_gateway_stage's own identity is the two-component rest_api_id/stage_name pair, not a single self-named argument, so identityArgName never fires on it, the same gap aws_api_gateway_method_settings's own fold parent has), wired to the stage this cohort renders instead of the generic placeholder; method_path left as the generic placeholder is not a real method path, set to the */* wildcard the provider docs use for "every method of every resource in the stage"
resource "aws_api_gateway_method_settings" "app" {
  rest_api_id = aws_api_gateway_rest_api.app.id
  method_path = "*/*"
  stage_name  = aws_api_gateway_stage.app.stage_name
  settings {
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: rest_api_id mis-wired the same way as aws_api_gateway_documentation_version above; content_type and schema left as the generic placeholder would not be valid JSON, set to a minimal real value
resource "aws_api_gateway_model" "app" {
  rest_api_id  = aws_api_gateway_rest_api.app.id
  content_type = "application/json"
  name         = "tofu-apigateway-cohort-api-gateway-model"
  schema       = jsonencode({})
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: body, endpoint_configuration.types
resource "aws_api_gateway_rest_api" "app" {
  name = "tofu-apigateway-cohort-api-gateway-rest-api"
  body = "{\"info\":{\"title\":\"example\",\"version\":\"1.0\"},\"openapi\":\"3.0.1\",\"paths\":{\"/path1\":{\"get\":{\"x-amazon-apigateway-integration\":{\"httpMethod\":\"GET\",\"payloadFormatVersion\":\"1.0\",\"type\":\"HTTP_PROXY\",\"uri\":\"https://ip-ranges.amazonaws.com/ip-ranges.json\"}}}}}"
  endpoint_configuration {
    types = ["REGIONAL"]
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_api_gateway_rest_api.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires "policy" as a plain string, but the provider validates it is well-formed JSON (validate: "\"policy\" contains an invalid JSON"), the same shape as aws_s3_bucket_policy above
resource "aws_api_gateway_rest_api_policy" "app" {
  rest_api_id = aws_api_gateway_rest_api.app.id
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = "*"
      Action    = "execute-api:Invoke"
      Resource  = "${aws_api_gateway_rest_api.app.execution_arn}/*"
    }]
  })
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: rest_api_id mis-wired the same way as aws_api_gateway_documentation_version above; deployment_id has no identity-table candidate because aws_api_gateway_deployment is not admitted this batch (rejected), so it is left as the generic placeholder string - a stage is its own coverage row and the deployment it names existing is not this type's identity concern
resource "aws_api_gateway_stage" "app" {
  rest_api_id   = aws_api_gateway_rest_api.app.id
  deployment_id = "placeholder"
  stage_name    = "tofu-apigateway-cohort-api-gateway-stage"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_api_gateway_stage.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description, product_code, api_stages.api_id, api_stages.stage, quota_settings.limit, quota_settings.offset, quota_settings.period, throttle_settings.burst_limit, throttle_settings.rate_limit
resource "aws_api_gateway_usage_plan" "app" {
  name         = "tofu-apigateway-cohort-api-gateway-usage-plan"
  description  = "my description"
  product_code = "MYCODE"
  api_stages {
    api_id = aws_api_gateway_rest_api.app.id
    stage  = aws_api_gateway_stage.app.stage_name
  }
  quota_settings {
    limit  = 20
    offset = 2
    period = "WEEK"
  }
  throttle_settings {
    burst_limit = 5
    rate_limit  = 10
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_api_gateway_usage_plan.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: key_type
resource "aws_api_gateway_usage_plan_key" "app" {
  usage_plan_id = aws_api_gateway_usage_plan.app.id
  key_id        = aws_api_gateway_api_key.app.id
  key_type      = "API_KEY"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_api_gateway_vpc_link" "app" {
  name        = "tofu-apigateway-cohort-api-gateway-vpc-link"
  target_arns = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_api_gateway_vpc_link.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: protocol_type is a fixed enum (validate: "expected protocol_type to be one of [WEBSOCKET HTTP]"), not the generic placeholder
resource "aws_apigatewayv2_api" "app" {
  name          = "tofu-apigateway-cohort-apigatewayv2-api"
  protocol_type = "HTTP"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_apigatewayv2_api.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: domain_name_configuration's three required arguments are each validated: certificate_arn as a well-formed ARN (validate: "invalid ARN: arn: invalid prefix"), endpoint_type and security_policy as fixed enums (validate: "expected ... to be one of [...]")
resource "aws_apigatewayv2_domain_name" "app" {
  domain_name = "tofu-apigateway-cohort-apigatewayv2-domain-name"
  domain_name_configuration {
    certificate_arn = "arn:aws:acm:us-east-1:000000000000:certificate/tofu-apigateway-cohort-placeholder"
    endpoint_type   = "REGIONAL"
    security_policy = "TLS_1_2"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_apigatewayv2_domain_name.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: domain_name was mis-wired to aws_api_gateway_domain_name.app (the v1 type) - both v1 and v2 domain name types self-identify by the same argument name, and parentRef's alphabetic tiebreak prefers "aws_api_gateway_domain_name" over "aws_apigatewayv2_domain_name" with no way to tell they are different API generations; corrected to the v2 domain name this type actually needs. action and condition are both required blocks the schema marks optional-in-shape but the provider requires present (validate: "Block action/condition must have a configuration value"), and priority must be 1-1000000 (validate: "must be between 1 and 1000000, got: 0")
resource "aws_apigatewayv2_routing_rule" "app" {
  domain_name = aws_apigatewayv2_domain_name.app.domain_name
  priority    = 1
  action {
    invoke_api {
      api_id = aws_apigatewayv2_api.app.id
      stage  = aws_apigatewayv2_stage.app.name
    }
  }
  condition {
    match_base_paths {
      any_of = ["/"]
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_apigatewayv2_stage" "app" {
  api_id = aws_apigatewayv2_api.app.id
  name   = "tofu-apigateway-cohort-apigatewayv2-stage"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_apigatewayv2_stage.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_apigatewayv2_vpc_link" "app" {
  name               = "tofu-apigateway-cohort-apigatewayv2-vpc-link"
  security_group_ids = ["placeholder"]
  subnet_ids         = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_apigatewayv2_vpc_link.app"
  }
}
