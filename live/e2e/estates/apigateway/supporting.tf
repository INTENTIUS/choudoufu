# Supporting, not coverage - added only so aws_lb.apigateway's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: neither subnets nor subnet_mapping is schema-Required (both Optional in the wire schema), so the required-only pass leaves both unset and terraform validate refuses with "one of `subnet_mapping,subnets` must be specified" - a provider-side ExactlyOneOf-shaped rule the doc page states as a NOTE ("one of either subnets or subnet_mapping is required") rather than as schema.Required; live/import-grammar.json already extracts it as an exclusive_groups entry for aws_lb ([["subnet_mapping","subnets"]]), which estate-gen does not yet consume generically (a wider fix than this one cohort's gap). aws_api_gateway_vpc_link's v1 VPC Link only accepts a Network Load Balancer as its target ("List of network load balancer arns" - api_gateway_vpc_link.html.markdown), and ec2-networking's own aws_lb feeds a network_load_balancer_arns argument too, so load_balancer_type is set to network here rather than left at the application default - which also means one subnet is sufficient (floci's ElbV2Service only enforces the two-Availability-Zone rule for type "application").
resource "aws_lb" "apigateway" {
  load_balancer_type = "network"
  subnets            = [aws_subnet.apigateway.id]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_lb.apigateway"
  }
}

# Supporting, not coverage - added only so aws_subnet.apigateway's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: cidr_block
resource "aws_subnet" "apigateway" {
  vpc_id     = aws_vpc.apigateway.id
  cidr_block = "10.0.1.0/24"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_subnet.apigateway"
  }
}

# Supporting, not coverage - added only so aws_vpc.apigateway's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: cidr_block
resource "aws_vpc" "apigateway" {
  cidr_block = "10.0.0.0/16"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_vpc.apigateway"
  }
}
