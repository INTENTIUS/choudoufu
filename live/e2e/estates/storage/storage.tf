# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" is validated against a letters/numbers/underscores pattern with no hyphens (validate: "must be must be between 1 and 256 characters, starting with a letter, and consisting of letters, numbers, and underscores"), but the generic pass's placeholder name is hyphenated
resource "aws_backup_framework" "app" {
  name = "tofu_storage_cohort_backup_framework"
  control {
    name = "placeholder"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_backup_framework.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" is validated against '^[a-zA-Z0-9\-\_]{2,50}$' and the generic pass's placeholder name is 54 characters, past the limit; min_retention_days and max_retention_days are both optional in the schema but the provider requires min_retention_days >= 7 in practice (validate: "value must be at least 7")
resource "aws_backup_logically_air_gapped_vault" "app" {
  name               = "tofu-storage-lag-vault"
  max_retention_days = 30
  min_retention_days = 7
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_backup_logically_air_gapped_vault.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: rule.rule_name, rule.schedule, rule.target_vault_name, rule.lifecycle.delete_after
resource "aws_backup_plan" "app" {
  name = aws_backup_framework.app.name
  rule {
    rule_name         = "tf_example_backup_rule"
    target_vault_name = aws_backup_vault.app.name
    schedule          = "cron(0 12 * * ? *)"
    lifecycle {
      delete_after = 14
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_backup_plan.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" is validated against the same letters/numbers/underscores pattern as aws_backup_framework above, no hyphens; report_setting.report_template is a required argument the schema does not constrain to an enum, but the provider does (validate: "expected report_setting.0.report_template to be one of [...]")
resource "aws_backup_report_plan" "app" {
  name = "tofu_storage_cohort_backup_report_plan"
  report_delivery_channel {
    s3_bucket_name = "placeholder"
  }
  report_setting {
    report_template = "RESOURCE_COMPLIANCE_REPORT"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_backup_report_plan.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" is validated against a letters/numbers/underscores pattern with no hyphens, the same shape as aws_backup_framework above; recovery_point_selection is a required block the schema does not mark required (MinItems 0), and its own algorithm, include_vaults and recovery_point_types arguments are all required with no schema-visible default (validate: "Block recovery_point_selection must have a configuration value")
resource "aws_backup_restore_testing_plan" "app" {
  name                = "tofu_storage_cohort_restore_testing_plan"
  schedule_expression = "placeholder"
  recovery_point_selection {
    algorithm            = "LATEST_WITHIN_WINDOW"
    include_vaults       = ["*"]
    recovery_point_types = ["CONTINUOUS"]
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_backup_restore_testing_plan.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: kms_key_arn
resource "aws_backup_vault" "app" {
  name        = "tofu-storage-cohort-backup-vault"
  kms_key_arn = aws_kms_key.storage.arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_backup_vault.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_efs_access_point" "app" {
  file_system_id = aws_efs_file_system.app.id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_efs_access_point.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: creation_token
resource "aws_efs_file_system" "app" {
  creation_token = "my-product"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_efs_file_system.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: data_repository_path, file_system_id and file_system_path are all plain strings in the schema, but the provider validates their shape at plan time (validate: "must begin with s3://", "must begin with fs-", "path must begin with /"); the generic "placeholder" string satisfies none of the three
resource "aws_fsx_data_repository_association" "app" {
  data_repository_path = "s3://tofu-storage-cohort-bucket/data"
  file_system_id       = "fs-0123456789abcdef0"
  file_system_path     = "/data"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_fsx_data_repository_association.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: import_path, storage_capacity
resource "aws_fsx_lustre_file_system" "app" {
  subnet_ids       = [aws_subnet.storage.id]
  import_path      = aws_s3_bucket.storage.bucket
  storage_capacity = 1200
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_fsx_lustre_file_system.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: deployment_type is a plain string in the schema but the provider validates it against a fixed enum (validate: "expected deployment_type to be one of [...]"); storage_capacity is optional/computed but the provider requires it in the range 1024-1048576 when set (validate: "expected storage_capacity to be in the range"); throughput_capacity and throughput_capacity_per_ha_pair are each optional alone, but the provider requires exactly one (validate: "one of ... must be specified")
resource "aws_fsx_ontap_file_system" "app" {
  deployment_type     = "SINGLE_AZ_1"
  preferred_subnet_id = "placeholder"
  storage_capacity    = 1024
  subnet_ids          = [aws_subnet.storage.id]
  throughput_capacity = 128
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_fsx_ontap_file_system.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_fsx_ontap_storage_virtual_machine" "app" {
  file_system_id = aws_fsx_ontap_file_system.app.id
  name           = aws_backup_framework.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_fsx_ontap_storage_virtual_machine.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: size_in_bytes and size_in_megabytes are each optional alone, but the provider requires exactly one (validate: "one of ... must be specified"); storage_virtual_machine_id is a plain string in the schema but the provider validates its length against the real 21-character svm-… shape (validate: "expected length of storage_virtual_machine_id to be in the range (21 - 21)"), which the generic placeholder string is not
resource "aws_fsx_ontap_volume" "app" {
  name                       = aws_backup_framework.app.name
  storage_virtual_machine_id = "svm-0123456789abcdef0"
  size_in_megabytes          = 1024
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_fsx_ontap_volume.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: deployment_type, storage_capacity, throughput_capacity
resource "aws_fsx_openzfs_file_system" "app" {
  deployment_type     = "SINGLE_AZ_1"
  subnet_ids          = [aws_subnet.storage.id]
  throughput_capacity = 64
  storage_capacity    = 64
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_fsx_openzfs_file_system.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: volume_id is a plain string in the schema but the provider validates its length against the real 23-character fsvol-… shape (validate: "expected length of volume_id to be in the range (23 - 23)")
resource "aws_fsx_openzfs_snapshot" "app" {
  name      = aws_backup_framework.app.name
  volume_id = "fsvol-0123456789abcdef0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_fsx_openzfs_snapshot.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: parent_volume_id
resource "aws_fsx_openzfs_volume" "app" {
  name             = aws_backup_framework.app.name
  parent_volume_id = aws_fsx_openzfs_file_system.app.root_volume_id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_fsx_openzfs_volume.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: type is a plain string in the schema but the provider validates it against the enum ["OPENZFS" "ONTAP"] (validate: "Invalid String Enum Value"); openzfs_configuration is a required block for type = "OPENZFS" that the schema does not mark required, its own volume_id is required with no schema-visible default, and its nested file_system_identity block is itself required with a required "type" of its own (validate: "Block ... must have a configuration value" at each level)
resource "aws_fsx_s3_access_point_attachment" "app" {
  name = "tofu-storage-cohort-fsx-s3-access-point-attachment"
  type = "OPENZFS"
  openzfs_configuration {
    volume_id = aws_fsx_openzfs_volume.app.id
    file_system_identity {
      type = "POSIX"
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: throughput_capacity is optional/computed in the schema, rendered as the generic pass's numeric zero placeholder, but the provider validates it against a fixed set of MB/s values (validate: "expected throughput_capacity to be one of [8 16 32 ...]"), none of which is zero
resource "aws_fsx_windows_file_system" "app" {
  subnet_ids          = [aws_subnet.storage.id]
  throughput_capacity = 8
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_fsx_windows_file_system.app"
  }
}
