# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: blocked_input_messaging, blocked_outputs_messaging, description
resource "aws_bedrock_guardrail" "app" {
  blocked_input_messaging   = "example"
  blocked_outputs_messaging = "example"
  name                      = aws_iam_role.ai-location.name
  description               = "example"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrock_guardrail.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description
resource "aws_bedrock_inference_profile" "app" {
  name        = aws_iam_role.ai-location.name
  description = "Profile with tag for cost allocation tracking"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrock_inference_profile.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_bedrockagent_agent" "app" {
  agent_name              = "tofu-ai-location-cohort-bedrockagent-agent"
  agent_resource_role_arn = aws_iam_role.ai-location.arn
  foundation_model        = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagent_agent.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_bedrockagent_agent_alias" "app" {
  agent_alias_name = "tofu-ai-location-cohort-bedrockagent-agent-alias"
  agent_id         = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagent_agent_alias.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_bedrockagent_flow" "app" {
  execution_role_arn = aws_iam_role.ai-location.arn
  name               = aws_iam_role.ai-location.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagent_flow.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: role_arn is named exactly "role_arn", which isRoleArg's curated alias does not match (only "role" or a "*_role_arn" suffix); the generic placeholder string is not a well-formed ARN (validate: "cannot be parsed as an ARN"). knowledge_base_configuration is Required-in-shape but the generic pass never visits it (validate: "Block ... must have a configuration value"); type = "MANAGED" needs no storage_configuration (the provider's own docs: "storage_configuration is not required when knowledge_base_configuration.type is MANAGED"), but the provider still requires the matching managed_knowledge_base_configuration sub-block present even though every field inside it is Optional-with-defaults (validate: "must be configured when ... type equals MANAGED")
resource "aws_bedrockagent_knowledge_base" "app" {
  name     = aws_iam_role.ai-location.name
  role_arn = aws_iam_role.ai-location.arn
  knowledge_base_configuration {
    type = "MANAGED"
    managed_knowledge_base_configuration {
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagent_knowledge_base.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description
resource "aws_bedrockagent_prompt" "app" {
  name        = aws_iam_role.ai-location.name
  description = "My prompt description."
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagent_prompt.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: role_arn is the same bare-"role_arn" gap as aws_bedrockagent_knowledge_base above. agent_runtime_artifact and network_configuration are both Required-in-shape blocks the generic pass never visits (validate: "Block ... must have a configuration value"); container_configuration.container_uri and network_mode = "PUBLIC" are the minimal valid choice within each. agent_runtime_name must match ^[a-zA-Z][a-zA-Z0-9_]{0,47}$ (validate: "value must match regular expression"), which the generic tofu-<cohort>-cohort-<type> placeholder's hyphens violate
resource "aws_bedrockagentcore_agent_runtime" "app" {
  agent_runtime_name = "tofu_ai_location_cohort_agent_runtime"
  role_arn           = aws_iam_role.ai-location.arn
  agent_runtime_artifact {
    container_configuration {
      container_uri = "000000000000.dkr.ecr.us-east-1.amazonaws.com/tofu-ai-location-cohort-agent-runtime:latest"
    }
  }
  network_configuration {
    network_mode = "PUBLIC"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_agent_runtime.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name was mis-wired to aws_iam_role.<cohort>.name by the generic pass's same-name parentRef search, the same shape aws_bedrockagentcore_code_interpreter's own override above explains - this type's own name pattern (^[a-zA-Z][a-zA-Z0-9_]{0,47}$) surfaced only at apply time (terraform validate does not evaluate cross-resource references), against the pinned floci image during this batch's verification
resource "aws_bedrockagentcore_agent_runtime_endpoint" "app" {
  agent_runtime_id = "placeholder"
  name             = "tofu_ai_location_runtime_endpoint"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_agent_runtime_endpoint.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires exactly one of api_key/api_key_wo (validate: "No attribute specified when one (and only one) of [api_key,api_key_wo] is required"); the generic pass renders neither, since api_key is Sensitive and this generator's required-only pass only fills non-sensitive Required arguments
resource "aws_bedrockagentcore_api_key_credential_provider" "app" {
  name    = aws_iam_role.ai-location.name
  api_key = "tofu-cohort-placeholder-api-key"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_api_key_credential_provider.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: network_configuration is Required-in-shape but the generic pass never visits it (validate: "Block network_configuration must have a configuration value"); network_mode = "PUBLIC" is the minimal valid choice (vpc_config is Optional, only needed for network_mode = "VPC"). name is also mis-wired to aws_iam_role.<cohort>.name by the generic pass's same-name parentRef search (this type is server-assigned, so identityArgName never supplies its own name); left as-is rather than overridden, since aws_bedrockagentcore_browser's own "name" argument carries no format pattern in the provider's docs and the IAM role's name string is a harmless, if confusing, valid value here — unlike its siblings below, which do enforce ^[a-zA-Z][a-zA-Z0-9_]{0,47}$
resource "aws_bedrockagentcore_browser" "app" {
  name = aws_iam_role.ai-location.name
  network_configuration {
    network_mode = "PUBLIC"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_browser.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name was mis-wired to aws_iam_role.<cohort>.name by the generic pass's same-name parentRef search, the same shape aws_bedrockagentcore_code_interpreter's own override below explains - this type's own name pattern (^[a-zA-Z][a-zA-Z0-9_]{0,47}$) surfaced only at apply time (terraform validate does not evaluate cross-resource references), against the pinned floci image during this batch's verification
resource "aws_bedrockagentcore_browser_profile" "app" {
  name = "tofu_ai_location_browser_profile"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_browser_profile.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: same network_configuration gap as aws_bedrockagentcore_browser above. name was also mis-wired to aws_iam_role.<cohort>.name by the generic pass's same-name parentRef search (this type is server-assigned, so identityArgName never supplies its own name, and parentRef's fallback then matches any sibling that owns a "name" argument) - the same "mis-wired to a same-named sibling" shape the streaming batch's own aws_appsync_graphql_api needed corrected, except here the sibling it lands on happens to be an IAM role whose own name contains hyphens the ^[a-zA-Z][a-zA-Z0-9_]{0,47}$ pattern (validate: "must start with a letter and contain only letters, numbers, and underscores") rejects
resource "aws_bedrockagentcore_code_interpreter" "app" {
  name = "tofu_ai_location_code_interpreter"
  network_configuration {
    network_mode = "PUBLIC"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_code_interpreter.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: evaluator_config is Required-in-shape but the generic pass never visits it (validate: "Block evaluator_config must have a configuration value"); llm_as_a_judge with a numerical rating_scale is the minimal valid variant (code_based would need a real Lambda ARN this cohort has no sibling for). evaluator_name must match ^[a-zA-Z][a-zA-Z0-9_]{0,47}$ (validate: "must begin with a letter and contain only alphanumeric characters and underscores"), which the generic placeholder's hyphens violate. level is a fixed enum (TOOL_CALL/TRACE/SESSION), not the generic placeholder string
resource "aws_bedrockagentcore_evaluator" "app" {
  evaluator_name = "tofu_ai_location_cohort_evaluator"
  level          = "SESSION"
  evaluator_config {
    llm_as_a_judge {
      instructions = "Score the agent's helpfulness on the scale below."
      model_config {
        bedrock_evaluator_model_config {
          model_id = "anthropic.claude-3-haiku-20240307-v1:0"
        }
      }
      rating_scale {
        numerical {
          definition = "How helpful the agent's response was."
          label      = "Helpfulness"
          value      = 1
        }
      }
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_evaluator.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: role_arn is the same bare-"role_arn" gap as aws_bedrockagent_knowledge_base above. authorizer_type = "AWS_IAM" avoids the Optional-in-shape-but-Required-in-practice authorizer_configuration block CUSTOM_JWT would need (validate confirms authorizer_configuration is required only "when authorizer_type is set to CUSTOM_JWT")
resource "aws_bedrockagentcore_gateway" "app" {
  authorizer_type = "AWS_IAM"
  name            = aws_iam_role.ai-location.name
  role_arn        = aws_iam_role.ai-location.arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_gateway.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: model is Required-in-shape but the generic pass never visits it (validate: "Block model must have a configuration value"); bedrock_model_config is the minimal valid variant. harness_name must be 1-40 characters (validate: "string length must be between 1 and 40"), shorter than the generic tofu-<cohort>-cohort-<type> placeholder's 48
resource "aws_bedrockagentcore_harness" "app" {
  execution_role_arn = aws_iam_role.ai-location.arn
  harness_name       = "tofu_ai_location_harness"
  model {
    bedrock_model_config {
      model_id = "anthropic.claude-3-haiku-20240307-v1:0"
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_harness.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: event_expiry_duration must be 7-365 (validate: "value must be between 7 and 365"); the generic numeric placeholder is 0. name was also mis-wired to aws_iam_role.<cohort>.name by the generic pass's same-name parentRef search, the same shape aws_bedrockagentcore_code_interpreter's own override above explains - this type's own name pattern surfaced only at apply time (terraform validate does not evaluate cross-resource references), against the pinned floci image during this batch's verification
resource "aws_bedrockagentcore_memory" "app" {
  event_expiry_duration = 30
  name                  = "tofu_ai_location_memory"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_memory.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: credential_provider_vendor is a fixed enum (validate: "does not match any valid values", the pinned provider's real set differs from its own docs) - CustomOauth2 is the one variant whose oauth2_provider_config sub-block (custom_oauth2_provider_config) has no Required fields of its own. oauth2_provider_config itself is Required-in-shape but the generic pass never visits it
resource "aws_bedrockagentcore_oauth2_credential_provider" "app" {
  credential_provider_vendor = "CustomOauth2"
  name                       = aws_iam_role.ai-location.name
  oauth2_provider_config {
    custom_oauth2_provider_config {
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_oauth2_credential_provider.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: data_source_config, evaluator and rule are all Required-in-shape blocks the generic pass never visits (validate: "Block ... must have a configuration value"). online_evaluation_config_name must start with a letter and contain only alphanumerics/underscores, up to 48 characters (validate: "must start with a letter and contain only alphanumeric characters and underscores"), which the generic placeholder's hyphens and length both violate
resource "aws_bedrockagentcore_online_evaluation_config" "app" {
  enable_on_create              = false
  evaluation_execution_role_arn = aws_iam_role.ai-location.arn
  online_evaluation_config_name = "tofu_ai_location_online_eval"
  data_source_config {
    cloudwatch_logs {
      log_group_names = ["/tofu-ai-location-cohort/agent-traces"]
      service_names   = ["bedrock-agentcore"]
    }
  }
  evaluator {
    evaluator_id = "Builtin.Helpfulness"
  }
  rule {
    sampling_config {
      sampling_percentage = 10
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_online_evaluation_config.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name was mis-wired to aws_iam_role.<cohort>.name by the generic pass's same-name parentRef search, the same shape aws_bedrockagentcore_code_interpreter's own override above explains - this type's own name pattern (^[a-zA-Z][a-zA-Z0-9_]{0,47}$) surfaced only at apply time (terraform validate does not evaluate cross-resource references), against the pinned floci image during this batch's verification
resource "aws_bedrockagentcore_policy_engine" "app" {
  name = "tofu_ai_location_policy_engine"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_bedrockagentcore_policy_engine.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: resource_arn is validated as a well-formed ARN (validate: "cannot be parsed as an ARN"); the generic identity-argument placeholder ("tofu-<cohort>-cohort-...") is not one. policy is validated as well-formed JSON (validate: "is not valid JSON string format"), the same shape as aws_dynamodb_resource_policy's own override above
resource "aws_bedrockagentcore_resource_policy" "app" {
  resource_arn = "arn:aws:bedrock-agentcore:us-east-1:000000000000:runtime/tofu-ai-location-cohort"
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = "*"
      Action    = "bedrock-agentcore:InvokeAgentRuntime"
      Resource  = "arn:aws:bedrock-agentcore:us-east-1:000000000000:runtime/tofu-ai-location-cohort"
    }]
  })
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: language_code is a fixed enum (validate: "expected language_code to be one of [en es fr de it pt]"), not the generic placeholder string. input_data_config's data_format defaults to COMPREHEND_CSV, which then requires s3_uri (validate: "one of input_data_config.0.augmented_manifests,input_data_config.0.s3_uri must be specified"); the generic pass renders the block but leaves it empty because none of its own arguments are schema-Required
resource "aws_comprehend_document_classifier" "app" {
  data_access_role_arn = aws_iam_role.ai-location.arn
  language_code        = "en"
  name                 = aws_iam_role.ai-location.name
  input_data_config {
    s3_uri = "s3://tofu-ai-location-cohort-comprehend/train/"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_comprehend_document_classifier.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: role_arn is the same bare-"role_arn" gap as aws_bedrockagent_knowledge_base above
resource "aws_kendra_index" "app" {
  name     = aws_iam_role.ai-location.name
  role_arn = aws_iam_role.ai-location.arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_kendra_index.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: role_arn is the same bare-"role_arn" gap as aws_bedrockagent_knowledge_base above. data_privacy is Required-in-shape but the generic pass never visits it (validate: "Block data_privacy must have a configuration value"). idle_session_ttl_in_seconds must be 60-86400 (validate: "cannot be parsed as an ARN" aside, the schema documents the range directly); the generic numeric placeholder is 0
resource "aws_lexv2models_bot" "app" {
  idle_session_ttl_in_seconds = 60
  name                        = aws_iam_role.ai-location.name
  role_arn                    = aws_iam_role.ai-location.arn
  data_privacy {
    child_directed = false
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_lexv2models_bot.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_lexv2models_bot_locale" "app" {
  bot_id                           = "placeholder"
  bot_version                      = "placeholder"
  locale_id                        = "placeholder"
  n_lu_intent_confidence_threshold = 0
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_location_geofence_collection" "app" {
  collection_name = "tofu-ai-location-cohort-location-geofence-collection"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_location_geofence_collection.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: configuration.style
resource "aws_location_map" "app" {
  map_name = "tofu-ai-location-cohort-location-map"
  configuration {
    style = "VectorHereBerlin"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_location_map.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: data_source
resource "aws_location_place_index" "app" {
  index_name  = "tofu-ai-location-cohort-location-place-index"
  data_source = "Here"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_location_place_index.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: data_source
resource "aws_location_route_calculator" "app" {
  calculator_name = "tofu-ai-location-cohort-location-route-calculator"
  data_source     = "Here"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_location_route_calculator.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_location_tracker" "app" {
  tracker_name = "tofu-ai-location-cohort-location-tracker"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_location_tracker.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: consumer_arn is validated as a well-formed ARN (validate: "invalid ARN: arn: invalid prefix"); wired to this cohort's own aws_location_geofence_collection, the same consumer shape the provider's own example config uses (aws_location_tracker_association.example.consumer_arn = aws_location_geofence_collection.example.collection_arn)
resource "aws_location_tracker_association" "app" {
  consumer_arn = aws_location_geofence_collection.app.collection_arn
  tracker_name = aws_location_tracker.app.tracker_name
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: attachments_configuration is Required-in-shape but the generic pass never visits it (validate: "Block attachments_configuration must have a configuration value"); DISABLED is the minimal valid choice. identity_center_instance_arn is validated as a well-formed ARN (validate: "cannot be parsed as an ARN"); this cohort has no IAM Identity Center instance sibling to reference, so it stays a literal placeholder ARN, the same "no real sibling to reference" shape aws_rds_integration's own override above accepts for its target_arn
resource "aws_qbusiness_application" "app" {
  display_name                 = "tofu-ai-location-cohort-qbusiness-application"
  iam_service_role_arn         = aws_iam_role.ai-location.arn
  identity_center_instance_arn = "arn:aws:sso:::instance/ssoins00000000001"
  attachments_configuration {
    attachments_control_mode = "DISABLED"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_qbusiness_application.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_rekognition_collection" "app" {
  collection_id = "tofu-ai-location-cohort-rekognition-collection"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rekognition_collection.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: auto_update, feature
resource "aws_rekognition_project" "app" {
  name        = "tofu-ai-location-cohort-rekognition-project"
  auto_update = "ENABLED"
  feature     = "CONTENT_MODERATION"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rekognition_project.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: role_arn is the same bare-"role_arn" gap as aws_bedrockagent_knowledge_base above. input, output and settings are all Required-in-shape blocks the generic pass never visits (validate: "Block ... must have a configuration value"); each also requires at least one of its own Optional-in-shape sub-blocks in practice (validate: "At least one attribute out of [...] must be specified") - kinesis_video_stream, s3_destination and connected_home are the minimal valid choice within each
resource "aws_rekognition_stream_processor" "app" {
  name     = "tofu-ai-location-cohort-rekognition-stream-processor"
  role_arn = aws_iam_role.ai-location.arn
  input {
    kinesis_video_stream {
      arn = "arn:aws:kinesisvideo:us-east-1:000000000000:stream/tofu-ai-location-cohort-stream/1234567890000"
    }
  }
  output {
    s3_destination {
      bucket = "tofu-ai-location-cohort-rekognition"
    }
  }
  settings {
    connected_home {
      labels = ["PERSON"]
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rekognition_stream_processor.app"
  }
}
