# aps cohort: generator facts

Generated by `tools/estate-gen`; rewritten in full on every run. Hand
narrative belongs in README.md, which the generator never rewrites.

Cohort: `aps`. Generated by `tools/estate-gen` (issue #56) from hashicorp/aws 6.59.0's schema and `internal/live/identity/table.go`'s identity table.

Regenerate with:

```
go run ./tools/estate-gen -cohort aps -types aws_prometheus_alert_manager_definition,aws_prometheus_query_logging_configuration,aws_prometheus_scraper,aws_prometheus_scraper_logging_configuration,aws_prometheus_workspace -out live/e2e/estates/aps
```

## Provenance

| Resource | Kind | Overrides |
|---|---|---|
| `aws_prometheus_alert_manager_definition.app` | coverage | workspace_id left as a generic placeholder name instead of a reference (aws_prometheus_workspace is server-assigned, so parentRef never proposes it); wired to the workspace this cohort renders. definition is a required string the provider expects as YAML Alertmanager configuration; the generic placeholder is not valid YAML |
| `aws_prometheus_query_logging_configuration.app` | coverage | workspace_id left as a generic placeholder name instead of a reference, the same reason and correction as aws_prometheus_alert_manager_definition above. destination is a required block the schema marks optional-in-shape but the provider requires present, and its own nested filters block is required in turn (validate: "Block destination[0].filters must have a configuration value") |
| `aws_prometheus_scraper.app` | coverage | schema requires only scrape_configuration; the provider also requires the source and destination blocks (validate: "Missing required argument"), each with their own nested required arguments (an EKS-shaped source, an AMP workspace ARN destination) the generic pass has no schema signal for |
| `aws_prometheus_scraper_logging_configuration.app` | coverage | scraper_id left as a generic placeholder name instead of a reference (aws_prometheus_scraper is server-assigned, so parentRef never proposes it); wired to the scraper this cohort renders. logging_destination is a required block the schema marks optional-in-shape but the provider requires present |
| `aws_prometheus_workspace.app` | coverage | none |

## Requested types

- `aws_prometheus_alert_manager_definition`
- `aws_prometheus_query_logging_configuration`
- `aws_prometheus_scraper`
- `aws_prometheus_scraper_logging_configuration`
- `aws_prometheus_workspace`

## Files

| File | Contents |
|---|---|
| `versions.tf` | `terraform`/`provider "aws"` blocks, identical in shape to `live/e2e/estate/versions.tf`. |
| `locals.tf` | `estate_tag` — "aps-cohort", distinct from every other cohort's own tag. |
| `aps.tf` | Every requested (coverage) resource. |

## Gating

Nothing here runs against a live or emulated cloud. `go test ./internal/live/lint/... ./internal/live/identity/...` picks this
directory up through `internal/live/flocitest.FixtureDirs` (#48's union pin).

`terraform validate` passes against the real provider release (6.59.0) this fixture pins.
