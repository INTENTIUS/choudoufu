# media cohort: generator facts

Generated by `tools/estate-gen`; rewritten in full on every run. Hand
narrative belongs in README.md, which the generator never rewrites.

Cohort: `media`. Generated by `tools/estate-gen` (issue #56) from hashicorp/aws 6.58.0's schema and `internal/live/identity/table.go`'s identity table.

Regenerate with:

```
go run ./tools/estate-gen -cohort media -types aws_ivs_channel,aws_ivs_playback_key_pair,aws_ivs_recording_configuration,aws_ivschat_logging_configuration,aws_ivschat_room,aws_media_package_channel,aws_media_packagev2_channel_group,aws_medialive_multiplex,aws_medialive_multiplex_program -out live/e2e/estates/media
```

## Provenance

| Resource | Kind | Overrides |
|---|---|---|
| `aws_ivs_channel.app` | coverage | none |
| `aws_ivs_playback_key_pair.app` | coverage | none |
| `aws_ivs_recording_configuration.app` | coverage | none |
| `aws_ivschat_logging_configuration.app` | coverage | destination_configuration is Required in the provider's own docs (exactly one of cloudwatch_logs/firehose/s3, each with its own Required leaf) but Optional in the wire schema - enforced only by the provider's plan-time validation, so the generic required-only pass never visits it at all; wired to a literal S3 bucket name, the same destination_configuration.s3 shape aws_ivs_recording_configuration already renders (the generic pass fills that one because it is genuinely block-Required there) |
| `aws_ivschat_room.app` | coverage | none |
| `aws_media_package_channel.app` | coverage | none |
| `aws_media_packagev2_channel_group.app` | coverage | none |
| `aws_medialive_multiplex.app` | coverage | name is a plain client-chosen string (not this type's identity - aws_medialive_multiplex is server-assigned), but gen.go's parentRef mistakes it for a parent reference: this cohort's other client-named type, aws_media_packagev2_channel_group, also owns a single-component identity argument called "name", and parentRef's own same-name tiebreaker only guards two types that *both* claim "name" as their identity - not a server-assigned type's ordinary same-named argument. Left alone, the generic pass would point the multiplex at an unrelated MediaPackage channel group's name; availability_zones is a required list of strings, but the provider validates a 2-item minimum (validate: "attribute availability_zones requires 2 item minimum, but config has only 1 declared"); the generic pass emits one placeholder element; multiplex_settings is Required in the provider's own docs (transport_stream_bitrate and transport_stream_id both Required within it) but Optional in the wire schema - enforced only by the provider's plan-time validation, so the generic required-only pass never visits it at all |
| `aws_medialive_multiplex_program.app` | coverage | multiplex_id names the parent aws_medialive_multiplex by its server-assigned id, but this type's identity is a two-component composite (program_name, multiplex_id joined by "/"), and gen.go's identityArgName only links a single-component identity - so parentRef has nothing to match multiplex_id against and the generic pass leaves it a disconnected placeholder, the same "no automatic link to a server-assigned parent" gap cognitoUserPoolIDRef and iamPolicyArnRef below already work around for their own types |

## Requested types

- `aws_ivs_channel`
- `aws_ivs_playback_key_pair`
- `aws_ivs_recording_configuration`
- `aws_ivschat_logging_configuration`
- `aws_ivschat_room`
- `aws_media_package_channel`
- `aws_media_packagev2_channel_group`
- `aws_medialive_multiplex`
- `aws_medialive_multiplex_program`

## Files

| File | Contents |
|---|---|
| `versions.tf` | `terraform`/`provider "aws"` blocks, identical in shape to `live/e2e/estate/versions.tf`. |
| `locals.tf` | `estate_tag` — "media-cohort", distinct from every other cohort's own tag. |
| `media.tf` | Every requested (coverage) resource. |

## Gating

Nothing here runs against a live or emulated cloud. `go test ./internal/live/lint/... ./internal/live/identity/...` picks this
directory up through `internal/live/flocitest.FixtureDirs` (#48's union pin).

`terraform validate` passes against the real provider release (6.58.0) this fixture pins.
