# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "ip_count" is a required number the schema does not constrain, but the provider validates it against a fixed set (validate: "Attribute ip_count value must be one of: [3 21]"); the generic placeholder 0 matches neither
resource "aws_cloudfront_anycast_ip_list" "app" {
  ip_count = 3
  name     = aws_cloudfront_function.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudfront_anycast_ip_list.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: connection_function_config is Optional+Computed in the wire schema, but the provider requires it present in practice (validate: "Block connection_function_config must have a configuration value as the provider has marked it as required"), and its comment/runtime members are both required with no default the generic pass can infer
resource "aws_cloudfront_connection_function" "app" {
  connection_function_code = "placeholder"
  name                     = aws_cloudfront_function.app.name
  connection_function_config {
    comment = "tofu route53-cloudfront cohort connection function"
    runtime = "cloudfront-js-2.0"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudfront_connection_function.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_cloudfront_connection_group" "app" {
  name = aws_cloudfront_function.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudfront_connection_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: default_cache_behavior.viewer_protocol_policy and restrictions.geo_restriction.restriction_type are both required strings the schema does not constrain to an enum, but the provider validates each against a fixed set (validate: "expected ... to be one of [...]"); the generic placeholder string matches neither
resource "aws_cloudfront_distribution" "app" {
  enabled = false
  default_cache_behavior {
    allowed_methods        = ["placeholder"]
    cached_methods         = ["placeholder"]
    target_origin_id       = "placeholder"
    viewer_protocol_policy = "allow-all"
  }
  origin {
    domain_name = "placeholder"
    origin_id   = "placeholder"
  }
  restrictions {
    geo_restriction {
      restriction_type = "none"
    }
  }
  viewer_certificate {
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudfront_distribution.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_cloudfront_distribution_tenant" "app" {
  distribution_id = aws_cloudfront_monitoring_subscription.app.distribution_id
  name            = aws_cloudfront_function.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudfront_distribution_tenant.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "runtime" is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected runtime to be one of [...]"); the generic placeholder string matches neither
resource "aws_cloudfront_function" "app" {
  name    = "tofu-route53-cloudfront-cohort-cloudfront-function"
  code    = "placeholder"
  runtime = "cloudfront-js-2.0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudfront_function.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_cloudfront_key_value_store" "app" {
  name = "tofu-route53-cloudfront-cohort-cloudfront-key-value-store"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudfront_key_value_store.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: monitoring_subscription.realtime_metrics_subscription_config.realtime_metrics_subscription_status is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected ... to be one of [...]"); the generic placeholder string matches neither
resource "aws_cloudfront_monitoring_subscription" "app" {
  distribution_id = "tofu-route53-cloudfront-cohort-cloudfront-monitoring-subscription"
  monitoring_subscription {
    realtime_metrics_subscription_config {
      realtime_metrics_subscription_status = "Enabled"
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: viewer_certificate, tenant_config and default_cache_behavior are all Optional+Computed in the wire schema, but the provider requires all three present in practice (validate: "Block ... must have a configuration value as the provider has marked it as required") — a newer plugin-framework resource whose Required bit the generic required-only pass does not see the same way it sees aws_cloudfront_distribution's SDKv2 schema above; default_cache_behavior's own nested allowed_methods block is the same shape one level down (validate: "Block default_cache_behavior[0].allowed_methods must have a configuration value as the provider has marked it as required"), with items and cached_methods both required sets no default the generic pass can infer
resource "aws_cloudfront_multitenant_distribution" "app" {
  comment = "placeholder"
  enabled = false
  viewer_certificate {
  }
  tenant_config {
  }
  default_cache_behavior {
    target_origin_id       = "placeholder"
    viewer_protocol_policy = "allow-all"
    allowed_methods {
      items          = ["GET", "HEAD"]
      cached_methods = ["GET", "HEAD"]
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudfront_multitenant_distribution.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: origin_access_control_origin_type, signing_behavior and signing_protocol are all required strings the schema does not constrain to an enum, but the provider validates each against a fixed set (validate: "expected ... to be one of [...]"); the generic placeholder string matches none of them
resource "aws_cloudfront_origin_access_control" "app" {
  name                              = aws_cloudfront_function.app.name
  origin_access_control_origin_type = "s3"
  signing_behavior                  = "always"
  signing_protocol                  = "sigv4"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "sampling_rate" is a required number the schema constrains only to a type, but the provider validates it is in range 1-100 (validate: "expected sampling_rate to be in the range (1 - 100), got 0"); endpoint.stream_type is a required string the provider validates against a fixed one-member set (validate: "expected stream_type to be one of [Kinesis]"); and kinesis_stream_config's role_arn/stream_arn are both required strings the provider validates are well-formed ARNs (validate: "is an invalid ARN") the generic placeholder string is not
resource "aws_cloudfront_realtime_log_config" "app" {
  fields        = ["placeholder"]
  name          = aws_cloudfront_function.app.name
  sampling_rate = 50
  endpoint {
    stream_type = "Kinesis"
    kinesis_stream_config {
      role_arn   = "arn:aws:iam::000000000000:role/tofu-route53-cloudfront-cohort-realtime-log-role"
      stream_arn = "arn:aws:kinesis:us-east-1:000000000000:stream/tofu-route53-cloudfront-cohort-realtime-log-stream"
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: ca_certificates_bundle_source is Optional+Computed in the wire schema, but the provider requires it present in practice (validate: "Block ca_certificates_bundle_source must have a configuration value as the provider has marked it as required"), and its nested ca_certificates_bundle_s3_location block's bucket/key/region are all required with no default the generic pass can infer
resource "aws_cloudfront_trust_store" "app" {
  name = aws_cloudfront_function.app.name
  ca_certificates_bundle_source {
    ca_certificates_bundle_s3_location {
      bucket = "tofu-route53-cloudfront-cohort-trust-store-bundle"
      key    = "ca-bundle.pem"
      region = "us-east-1"
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudfront_trust_store.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: vpc_origin_endpoint_config is Optional+Computed in the wire schema, but the provider requires it present in practice (validate: "Block vpc_origin_endpoint_config must have a configuration value as the provider has marked it as required"), and its arn/http_port/https_port/name/origin_protocol_policy members are all required with no default the generic pass can infer; its own nested origin_ssl_protocols block is the same shape one level down (validate: "Block vpc_origin_endpoint_config[0].origin_ssl_protocols must have a configuration value as the provider has marked it as required"), with items and quantity both required with no default either
resource "aws_cloudfront_vpc_origin" "app" {
  vpc_origin_endpoint_config {
    arn                    = "arn:aws:ec2:us-east-1:000000000000:vpc-endpoint-service/vpce-svc-tofu-route53-cloudfront-cohort"
    http_port              = 80
    https_port             = 443
    name                   = "tofu-route53-cloudfront-cohort-vpc-origin"
    origin_protocol_policy = "https-only"
    origin_ssl_protocols {
      items    = ["TLSv1.2"]
      quantity = 1
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudfront_vpc_origin.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "type" is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected type to be one of [...]"); the generic placeholder string matches neither
resource "aws_route53_health_check" "app" {
  type = "HTTP"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53_health_check.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53_hosted_zone_dnssec" "app" {
  hosted_zone_id = "tofu-route53-cloudfront-cohort-route53-hosted-zone-dnssec"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "key_management_service_arn" is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN"); the generic placeholder string is not
resource "aws_route53_key_signing_key" "app" {
  hosted_zone_id             = aws_route53_hosted_zone_dnssec.app.hosted_zone_id
  key_management_service_arn = "arn:aws:kms:us-east-1:000000000000:key/tofu-route53-cloudfront-cohort-ksk"
  name                       = aws_cloudfront_function.app.name
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "direction" is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected direction to be one of [...]"); and ip_address is a set-nesting block with MinItems 2 - the generic pass emits two blocks, but both carry the identical placeholder subnet_id, and a set collapses identical elements to one, so the provider sees only 1 (validate: "Attribute ip_address requires 2 item minimum, but config has only 1 declared")
resource "aws_route53_resolver_endpoint" "app" {
  direction          = "INBOUND"
  security_group_ids = ["placeholder"]
  ip_address {
    subnet_id = "subnet-tofu01cohortplaceholder"
  }
  ip_address {
    subnet_id = "subnet-tofu02cohortplaceholder"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53_resolver_endpoint.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53_resolver_firewall_domain_list" "app" {
  name = aws_cloudfront_function.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53_resolver_firewall_domain_list.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "action" is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected action to be one of [ALLOW BLOCK ALERT]"); the generic placeholder string matches none of them, and only ALLOW/ALERT need no further conditionally-required block_response. firewall_domain_list_id is Optional in the schema (it names the standard-rule shape; dns_threat_protection names the advanced-rule shape instead), but the provider requires exactly one of the two set - not caught by "terraform validate" itself, only surfaced at apply (validate: "one of firewall_domain_list_id or dns_threat_protection must be specified") - and the fixture picks the standard-rule shape, the one internal/live/identity/table.go's own parent-derived aws_route53_resolver_firewall_rule entry composes an identity for.
resource "aws_route53_resolver_firewall_rule" "app" {
  action                  = "ALLOW"
  firewall_rule_group_id  = "placeholder"
  name                    = aws_cloudfront_function.app.name
  priority                = 0
  firewall_domain_list_id = "placeholder"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53_resolver_firewall_rule_group" "app" {
  name = aws_cloudfront_function.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53_resolver_firewall_rule_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53_resolver_firewall_rule_group_association" "app" {
  firewall_rule_group_id = "placeholder"
  name                   = aws_cloudfront_function.app.name
  priority               = 0
  vpc_id                 = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53_resolver_firewall_rule_group_association.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "destination_arn" is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN"); the generic placeholder string is not
resource "aws_route53_resolver_query_log_config" "app" {
  destination_arn = "arn:aws:logs:us-east-1:000000000000:log-group:/tofu-route53-cloudfront-cohort-query-logs"
  name            = aws_cloudfront_function.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53_resolver_query_log_config.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "rule_type" is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected rule_type to be one of [...]"); the generic placeholder string matches neither
resource "aws_route53_resolver_rule" "app" {
  domain_name = "tofu-route53-cloudfront-cohort-route53-resolver-rule"
  rule_type   = "FORWARD"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53_resolver_rule.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53_resolver_rule_association" "app" {
  resolver_rule_id = "placeholder"
  vpc_id           = "placeholder"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53_zone_association" "app" {
  vpc_id  = "placeholder"
  zone_id = "placeholder"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53profiles_association" "app" {
  name        = aws_cloudfront_function.app.name
  profile_id  = "placeholder"
  resource_id = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53profiles_association.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53profiles_profile" "app" {
  name = aws_cloudfront_function.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53profiles_profile.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53recoverycontrolconfig_cluster" "app" {
  name = aws_cloudfront_function.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53recoverycontrolconfig_cluster.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53recoverycontrolconfig_control_panel" "app" {
  cluster_arn = "placeholder"
  name        = aws_cloudfront_function.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53recoverycontrolconfig_control_panel.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: asserted_controls and gating_controls are both Optional in the schema, but the provider requires exactly one of them set (validate: "one of asserted_controls,gating_controls must be specified"); a gating rule also needs target_controls, the list of controls it gates, which the schema likewise leaves Optional; rule_config.type is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected type to be one of [ATLEAST AND OR]")
resource "aws_route53recoverycontrolconfig_safety_rule" "app" {
  control_panel_arn = "placeholder"
  name              = aws_cloudfront_function.app.name
  wait_period_ms    = 0
  rule_config {
    inverted  = false
    threshold = 0
    type      = "ATLEAST"
  }
  gating_controls = ["arn:aws:route53-recovery-control::000000000000:controlpanel/tofu-route53-cloudfront-cohort-panel/routingcontrol/tofu-route53-cloudfront-cohort-gating"]
  target_controls = ["arn:aws:route53-recovery-control::000000000000:controlpanel/tofu-route53-cloudfront-cohort-panel/routingcontrol/tofu-route53-cloudfront-cohort-target"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_route53recoverycontrolconfig_safety_rule.app"
  }
}
