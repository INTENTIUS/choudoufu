# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: type is a required enum (validate: "expected type to be one of [...]"), and the generic string placeholder is not a member
resource "aws_athena_data_catalog" "app" {
  name        = "tofu-data-cohort-athena-data-catalog"
  description = "placeholder"
  parameters  = {}
  type        = "GLUE"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_athena_data_catalog.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: configuration.enforce_workgroup_configuration, configuration.publish_cloudwatch_metrics_enabled, configuration.result_configuration.output_location, configuration.result_configuration.encryption_configuration.encryption_option, configuration.result_configuration.encryption_configuration.kms_key_arn
resource "aws_athena_workgroup" "app" {
  name = "tofu-data-cohort-athena-workgroup"
  configuration {
    enforce_workgroup_configuration    = true
    publish_cloudwatch_metrics_enabled = true
    result_configuration {
      output_location = aws_s3_bucket.data.bucket
      encryption_configuration {
        encryption_option = "SSE_KMS"
        kms_key_arn       = aws_kms_key.data.arn
      }
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_athena_workgroup.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: name coincidentally collides with aws_athena_data_catalog's own "name" identity argument in this cohort - see this file's data-plane batch header comment - so the generic pass's parentRef wiring points it at that unrelated resource instead of giving it its own placeholder
resource "aws_glue_catalog_database" "app" {
  name = "tofu_data_cohort_glue_catalog_database"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_glue_catalog_database.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: name coincidentally collides with aws_athena_data_catalog's own "name" identity argument in this cohort - see this file's data-plane batch header comment; database_name is a plain required string argument the schema does not mark as any other type's identity, so parentRef never connects it to the sibling aws_glue_catalog_database (whose own identity is the account-derived catalog_id:name composite, not a single "database_name"-named argument) and the generic pass gives it an unrelated placeholder instead
resource "aws_glue_catalog_table" "app" {
  database_name = aws_glue_catalog_database.app.name
  name          = "tofu_data_cohort_glue_catalog_table"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: csv_classifier.allow_single_column, csv_classifier.contains_header, csv_classifier.delimiter, csv_classifier.disable_value_trimming, csv_classifier.header, csv_classifier.quote_symbol
resource "aws_glue_classifier" "app" {
  name = "tofu-data-cohort-glue-classifier"
  csv_classifier {
    allow_single_column    = false
    contains_header        = "PRESENT"
    delimiter              = ","
    disable_value_trimming = false
    header                 = ["example1", "example2"]
    quote_symbol           = "'"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: name coincidentally collides with aws_athena_data_catalog's own "name" identity argument in this cohort - see this file's data-plane batch header comment
resource "aws_glue_connection" "app" {
  name = "tofu-data-cohort-glue-connection"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_glue_connection.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires only database_name, name and role; the provider also requires exactly one of catalog_target/delta_target/dynamodb_target/hudi_target/iceberg_target/jdbc_target/mongodb_target/s3_target (validate: "one of ... must be specified"), and s3_target requires a path
resource "aws_glue_crawler" "app" {
  name          = "tofu-data-cohort-glue-crawler"
  database_name = "tofu-data-cohort-glue-crawler"
  role          = aws_iam_role.data.arn
  s3_target {
    path = "s3://tofu-data-cohort-glue-crawler/"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_glue_crawler.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: data_catalog_encryption_settings.encryption_at_rest.catalog_encryption_mode is a required enum (validate: "expected catalog_encryption_mode to be one of [...]"), and the generic string placeholder is not a member
resource "aws_glue_data_catalog_encryption_settings" "app" {
  data_catalog_encryption_settings {
    connection_password_encryption {
      return_connection_password_encrypted = false
    }
    encryption_at_rest {
      catalog_encryption_mode = "DISABLED"
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: role_arn does not match isRoleArg's "role"/"*_role_arn" alias (it is exactly "role_arn", one underscore short), so the generic pass leaves it as an unparseable placeholder string (validate: "is an invalid ARN"); command.script_location has no schema default and an empty string is not a valid S3 or local path
resource "aws_glue_job" "app" {
  name     = "tofu-data-cohort-glue-job"
  role_arn = aws_iam_role.data.arn
  command {
    script_location = "s3://tofu-data-cohort-glue-job/script.py"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_glue_job.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: name coincidentally collides with aws_athena_data_catalog's own "name" identity argument in this cohort - see this file's data-plane batch header comment; role_arn does not match isRoleArg's alias, the same gap as aws_glue_job above; parameters.transform_type is a required enum (validate: "expected transform_type to be one of [...]") the generic placeholder does not satisfy
resource "aws_glue_ml_transform" "app" {
  name     = "tofu-data-cohort-glue-ml-transform"
  role_arn = aws_iam_role.data.arn
  input_record_tables {
    database_name = aws_glue_catalog_database.app.name
    table_name    = aws_glue_catalog_table.app.name
  }
  parameters {
    transform_type = "FIND_MATCHES"
    find_matches_parameters {
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_glue_ml_transform.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_glue_registry" "app" {
  registry_name = "tofu-data-cohort-glue-registry"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_glue_registry.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: type, actions.job_name
resource "aws_glue_trigger" "app" {
  name = "tofu-data-cohort-glue-trigger"
  type = "ON_DEMAND"
  actions {
    job_name = aws_glue_job.app.name
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_glue_trigger.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: name coincidentally collides with aws_athena_data_catalog's own "name" identity argument in this cohort - see this file's data-plane batch header comment; destination is a required enum naming which optional *_configuration block the provider actually reads (validate: "expected destination to be one of [...]"), and the matching extended_s3_configuration block is itself optional in the schema while the provider requires it in practice once destination = "extended_s3"
resource "aws_kinesis_firehose_delivery_stream" "app" {
  name        = "tofu-data-cohort-firehose"
  destination = "extended_s3"
  extended_s3_configuration {
    bucket_arn = "arn:aws:s3:::tofu-data-cohort-firehose"
    role_arn   = aws_iam_role.data.arn
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_kinesis_firehose_delivery_stream.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: shard_count has no schema default and is left unset by the generic pass, but the provider's own CustomizeDiff defaults stream_mode_details.stream_mode to "PROVISIONED" and then requires shard_count to be at least 1 (found only by running the generic pass's output through terraform apply, not validate - the check is plan-time, not schema-level)
resource "aws_kinesis_stream" "app" {
  name        = "tofu-data-cohort-kinesis-stream"
  shard_count = 1
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_kinesis_stream.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: name coincidentally collides with aws_athena_data_catalog's own "name" identity argument in this cohort - see this file's data-plane batch header comment; stream_arn is a required argument the schema alone cannot wire to the sibling aws_kinesis_stream (its identity is "name", not "stream_arn", so parentRef never connects the two)
resource "aws_kinesis_stream_consumer" "app" {
  name       = "tofu-data-cohort-kinesis-stream-consumer"
  stream_arn = aws_kinesis_stream.app.arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_kinesis_stream_consumer.app"
  }
}
