# Supporting, not coverage - added only so aws_iam_role.data's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires "assume_role_policy" as a plain string, but the provider validates it is well-formed JSON (validate: "\"assume_role_policy\" contains an invalid JSON"); the generic string placeholder is not JSON
resource "aws_iam_role" "data" {
  name = "tofu-data-cohort-iam-role"
  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = { Service = "data.amazonaws.com" }
      Action    = "sts:AssumeRole"
    }]
  })
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iam_role.data"
  }
}

# Supporting, not coverage - added only so aws_kms_key.data's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: deletion_window_in_days, description, enable_key_rotation
resource "aws_kms_key" "data" {
  deletion_window_in_days = 20
  description             = "An example symmetric encryption KMS key"
  enable_key_rotation     = true
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_kms_key.data"
  }
}

# Supporting, not coverage - added only so aws_s3_bucket.data's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_s3_bucket" "data" {
  bucket = "tofu-data-cohort-s3-bucket"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_s3_bucket.data"
  }
}
