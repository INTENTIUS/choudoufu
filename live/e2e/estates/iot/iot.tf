# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "authorizer_function_arn" is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN"); this argument names a Lambda function, not a role, so isRoleArg's cross-reference does not apply (this cohort requests no aws_lambda_function) - a literal, well-formed placeholder ARN is enough to satisfy the format check alone. "signing_disabled" is Optional and defaults to false (signing enabled), and the provider then requires "token_key_name" and "token_signing_public_keys" (apply: "\"token_key_name\" is required when signing is enabled", not caught by "terraform validate" - a real API-facing requirement, not a floci gap) - overridden to true so this fixture needs neither.
resource "aws_iot_authorizer" "app" {
  name                    = "tofu-iot-cohort-authorizer"
  authorizer_function_arn = "arn:aws:lambda:us-east-1:000000000000:function:tofu-iot-authorizer"
  signing_disabled        = true
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iot_authorizer.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: properties.description
resource "aws_iot_billing_group" "app" {
  name = "tofu-iot-cohort-billing-group"
  properties {
    description = "This is my billing group"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iot_billing_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_iot_domain_configuration" "app" {
  name = "tofu-iot-cohort-domain-configuration"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iot_domain_configuration.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "policy" is a required string the schema does not constrain, but the provider validates it is well-formed JSON (validate: "\"policy\" contains an invalid JSON"); the generic placeholder string is not
resource "aws_iot_policy" "app" {
  name = "tofu-iot-cohort-policy"
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect   = "Allow"
      Action   = ["iot:Publish"]
      Resource = "*"
    }]
  })
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iot_policy.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" is this type's own identity argument (internal/live/identity/table.go), but the generic pass's tofu-<cohort>-<type> placeholder ("tofu-iot-cohort-provisioning-template", 37 characters) exceeds the provider's documented 36-character limit (validate: "expected length of name to be in the range (1 - 36)"); "template_body" is a required string the schema does not constrain, but the provider validates it is well-formed JSON (validate: "\"template_body\" contains an invalid JSON") - the generic placeholder string is neither short enough nor JSON.
resource "aws_iot_provisioning_template" "app" {
  name                  = "tofu-iot-provtmpl"
  provisioning_role_arn = aws_iam_role.iot.arn
  template_body = jsonencode({
    Parameters = {
      "AWS::IoT::Certificate::Id" = { Type = "String" }
    }
    Resources = {
      certificate = {
        Type = "AWS::IoT::Certificate"
        Properties = {
          CertificateId = { Ref = "AWS::IoT::Certificate::Id" }
          Status        = "ACTIVE"
        }
      }
      thing = {
        Type = "AWS::IoT::Thing"
        Properties = {
          ThingName = { Ref = "AWS::IoT::Certificate::Id" }
        }
      }
    }
  })
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iot_provisioning_template.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "role_arn" is a required string the schema does not constrain and (unlike aws_codepipeline's own "role_arn" above) the provider ships no ARN-format validator on this particular attribute, so the generic placeholder passes "terraform validate" unchanged - but it is still the same bare isRoleArg gap named on aws_codepipeline and aws_codebuild_project above ("role_arn" alone does not end "_role_arn"), and a non-ARN placeholder would fail at apply against a real IoT role_alias create, so this override wires the real cross-resource reference anyway rather than leaving a validate-clean but apply-broken row.
resource "aws_iot_role_alias" "app" {
  alias    = "tofu-iot-cohort-role-alias"
  role_arn = aws_iam_role.iot.arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iot_role_alias.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_iot_thing" "app" {
  name = "tofu-iot-cohort-thing"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_iot_thing_group" "app" {
  name = aws_iot_thing.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iot_thing_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_iot_thing_type" "app" {
  name = aws_iot_thing.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iot_thing_type.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "name" is this type's own identity argument (internal/live/identity/table.go), but the generic pass's tofu-<cohort>-<type> placeholder uses hyphens, and the provider validates topic rule names against ^[0-9A-Za-z_]+$ (validate: "Name must match the pattern ^[0-9A-Za-z_]+$") - hyphens are not in that set, unlike every other client-named IoT type in this cohort.
resource "aws_iot_topic_rule" "app" {
  name        = "tofu_iot_topic_rule"
  enabled     = false
  sql         = "placeholder"
  sql_version = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_iot_topic_rule.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: "vpc_configuration.role_arn" is a required string nested inside the vpc_configuration block; the schema does not constrain it, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN") - isRoleArg's generic cross-reference only scans each type's top-level required arguments (planCohort's requiredArgNames pass), not nested block arguments, so this nested role_arn is never auto-wired the way aws_iot_provisioning_template's top-level provisioning_role_arn is.
resource "aws_iot_topic_rule_destination" "app" {
  vpc_configuration {
    role_arn   = aws_iam_role.iot.arn
    subnet_ids = ["placeholder"]
    vpc_id     = "placeholder"
  }
}
