# Supporting, not coverage - added only so aws_subnet.dynamodb-elasticache's referring resources have a value to point at: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_subnet" "dynamodb-elasticache" {
  vpc_id = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_subnet.dynamodb-elasticache"
  }
}
