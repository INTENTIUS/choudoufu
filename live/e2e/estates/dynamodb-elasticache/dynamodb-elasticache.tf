# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_dynamodb_global_table" "app" {
  name = "tofu-dynamodb-elasticache-cohort-dynamodb-global-table"
  replica {
    region_name = "placeholder"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires "resource_arn" as a plain string, but the provider validates it is a well-formed ARN (validate: "The provided value cannot be parsed as an ARN"); the generic identity-argument placeholder ("tofu-<cohort>-cohort-...") is not one. schema also requires "policy" as a plain string, but the provider validates it is well-formed JSON (validate: "is not valid JSON string format"); the generic string placeholder is not JSON
resource "aws_dynamodb_resource_policy" "app" {
  resource_arn = "arn:aws:dynamodb:us-east-1:000000000000:table/tofu-dynamodb-elasticache-cohort-app"
  policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = "*"
      Action    = "dynamodb:DescribeTable"
      Resource  = "arn:aws:dynamodb:us-east-1:000000000000:table/tofu-dynamodb-elasticache-cohort-app"
    }]
  })
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires only cluster_id; the provider also requires exactly one of engine/replication_group_id (validate: "one of engine,replication_group_id must be specified") and, once engine is chosen, node_type, num_cache_nodes and parameter_group_name become required in practice too (validate: "Missing required argument"). engine is set to "memcached" rather than the more familiar "redis": AWS's CreateCacheCluster API (which this resource calls directly, unlike aws_elasticache_replication_group) only accepts the redis/valkey engines when joining an existing replication group, confirmed by floci's own emulation of that same real-AWS rule (apply: "InvalidParameterValue: Engine must be 'memcached'. For Redis/Valkey use CreateReplicationGroup.") — not a floci gap, the standalone-cluster shape genuinely requires memcached. cluster_id is also length-limited to 50 characters (validate: "expected length ... to be in the range (1 - 50)"), and this cohort's own name ("dynamodb-elasticache") makes the generic tofu-<cohort>-cohort-<type> placeholder 54 characters - shortened here to a value that still names the cohort and the type
resource "aws_elasticache_cluster" "app" {
  cluster_id           = "tofu-ddb-ec-cluster"
  engine               = "memcached"
  node_type            = "cache.t3.micro"
  num_cache_nodes      = 1
  parameter_group_name = "default.memcached1.6"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_elasticache_cluster.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: family
resource "aws_elasticache_parameter_group" "app" {
  name   = "tofu-dynamodb-elasticache-cohort-elasticache-parameter-group"
  family = "redis2.8"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_elasticache_parameter_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires only replication_group_id; the provider also requires node_type in practice once no global_replication_group_id is set (validate: "\"node_type\" is required unless \"global_replication_group_id\" is set"), and engine defaults to redis but is set explicitly here for clarity. replication_group_id is also length-limited to 40 characters (validate: "expected length ... to be in the range (1 - 40)"), and this cohort's own name ("dynamodb-elasticache") makes the generic tofu-<cohort>-cohort-<type> placeholder 58 characters - shortened here to a value that still names the cohort and the type
resource "aws_elasticache_replication_group" "app" {
  replication_group_id = "tofu-ddb-ec-replgrp"
  description          = "placeholder"
  engine               = "redis"
  node_type            = "cache.t3.micro"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_elasticache_replication_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: engine
resource "aws_elasticache_serverless_cache" "app" {
  name   = "tofu-dynamodb-elasticache-cohort-elasticache-serverless-cache"
  engine = "memcached"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_elasticache_serverless_cache.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_elasticache_subnet_group" "app" {
  name       = "tofu-dynamodb-elasticache-cohort-elasticache-subnet-group"
  subnet_ids = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_elasticache_subnet_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: engine is a required argument the schema types as an unconstrained string, but the provider validates it against a closed enum (validate: "expected engine to be one of [\"redis\" \"valkey\"]"); the generic placeholder string is neither
resource "aws_elasticache_user" "app" {
  user_id       = "tofu-dynamodb-elasticache-cohort-elasticache-user"
  access_string = "placeholder"
  engine        = "redis"
  user_name     = "tofu-dynamodb-elasticache-cohort-elasticache-user"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_elasticache_user.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: engine
resource "aws_elasticache_user_group" "app" {
  user_group_id = "tofu-dynamodb-elasticache-cohort-elasticache-user-group"
  engine        = "redis"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_elasticache_user_group.app"
  }
}
