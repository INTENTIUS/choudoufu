# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description
resource "aws_appconfig_application" "app" {
  name        = aws_glue_workflow.app.name
  description = "Example AppConfig Application"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_appconfig_application.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: location_uri
resource "aws_appconfig_configuration_profile" "app" {
  application_id = aws_appconfig_application.app.id
  location_uri   = "hosted"
  name           = aws_glue_workflow.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_appconfig_configuration_profile.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: deployment_strategy_id is validated against a pattern (validate: "expected value of deployment_strategy_id to match regular expression \"(^[0-9a-z]{4,7}$|^AppConfig\\.[0-9A-Za-z]{9,40}$)\""); no strategy type is admitted to reference, so the predefined AppConfig.AllAtOnce strategy the service ships is the literal
resource "aws_appconfig_deployment" "app" {
  application_id           = aws_appconfig_application.app.id
  configuration_profile_id = aws_appconfig_configuration_profile.app.configuration_profile_id
  configuration_version    = "placeholder"
  deployment_strategy_id   = "AppConfig.AllAtOnce"
  environment_id           = aws_appconfig_environment.app.environment_id
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_appconfig_deployment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_appconfig_environment" "app" {
  application_id = aws_appconfig_application.app.id
  name           = aws_glue_workflow.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_appconfig_environment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: content, content_type
resource "aws_appconfig_hosted_configuration_version" "app" {
  application_id           = aws_appconfig_application.app.id
  configuration_profile_id = aws_appconfig_configuration_profile.app.configuration_profile_id
  content                  = "{\"foo\":\"bar\",\"fruit\":[\"apple\",\"pear\",\"orange\"],\"isThingEnabled\":true}"
  content_type             = "application/json"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: execution_role is Required and validated as a well-formed ARN (validate: "value must be a valid ARN"); the generic placeholder string is not one; recovery_approach is Required and validated against a closed enum (validate: "does not match any valid values", valid: activeActive, activePassive); the generic placeholder string satisfies neither; regions is validated against a two-element floor the wire schema does not express (validate at 6.59.0: "Attribute regions list must contain at least 2 elements, got: 1"); the generic single-element placeholder list is one short
resource "aws_arcregionswitch_plan" "app" {
  execution_role    = "arn:aws:iam::000000000000:role/placeholder"
  name              = aws_glue_workflow.app.name
  recovery_approach = "activeActive"
  regions           = ["us-east-1", "us-west-2"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_arcregionswitch_plan.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_athena_named_query" "app" {
  database = "placeholder"
  name     = aws_glue_workflow.app.name
  query    = "placeholder"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description
resource "aws_billing_view" "app" {
  name        = aws_glue_workflow.app.name
  description = "example description"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_billing_view.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: analytics_engine, description, query_log_status, data_encryption_metadata.allow_clear_text, data_encryption_metadata.allow_duplicates, data_encryption_metadata.allow_joins_on_columns_with_different_names, data_encryption_metadata.preserve_nulls
resource "aws_cleanrooms_collaboration" "app" {
  creator_display_name     = "tofu-remainder-cohort-cleanrooms-collaboration"
  creator_member_abilities = ["placeholder"]
  description              = "I made this collaboration with terraform!"
  name                     = aws_glue_workflow.app.name
  query_log_status         = "DISABLED"
  analytics_engine         = "SPARK"
  data_encryption_metadata {
    allow_clear_text                            = true
    allow_duplicates                            = true
    allow_joins_on_columns_with_different_names = true
    preserve_nulls                              = false
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cleanrooms_collaboration.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: image_id is Required and validated against a closed enum of real Cloud9 AMI aliases (validate: "expected image_id to be one of [...], got placeholder"); the generic placeholder string satisfies none of them
resource "aws_cloud9_environment_ec2" "app" {
  image_id      = "amazonlinux-2023-x86_64"
  instance_type = "placeholder"
  name          = aws_glue_workflow.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloud9_environment_ec2.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: the parameters_in_cache_key_and_forwarded_to_origin block's cookies_config.cookie_behavior and query_strings_config.query_string_behavior are each Required and validated against a closed enum (validate: "expected ... to be one of [...], got placeholder"); the generic placeholder string satisfies neither
resource "aws_cloudfront_cache_policy" "app" {
  name = aws_glue_workflow.app.name
  parameters_in_cache_key_and_forwarded_to_origin {
    cookies_config {
      cookie_behavior = "none"
    }
    headers_config {
    }
    query_strings_config {
      query_string_behavior = "none"
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_cloudtrail" "app" {
  name           = aws_glue_workflow.app.name
  s3_bucket_name = "tofu-remainder-cohort-cloudtrail"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudtrail.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_codegurureviewer_repository_association" "app" {
  repository {
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_codegurureviewer_repository_association.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: domain_execution_role is Required and validated as a well-formed ARN (validate: "cannot be parsed as an ARN"); the generic placeholder string is not one
resource "aws_datazone_domain" "app" {
  domain_execution_role = "arn:aws:iam::000000000000:role/placeholder"
  name                  = aws_glue_workflow.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datazone_domain.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_datazone_environment_blueprint_configuration" "app" {
  domain_id                = "placeholder"
  enabled_regions          = ["placeholder"]
  environment_blueprint_id = "placeholder"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: domain_identifier is validated against the DataZone domain-ID pattern (validate: "Attribute domain_identifier ^dzd[-_][a-zA-Z0-9_-]{1,36}$, got: placeholder"); wired to this cohort's own aws_datazone_domain - this type is server-assigned in the identity table, so the argument is not identity-bound and the reference is plain apply-correctness; model is a required nested block the generic required-only pass does not emit (validate: "Block model must have a configuration value as the provider has marked it as required"); its one member is a Smithy model document, supplied here as the minimal structure the service accepts
resource "aws_datazone_form_type" "app" {
  domain_identifier         = aws_datazone_domain.app.id
  name                      = aws_glue_workflow.app.name
  owning_project_identifier = "placeholder"
  model {
    smithy = "structure exampleForm { }"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_detective_graph" "app" {
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_detective_graph.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: deletion_protection_enabled
resource "aws_dsql_cluster" "app" {
  deletion_protection_enabled = true
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_dsql_cluster.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: bandwidth is Required and validated against a closed enum of real Direct Connect port speeds (validate: "expected bandwidth to be one of [...], got placeholder"); the generic placeholder string satisfies none of them
resource "aws_dx_connection" "app" {
  bandwidth = "1Gbps"
  location  = "placeholder"
  name      = aws_glue_workflow.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_dx_connection.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_ec2_instance_connect_endpoint" "app" {
  subnet_id = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_ec2_instance_connect_endpoint.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_efs_mount_target" "app" {
  file_system_id = "placeholder"
  subnet_id      = "placeholder"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description
resource "aws_evidently_project" "app" {
  name        = aws_glue_workflow.app.name
  description = "Example Description"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_evidently_project.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema shows build_id and script_id as both Optional, but the provider requires exactly one (validate: "one of build_id,script_id must be specified"); this cohort has no admitted GameLift build/script sibling to reference, so build_id is set to a placeholder build id; ec2_instance_type is Required and validated against a closed enum of real EC2 instance types (validate: "expected ec2_instance_type to be one of [...], got placeholder"); the generic placeholder string satisfies none of them
resource "aws_gamelift_fleet" "app" {
  ec2_instance_type = "c5.large"
  name              = aws_glue_workflow.app.name
  build_id          = "build-0123456789abcdef0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_gamelift_fleet.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_glue_workflow" "app" {
  name = "tofu-remainder-cohort-glue-workflow"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_glue_workflow.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema shows container_recipe_arn and image_recipe_arn as both Optional, but the provider requires exactly one (validate: "one of container_recipe_arn,image_recipe_arn must be specified"); this cohort has no admitted Image Builder recipe sibling to reference, so image_recipe_arn is set to a placeholder recipe ARN; infrastructure_configuration_arn is Required and validated as a real Image Builder infrastructure-configuration ARN shape (validate: "valid infrastructure configuration ARN must be provided"); the generic placeholder string does not match it
resource "aws_imagebuilder_image_pipeline" "app" {
  infrastructure_configuration_arn = "arn:aws:imagebuilder:us-east-1:000000000000:infrastructure-configuration/tofu-remainder-cohort-infra-config"
  name                             = aws_glue_workflow.app.name
  image_recipe_arn                 = "arn:aws:imagebuilder:us-east-1:000000000000:image-recipe/tofu-remainder-cohort-recipe/1.0.0"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_imagebuilder_image_pipeline.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: finding_publishing_frequency, status
resource "aws_macie2_account" "app" {
  finding_publishing_frequency = "FIFTEEN_MINUTES"
  status                       = "ENABLED"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: exportable, key_attributes.key_algorithm, key_attributes.key_class, key_attributes.key_usage, key_attributes.key_modes_of_use.decrypt, key_attributes.key_modes_of_use.encrypt, key_attributes.key_modes_of_use.unwrap, key_attributes.key_modes_of_use.wrap
resource "aws_paymentcryptography_key" "app" {
  exportable = true
  key_attributes {
    key_algorithm = "TDES_3KEY"
    key_class     = "SYMMETRIC_KEY"
    key_usage     = "TR31_P0_PIN_ENCRYPTION_KEY"
    key_modes_of_use {
      decrypt = true
      encrypt = true
      unwrap  = true
      wrap    = true
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_paymentcryptography_key.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: alias_name is validated against the service's alias/ prefix rule (validate: "An alias must begin with alias/ followed by a name"); the generic tofu-<cohort>-cohort-<type> literal lacks the prefix
resource "aws_paymentcryptography_key_alias" "app" {
  alias_name = "alias/tofu-remainder-cohort-key-alias"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_route53_cidr_collection" "app" {
  name = aws_glue_workflow.app.name
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_securityhub_account" "app" {
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_sesv2_contact_list" "app" {
  contact_list_name = "tofu-remainder-cohort-sesv2-contact-list"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_sesv2_contact_list.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_sesv2_email_identity" "app" {
  email_identity = "tofu-remainder-cohort-sesv2-email-identity"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_sesv2_email_identity.app"
  }
}
