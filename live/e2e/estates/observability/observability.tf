# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: rule is a required argument typed as a nested block with MinItems 0 in the wire schema, so the generic required-only pass never renders one at all - not caught by "terraform validate" (which only checks the arguments a block actually has), only surfaced applying against floci (apply: "missing required field, PutAlarmMuteRuleInput.Rule"). Its own nested schedule block is likewise required in practice.
resource "aws_cloudwatch_alarm_mute_rule" "app" {
  name = "tofu-observability-cohort-cloudwatch-alarm-mute-rule"
  rule {
    schedule {
      duration   = "PT4H"
      expression = "cron(0 2 * * ? *)"
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudwatch_alarm_mute_rule.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: rule_definition is a required string the schema does not constrain, but the provider validates it is well-formed JSON (validate: "A string value was provided that is not valid JSON string format"); the generic placeholder string is not
resource "aws_cloudwatch_contributor_insight_rule" "app" {
  rule_name = "tofu-observability-cohort-cloudwatch-contributor-insight-rule"
  rule_definition = jsonencode({
    Schema = {
      Name    = "CloudWatchLogRule"
      Version = 1
    }
    LogGroupNames = ["tofu-observability-cohort-insight-source"]
    LogFormat     = "JSON"
    Contribution = {
      Keys = ["$.ip"]
    }
    AggregateOn = "Count"
  })
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudwatch_contributor_insight_rule.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: connection_arn is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN"); http_method is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected http_method to be one of [...]"). connection_arn references this same cohort's aws_cloudwatch_event_connection.app.arn - an unknown value at validate time, which the ARN-format check never runs against - rather than a literal placeholder.
resource "aws_cloudwatch_event_api_destination" "app" {
  name                = "tofu-observability-cohort-cloudwatch-event-api-destination"
  connection_arn      = aws_cloudwatch_event_connection.app.arn
  http_method         = "POST"
  invocation_endpoint = "placeholder"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name is length-limited to 48 characters (validate: "expected length of name to be in the range (1 - 48)"), and this cohort's own name ("observability") makes the generic tofu-<cohort>-cohort-<type> placeholder 51 characters - shortened here to a value that still names the cohort and the type. event_source_arn is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN"); it references this same cohort's aws_cloudwatch_event_bus.app.arn - an unknown value at validate time, which the ARN-format check never runs against - rather than a literal placeholder.
resource "aws_cloudwatch_event_archive" "app" {
  name             = "tofu-obs-event-archive"
  event_source_arn = aws_cloudwatch_event_bus.app.arn
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_cloudwatch_event_bus" "app" {
  name = "tofu-observability-cohort-cloudwatch-event-bus"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudwatch_event_bus.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: authorization_type is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected authorization_type to be one of [...]"); auth_parameters is a required block, but the provider requires exactly one of its api_key/basic/oauth children set in practice (validate: "Invalid combination of arguments" x3 on an empty auth_parameters), and the chosen child's own key/value pair is itself required.
resource "aws_cloudwatch_event_connection" "app" {
  name               = "tofu-observability-cohort-cloudwatch-event-connection"
  authorization_type = "API_KEY"
  auth_parameters {
    api_key {
      key   = "x-api-key"
      value = "tofu-observability-cohort-api-key-value"
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: event_bus is a required block appearing exactly twice in the schema (a global endpoint always names a primary and a secondary event bus), and each child's event_bus_arn is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN" x2); the generic pass's placeholder string is neither. The first bus references this same cohort's aws_cloudwatch_event_bus.app.arn; the second is a literal placeholder in a different region, since a global endpoint's two buses are documented as living in different regions. routing_config.failover_config's primary.health_check and secondary.route are both required in practice - not caught by "terraform validate" (which only checks the arguments a block actually has, and the generic pass rendered both primary and secondary as empty blocks), only surfaced applying against floci (apply: "missing required field, CreateEndpointInput.RoutingConfig.FailoverConfig.Primary" and "...Secondary").
resource "aws_cloudwatch_event_endpoint" "app" {
  name = "tofu-observability-cohort-cloudwatch-event-endpoint"
  event_bus {
    event_bus_arn = aws_cloudwatch_event_bus.app.arn
  }
  event_bus {
    event_bus_arn = "arn:aws:events:us-west-2:000000000000:event-bus/tofu-observability-cohort-secondary"
  }
  routing_config {
    failover_config {
      primary {
        health_check = "arn:aws:route53:::healthcheck/00000000-0000-0000-0000-000000000000"
      }
      secondary {
        route = "us-west-2"
      }
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: principal is a required string the schema does not constrain, but the provider validates it is "*" or a 12-digit AWS account ID (validate: "\"principal\" must be * or a 12 digit AWS account ID"); the generic placeholder string is neither
resource "aws_cloudwatch_event_permission" "app" {
  statement_id = "tofu-observability-cohort-cloudwatch-event-permission"
  principal    = "*"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: policy_document is a required string the schema does not constrain, but the provider validates it is well-formed JSON (validate: "contains an invalid JSON"); policy_type is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected policy_type to be one of [...]"); the generic placeholder string satisfies neither
resource "aws_cloudwatch_log_account_policy" "app" {
  policy_document = jsonencode({
    DestinationArn = "arn:aws:lambda:us-east-1:000000000000:function:tofu-observability-cohort-log-account-policy-target"
    FilterPattern  = ""
    Distribution   = "Random"
  })
  policy_name = aws_cloudwatch_log_resource_policy.app.policy_name
  policy_type = "SUBSCRIPTION_FILTER_POLICY"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_cloudwatch_log_anomaly_detector" "app" {
  enabled            = false
  log_group_arn_list = ["placeholder"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudwatch_log_anomaly_detector.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: delivery_destination_arn is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "The provided value cannot be parsed as an ARN"); it references this same cohort's aws_cloudwatch_log_delivery_destination.app.arn - an unknown value at validate time, which the ARN-format check never runs against - rather than a literal placeholder. delivery_source_name is likewise pointed at this same cohort's aws_cloudwatch_log_delivery_source.app.name instead of the generic pass's disconnected literal string, so the delivery names a source that actually exists in this estate.
resource "aws_cloudwatch_log_delivery" "app" {
  delivery_destination_arn = aws_cloudwatch_log_delivery_destination.app.arn
  delivery_source_name     = aws_cloudwatch_log_delivery_source.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudwatch_log_delivery.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: name is length-limited to 60 characters (validate: "Attribute name string length must be between 1 and 60"), and this cohort's own name ("observability") makes the generic tofu-<cohort>-cohort-<type> placeholder 61 characters - shortened here by one character's worth of margin. delivery_destination_configuration is Optional in the schema, but the provider requires it in practice unless delivery_destination_type is XRAY (validate: "delivery_destination_configuration is required when delivery_destination_type is not XRAY") - set to XRAY here rather than inventing a destination_resource_arn this cohort has no real destination for.
resource "aws_cloudwatch_log_delivery_destination" "app" {
  name                      = "tofu-obs-log-delivery-destination"
  delivery_destination_type = "XRAY"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudwatch_log_delivery_destination.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: resource_arn is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "The provided value cannot be parsed as an ARN"); the generic placeholder string is not. log_type is paired with resource_arn here as the provider's own documented CloudFront example (ACCESS_LOGS / a CloudFront distribution ARN) rather than left at the generic placeholder, since the two arguments name the same source in practice even though only the ARN shape is checked locally.
resource "aws_cloudwatch_log_delivery_source" "app" {
  name         = "tofu-observability-cohort-cloudwatch-log-delivery-source"
  log_type     = "ACCESS_LOGS"
  resource_arn = "arn:aws:cloudfront::000000000000:distribution/EDFDVBD6EXAMPLE"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudwatch_log_delivery_source.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: role_arn and target_arn are both required strings the schema does not constrain, but the provider validates each is a well-formed ARN (validate: "is an invalid ARN" x2); the generic placeholder string is neither
resource "aws_cloudwatch_log_destination" "app" {
  name       = "tofu-observability-cohort-cloudwatch-log-destination"
  role_arn   = "arn:aws:iam::000000000000:role/tofu-observability-cohort-log-destination"
  target_arn = "arn:aws:kinesis:us-east-1:000000000000:stream/tofu-observability-cohort-log-destination-target"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_cloudwatch_log_destination.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_cloudwatch_log_metric_filter" "app" {
  log_group_name = "tofu-observability-cohort-cloudwatch-log-metric-filter"
  name           = aws_cloudwatch_alarm_mute_rule.app.name
  pattern        = "placeholder"
  metric_transformation {
    name      = "placeholder"
    namespace = "placeholder"
    value     = "placeholder"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: policy_document is a required string the schema does not constrain, but the provider validates it is well-formed JSON (validate: "contains an invalid JSON"); the generic placeholder string is not
resource "aws_cloudwatch_log_resource_policy" "app" {
  policy_name = "tofu-observability-cohort-cloudwatch-log-resource-policy"
  policy_document = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = { Service = "logs.amazonaws.com" }
      Action    = "logs:PutLogEvents"
      Resource  = "*"
    }]
  })
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_cloudwatch_log_stream" "app" {
  log_group_name = "tofu-observability-cohort-cloudwatch-log-stream"
  name           = aws_cloudwatch_alarm_mute_rule.app.name
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: destination_arn is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "is an invalid ARN"); the generic placeholder string is not
resource "aws_cloudwatch_log_subscription_filter" "app" {
  destination_arn = "arn:aws:lambda:us-east-1:000000000000:function:tofu-observability-cohort-log-subscription-target"
  filter_pattern  = "placeholder"
  log_group_name  = "tofu-observability-cohort-cloudwatch-log-subscription-filter"
  name            = aws_cloudwatch_alarm_mute_rule.app.name
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: transformer_config is a required argument typed as a list of nested blocks with MinItems 0 in the wire schema, so the generic required-only pass never renders one at all (validate: "Block transformer_config must have a configuration value as the provider has marked it as required"), and the provider requires its first processor to be a parser - parse_json is the simplest one. log_group_arn is a required string the schema does not constrain, but the provider validates it is a well-formed ARN (validate: "The provided value cannot be parsed as an ARN"); this cohort admits no aws_cloudwatch_log_group of its own (that type ratifies elsewhere, in the client-named section above), so the ARN is a literal placeholder rather than a cross-reference.
resource "aws_cloudwatch_log_transformer" "app" {
  log_group_arn = "arn:aws:logs:us-east-1:000000000000:log-group:/tofu-observability-cohort-log-transformer-source"
  transformer_config {
    parse_json {
    }
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_cloudwatch_otel_enrichment" "app" {
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_cloudwatch_query_definition" "app" {
  name         = aws_cloudwatch_alarm_mute_rule.app.name
  query_string = "placeholder"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: account_access_type, authentication_providers and permission_type are each required strings (or a set of them) the schema does not constrain to an enum, but the provider validates each against a fixed set (validate: "expected ... to be one of [...]" x3); the generic placeholder string matches none of them
resource "aws_grafana_workspace" "app" {
  account_access_type      = "CURRENT_ACCOUNT"
  authentication_providers = ["AWS_SSO"]
  permission_type          = "SERVICE_MANAGED"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_grafana_workspace.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: domain and domain_list are both Optional in the schema, so the generic pass sets neither, but the provider requires exactly one of them (validate: "one of domain,domain_list must be specified" x2)
resource "aws_rum_app_monitor" "app" {
  name   = "tofu-observability-cohort-rum-app-monitor"
  domain = "tofu-observability-cohort.example.com"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_rum_app_monitor.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_sfn_activity" "app" {
  name = aws_cloudwatch_alarm_mute_rule.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_sfn_activity.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_synthetics_canary" "app" {
  name                 = "tofu-observability-cohort-synthetics-canary"
  artifact_s3_location = "placeholder"
  execution_role_arn   = aws_iam_role.observability.arn
  handler              = "placeholder"
  runtime_version      = "placeholder"
  schedule {
    expression = "placeholder"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_synthetics_canary.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_synthetics_group" "app" {
  name = "tofu-observability-cohort-synthetics-group"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_synthetics_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_xray_group" "app" {
  filter_expression = "placeholder"
  group_name        = "tofu-observability-cohort-xray-group"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_xray_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: policy_document is a required string the schema does not constrain, but the provider validates it is well-formed JSON (validate: "A string value was provided that is not valid JSON string format"); the generic placeholder string is not
resource "aws_xray_resource_policy" "app" {
  policy_name = "tofu-observability-cohort-xray-resource-policy"
  policy_document = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = { Service = "xray.amazonaws.com" }
      Action    = "xray:GetSamplingStatisticSummaries"
      Resource  = "*"
    }]
  })
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: rule_name is length-limited to 32 characters (validate: "expected length of rule_name to be in the range (1 - 32)"), and this cohort's own name ("observability") makes the generic tofu-<cohort>-cohort-<type> placeholder 46 characters - shortened here to a value that still names the cohort and the type. http_method is length-limited to 10 characters and the generic "placeholder" string is 11; priority must be in (1 - 9999) and version must be at least 1, but both are numeric arguments the generic required-only pass zero-values rather than infers a real member for; resource_arn has no local format check but "*" (match any resource) is the provider's own documented value for a rule with no specific resource.
resource "aws_xray_sampling_rule" "app" {
  rule_name      = "tofu-obs-xray-rule"
  fixed_rate     = 0
  host           = "placeholder"
  http_method    = "GET"
  priority       = 1000
  reservoir_size = 0
  resource_arn   = "*"
  service_name   = "tofu-obs-xray-rule"
  service_type   = "placeholder"
  url_path       = "placeholder"
  version        = 1
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_xray_sampling_rule.app"
  }
}
