# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: roles is Optional-shaped in nothing else - the provider requires at least one roles block in practice (validate: "Block roles must have a configuration value as the provider has marked it as required"), with role_arn and role_type both Required inside it (schema doesn't surface either as top-level Required).
resource "aws_auditmanager_assessment" "app" {
  framework_id = aws_auditmanager_framework.app.id
  name         = aws_config_config_rule.app.name
  roles {
    role_arn  = aws_iam_role.governance.arn
    role_type = "PROCESS_OWNER"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_auditmanager_assessment.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: control_sets.name
resource "aws_auditmanager_framework" "app" {
  name = aws_config_config_rule.app.name
  control_sets {
    name = "example"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_auditmanager_framework.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: action_type, approval_model, notification_type, action_threshold.action_threshold_type, action_threshold.action_threshold_value, definition.iam_action_definition.policy_arn, definition.iam_action_definition.roles, subscriber.address, subscriber.subscription_type
resource "aws_budgets_budget_action" "app" {
  action_type        = "APPLY_IAM_POLICY"
  approval_model     = "AUTOMATIC"
  budget_name        = "tofu-governance-cohort-budgets-budget-action"
  execution_role_arn = aws_iam_role.governance.arn
  notification_type  = "ACTUAL"
  action_threshold {
    action_threshold_type  = "ABSOLUTE_VALUE"
    action_threshold_value = 100
  }
  definition {
    iam_action_definition {
      policy_arn = aws_iam_policy.governance.arn
      roles      = [aws_iam_role.governance.name]
    }
  }
  subscriber {
    address           = "example@example.example"
    subscription_type = "EMAIL"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_budgets_budget_action.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: source.owner, source.source_detail.message_type, source.custom_policy_details.policy_runtime, source.custom_policy_details.policy_text
resource "aws_config_config_rule" "app" {
  name = "tofu-governance-cohort-config-config-rule"
  source {
    owner = "CUSTOM_POLICY"
    source_detail {
      message_type = "ConfigurationItemChangeNotification"
    }
    custom_policy_details {
      policy_runtime = "guard-2.x.x"
      policy_text    = "\t  rule tableisactive when\n\t\t  resourceType == \"AWS::DynamoDB::Table\" {\n\t\t  configuration.tableStatus == ['ACTIVE']\n\t  }\n\t  \n\t  rule checkcompliance when\n\t\t  resourceType == \"AWS::DynamoDB::Table\"\n\t\t  tableisactive {\n\t\t\t  supplementaryConfiguration.ContinuousBackupsDescription.pointInTimeRecoveryDescription.pointInTimeRecoveryStatus == \"ENABLED\"\n\t  }\n"
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_config_config_rule.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: account_aggregation_source.account_ids, account_aggregation_source.regions
resource "aws_config_configuration_aggregator" "app" {
  name = "tofu-governance-cohort-config-configuration-aggregator"
  account_aggregation_source {
    account_ids = ["123456789012"]
    regions     = ["us-west-2"]
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_config_configuration_aggregator.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires neither template_body nor template_s3_uri, but the provider requires exactly one of them in practice (validate: "one of template_body,template_s3_uri must be specified"); the generic pass sets neither. template_body is set to a minimal, syntactically valid Config conformance pack template wrapping one managed rule.
resource "aws_config_conformance_pack" "app" {
  name          = "tofu-governance-cohort-config-conformance-pack"
  template_body = <<-TEMPLATE
  Resources:
    ConformancePackVersioning:
      Type: AWS::Config::ConfigRule
      Properties:
        Source:
          Owner: AWS
          SourceIdentifier: S3_BUCKET_VERSIONING_ENABLED
  TEMPLATE
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: input_parameter.parameter_name, input_parameter.parameter_value
resource "aws_config_organization_conformance_pack" "app" {
  name = "tofu-governance-cohort-config-organization-conformance-pack"
  input_parameter {
    parameter_name  = "AccessKeysRotatedParameterMaxAccessKeyAge"
    parameter_value = "90"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: target_type is Required and the provider validates it against a closed enum with exactly one member (validate: "expected target_type to be one of [\"SSM_DOCUMENT\"]"); the generic placeholder string is not it.
resource "aws_config_remediation_configuration" "app" {
  config_rule_name = "tofu-governance-cohort-config-remediation-configuration"
  target_id        = "AWS-PublishSNSNotification"
  target_type      = "SSM_DOCUMENT"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: baseline_identifier, baseline_version, target_identifier, parameters.key, parameters.value
resource "aws_controltower_baseline" "app" {
  baseline_identifier = "arn:aws:controltower:us-east-1::baseline/17BSJV3IGJ2QSGA2"
  baseline_version    = "4.0"
  target_identifier   = aws_organizations_organizational_unit.app.arn
  parameters {
    key   = "IdentityCenterEnabledBaselineArn"
    value = "arn:aws:controltower:us-east-1:664418989480:enabledbaseline/XALULM96QHI525UOC"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_controltower_baseline.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: control_identifier and target_identifier are both Required and validated as well-formed ARNs (validate: "is an invalid ARN: arn: invalid prefix"); the generic placeholder string is neither. Set to the shapes the provider's own documented Import example uses: target_identifier an organizational unit ARN, control_identifier an AWS-defined control ARN - neither references a real sibling resource, the same "no real sibling to reference" shape several other overrides in this file accept.
resource "aws_controltower_control" "app" {
  target_identifier  = "arn:aws:organizations::123456789012:ou/o-exampleorgid/ou-exampleroot-exampleouid1"
  control_identifier = "arn:aws:controltower:us-east-1::control/AWS-GR_S3_BUCKET_VERSIONING_ENABLED"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires manifest_json as a plain string, but the provider validates it is well-formed JSON (validate: "\"manifest_json\" contains an invalid JSON"); the generic string placeholder is not JSON.
resource "aws_controltower_landing_zone" "app" {
  manifest_json = jsonencode({
    governedRegions = ["us-east-1"]
    organizationStructure = {
      security = { name = "Security" }
      sandbox  = { name = "Sandbox" }
    }
    centralizedLogging = {
      accountId = "123456789012"
      configurations = {
        loggingBucket       = { retentionDays = 365 }
        accessLoggingBucket = { retentionDays = 3650 }
      }
    }
  })
  version = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_controltower_landing_zone.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: email is Required and the provider validates it is a well-formed email address (validate: "invalid value for email (must be a valid email address)"); the generic placeholder string is not one.
resource "aws_organizations_account" "app" {
  email = "tofu-governance-cohort@example.com"
  name  = aws_config_config_rule.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_organizations_account.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: parent_id is Required and the provider validates it is a well-formed root or OU identifier (validate: "invalid value for parent_id"); the generic placeholder string is neither. Set to a syntactically valid organization root ID - no real root or parent OU is part of this cohort to reference.
resource "aws_organizations_organizational_unit" "app" {
  name      = aws_config_config_rule.app.name
  parent_id = "r-a1b2"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_organizations_organizational_unit.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires content as a plain string, but the provider validates it is well-formed JSON (validate: "\"content\" contains an invalid JSON"); the generic string placeholder is not JSON.
resource "aws_organizations_policy" "app" {
  content = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect   = "Allow"
      Action   = "*"
      Resource = "*"
    }]
  })
  name = aws_config_config_rule.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_organizations_policy.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires content as a plain string, but the provider validates it is well-formed JSON (validate: "\"content\" contains an invalid JSON"); the generic string placeholder is not JSON.
resource "aws_organizations_resource_policy" "app" {
  content = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = "*"
      Action    = "organizations:DescribeOrganization"
      Resource  = "*"
    }]
  })
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_organizations_resource_policy.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: type
resource "aws_resourceexplorer2_index" "app" {
  type = "LOCAL"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_resourceexplorer2_index.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: filters.filter_string, included_property.name
resource "aws_resourceexplorer2_view" "app" {
  name = aws_config_config_rule.app.name
  filters {
    filter_string = "resourcetype:ec2:instance"
  }
  included_property {
    name = "tags"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_resourceexplorer2_view.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: resource_query.query
resource "aws_resourcegroups_group" "app" {
  name = "tofu-governance-cohort-resourcegroups-group"
  resource_query {
    query = "{\n  \"ResourceTypeFilters\": [\n    \"AWS::EC2::Instance\"\n  ],\n  \"TagFilters\": [\n    {\n      \"Key\": \"Stage\",\n      \"Values\": [\"Test\"]\n    }\n  ]\n}\n"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_resourcegroups_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: description
resource "aws_servicecatalog_portfolio" "app" {
  name          = aws_config_config_rule.app.name
  provider_name = "tofu-governance-cohort-servicecatalog-portfolio"
  description   = "List of my organizations apps"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_servicecatalog_portfolio.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: type and principal_id are both Required; type is validated against a closed enum (validate: "expected type to be one of [\"ACCOUNT\" \"ORGANIZATION\" \"ORGANIZATIONAL_UNIT\" \"ORGANIZATION_MEMBER_ACCOUNT\"]") and, for the ACCOUNT case this override picks, principal_id is validated as a 12-digit account ID (validate: "must be a valid account ID, organization ARN/ID, or organizational unit ARN/ID") - the generic placeholder string satisfies neither.
resource "aws_servicecatalog_portfolio_share" "app" {
  portfolio_id = aws_servicecatalog_portfolio.app.id
  principal_id = "123456789012"
  type         = "ACCOUNT"
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: type is Required and the provider validates it against a closed enum (validate: "expected type to be one of [\"CLOUD_FORMATION_TEMPLATE\" \"MARKETPLACE\" \"TERRAFORM_OPEN_SOURCE\" \"TERRAFORM_CLOUD\" \"EXTERNAL\"]"); the generic placeholder string is not a member. provisioning_artifact_parameters is a required block whose own template_physical_id and template_url are both Optional in the schema, but the provider requires exactly one of them in practice (validate: "one of ... must be specified"), and the generic pass sets neither.
resource "aws_servicecatalog_product" "app" {
  name  = aws_config_config_rule.app.name
  owner = "placeholder"
  type  = "CLOUD_FORMATION_TEMPLATE"
  provisioning_artifact_parameters {
    template_url = "https://s3.amazonaws.com/tofu-governance-cohort/servicecatalog-product-template.json"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_servicecatalog_product.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires none of product_id/product_name or provisioning_artifact_id/provisioning_artifact_name; the provider requires exactly one of each pair in practice (validate: "one of ... must be specified" ×4), and the generic pass sets none of the four. product_id is wired to this cohort's own aws_servicecatalog_product; provisioning_artifact_name stays a literal, since no admitted type in this cohort represents a specific provisioning artifact.
resource "aws_servicecatalog_provisioned_product" "app" {
  name                       = aws_config_config_rule.app.name
  product_id                 = aws_servicecatalog_product.app.id
  provisioning_artifact_name = "v1"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_servicecatalog_provisioned_product.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_servicecatalogappregistry_application" "app" {
  name = aws_config_config_rule.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_servicecatalogappregistry_application.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: schema requires attributes as a plain string, but the provider validates it is well-formed JSON (validate: "Invalid JSON String Value"); the generic string placeholder is not JSON.
resource "aws_servicecatalogappregistry_attribute_group" "app" {
  attributes = jsonencode({
    environment = "governance"
  })
  name = aws_config_config_rule.app.name
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_servicecatalogappregistry_attribute_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.59.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_servicecatalogappregistry_attribute_group_association" "app" {
  application_id     = aws_servicecatalogappregistry_application.app.id
  attribute_group_id = aws_servicecatalogappregistry_attribute_group.app.id
}
