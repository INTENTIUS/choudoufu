# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: source_uri is a required string the schema does not constrain, but the provider validates it against a fixed pattern (validate: "invalid value for source_uri (should be a valid source uri)"), documented as a connector-profile scheme like "Salesforce://AppFlow/example"; the generic placeholder string does not match it
resource "aws_appintegrations_data_integration" "app" {
  kms_key    = "placeholder"
  name       = aws_appintegrations_event_integration.app.name
  source_uri = "Salesforce://AppFlow/tofu-data-movement-cohort"
  schedule_config {
    first_execution_from = "placeholder"
    object               = "placeholder"
    schedule_expression  = "placeholder"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_appintegrations_data_integration.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: event_filter.source is a required string the schema does not constrain, but the provider validates it against a fixed prefix regex (validate: "should be not be more than 255 alphanumeric, forward slashes, dots, underscores, or hyphen characters" - the message text does not match the actual pattern, which the provider's own source requires to start "aws.partner/"); the generic placeholder string does not start with it
resource "aws_appintegrations_event_integration" "app" {
  name            = "tofu-data-movement-cohort-appintegrations-event-integration"
  eventbridge_bus = "placeholder"
  event_filter {
    source = "aws.partner/tofu-data-movement-cohort"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_appintegrations_event_integration.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: activation_key and ip_address are both Optional in the schema, but the provider requires one of them at apply time ("one of activation_key or ip_address is required") - an apply-time-only gap `terraform validate` does not catch, found by hand-verifying this cohort against the pinned floci image. ip_address makes Terraform itself perform an HTTP GET against that address to retrieve the real activation key before the DataSync API call happens at all, which hangs indefinitely against any address that is not an actual reachable agent appliance; activation_key needs no such round-trip, so it is the one this override sets
resource "aws_datasync_agent" "app" {
  activation_key = "placeholder-activation-key"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_agent.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: authentication_type is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected authentication_type to be one of [\"SAS\" \"NONE\"]"); agent_arns is a required set of strings the provider validates are well-formed ARNs (validate: "is an invalid ARN")
resource "aws_datasync_location_azure_blob" "app" {
  agent_arns          = ["arn:aws:datasync:us-east-1:000000000000:agent/agent-tofudata-movementcohort"]
  authentication_type = "SAS"
  container_url       = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_azure_blob.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: efs_file_system_arn and ec2_config's security_group_arns/subnet_arn are all required strings the provider validates are well-formed ARNs (validate: "is an invalid ARN"); the generic placeholder string is not one
resource "aws_datasync_location_efs" "app" {
  efs_file_system_arn = "arn:aws:elasticfilesystem:us-east-1:000000000000:file-system/fs-tofudata-movementcohort"
  ec2_config {
    security_group_arns = ["arn:aws:ec2:us-east-1:000000000000:security-group/sg-tofudata-movementcohort"]
    subnet_arn          = "arn:aws:ec2:us-east-1:000000000000:subnet/subnet-tofudata-movementcohort"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_efs.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: fsx_filesystem_arn and security_group_arns are both required strings the provider validates are well-formed ARNs (validate: "is an invalid ARN")
resource "aws_datasync_location_fsx_lustre_file_system" "app" {
  fsx_filesystem_arn  = "arn:aws:fsx:us-east-1:000000000000:file-system/fs-tofudata-movementcohort"
  security_group_arns = ["arn:aws:ec2:us-east-1:000000000000:security-group/sg-tofudata-movementcohort"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_fsx_lustre_file_system.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: security_group_arns and storage_virtual_machine_arn are both required strings the provider validates are well-formed ARNs (validate: "is an invalid ARN"); protocol's nfs and smb sub-blocks are both Optional in the schema, but the provider requires exactly one set (validate: "one of protocol.0.nfs,protocol.0.smb must be specified"), and the generic pass renders neither, unlike its sibling aws_datasync_location_fsx_openzfs_file_system, whose protocol block has only one sub-block to choose
resource "aws_datasync_location_fsx_ontap_file_system" "app" {
  security_group_arns         = ["arn:aws:ec2:us-east-1:000000000000:security-group/sg-tofudata-movementcohort"]
  storage_virtual_machine_arn = "arn:aws:fsx:us-east-1:000000000000:storage-virtual-machine/svm-tofudata-movementcohort"
  protocol {
    nfs {
      mount_options {
      }
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_fsx_ontap_file_system.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: fsx_filesystem_arn and security_group_arns are both required strings the provider validates are well-formed ARNs (validate: "is an invalid ARN")
resource "aws_datasync_location_fsx_openzfs_file_system" "app" {
  fsx_filesystem_arn  = "arn:aws:fsx:us-east-1:000000000000:file-system/fs-tofudata-movementcohort"
  security_group_arns = ["arn:aws:ec2:us-east-1:000000000000:security-group/sg-tofudata-movementcohort"]
  protocol {
    nfs {
      mount_options {
      }
    }
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_fsx_openzfs_file_system.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: fsx_filesystem_arn and security_group_arns are both required strings the provider validates are well-formed ARNs (validate: "is an invalid ARN")
resource "aws_datasync_location_fsx_windows_file_system" "app" {
  fsx_filesystem_arn  = "arn:aws:fsx:us-east-1:000000000000:file-system/fs-tofudata-movementcohort"
  password            = "placeholder"
  security_group_arns = ["arn:aws:ec2:us-east-1:000000000000:security-group/sg-tofudata-movementcohort"]
  user                = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_fsx_windows_file_system.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: agent_arns is a required set of strings the provider validates are well-formed ARNs (validate: "is an invalid ARN"); name_node.port is a required number the schema does not range-constrain, but the provider validates it is a valid port (validate: "expected \"name_node.0.port\" to be a valid port number, got: 0"), and the generic pass's numeric zero placeholder is not one; authentication_type is Optional in the schema, but the provider's own AWS SDK request validation requires it client-side before any HTTP call is made ("missing required field, CreateLocationHdfsInput.AuthenticationType") - `terraform validate` does not catch this one either, only an apply against the pinned floci image did; SIMPLE also requires simple_user, which the schema likewise leaves Optional
resource "aws_datasync_location_hdfs" "app" {
  agent_arns = ["arn:aws:datasync:us-east-1:000000000000:agent/agent-tofudata-movementcohort"]
  name_node {
    hostname = "placeholder"
    port     = 8020
  }
  authentication_type = "SIMPLE"
  simple_user         = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_hdfs.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: on_prem_config.agent_arns is a required set of strings the provider validates are well-formed ARNs (validate: "is an invalid ARN")
resource "aws_datasync_location_nfs" "app" {
  server_hostname = "placeholder"
  subdirectory    = "placeholder"
  on_prem_config {
    agent_arns = ["arn:aws:datasync:us-east-1:000000000000:agent/agent-tofudata-movementcohort"]
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_nfs.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: server_hostname
resource "aws_datasync_location_object_storage" "app" {
  bucket_name     = "tofu-data-movement-cohort-datasync-location-object-storage"
  server_hostname = "example"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_object_storage.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: s3_bucket_arn is a required string the provider validates is a well-formed ARN (validate: "is an invalid ARN")
resource "aws_datasync_location_s3" "app" {
  s3_bucket_arn = "arn:aws:s3:::tofu-data-movement-cohort-datasync-s3"
  subdirectory  = "placeholder"
  s3_config {
    bucket_access_role_arn = aws_iam_role.data-movement.arn
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_s3.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: agent_arns is a required set of strings the provider validates are well-formed ARNs (validate: "is an invalid ARN")
resource "aws_datasync_location_smb" "app" {
  agent_arns      = ["arn:aws:datasync:us-east-1:000000000000:agent/agent-tofudata-movementcohort"]
  password        = "placeholder"
  server_hostname = "placeholder"
  subdirectory    = "placeholder"
  user            = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_location_smb.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: destination_location_arn and source_location_arn are both required strings the provider validates are well-formed ARNs (validate: "is an invalid ARN") - overridden to this cohort's own aws_datasync_location_nfs.app.arn and aws_datasync_location_s3.app.arn, the cross-resource reference issue #56 asks for and the provider's own documented example uses verbatim
resource "aws_datasync_task" "app" {
  destination_location_arn = aws_datasync_location_s3.app.arn
  source_location_arn      = aws_datasync_location_nfs.app.arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_datasync_task.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: certificate_pem and certificate_wallet are both Optional in the schema, but the provider requires exactly one set (validate: "one of certificate_pem,certificate_wallet must be specified"), and the generic pass sets neither
resource "aws_dms_certificate" "app" {
  certificate_id  = "tofu-data-movement-cohort-dms-certificate"
  certificate_pem = "placeholder-pem"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_dms_certificate.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: endpoint_type and engine_name are both required strings the schema does not constrain to an enum, but the provider validates each against a fixed set (validate: "expected ... to be one of [...]"); "s3" is not among engine_name's valid values - that shape is what the separate aws_dms_s3_endpoint type below covers
resource "aws_dms_endpoint" "app" {
  endpoint_id   = "tofu-data-movement-cohort-dms-endpoint"
  endpoint_type = "source"
  engine_name   = "mysql"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_dms_endpoint.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: sns_topic_arn is a required string the provider validates is a well-formed ARN (validate: "is an invalid ARN"), and no aws_sns_topic is part of this cohort to reference, the same gap aws_db_event_subscription's own override fills; source_type is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected source_type to be one of [...]")
resource "aws_dms_event_subscription" "app" {
  name             = "tofu-data-movement-cohort-dms-event-subscription"
  event_categories = ["placeholder"]
  sns_topic_arn    = "arn:aws:sns:us-east-1:000000000000:tofu-data-movement-cohort-events"
  source_type      = "replication-task"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_dms_event_subscription.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: replication_type is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected replication_type to be one of [...]"); source_endpoint_arn and target_endpoint_arn are both required strings the provider validates are well-formed ARNs (validate: "is an invalid ARN") - overridden to this cohort's own aws_dms_endpoint.app.endpoint_arn and aws_dms_s3_endpoint.app.endpoint_arn, the cross-resource reference issue #56 asks for and the provider's own documented example uses the same way; table_mappings is a required string the provider validates is well-formed JSON (validate: "contains an invalid JSON"); compute_config.replication_subnet_group_id is Optional in the schema, but the generic pass already renders a placeholder for it - overridden to this cohort's own aws_dms_replication_subnet_group.app.replication_subnet_group_id instead
resource "aws_dms_replication_config" "app" {
  replication_config_identifier = "placeholder"
  replication_type              = "full-load"
  source_endpoint_arn           = aws_dms_endpoint.app.endpoint_arn
  table_mappings = jsonencode({
    rules = [{
      rule-type = "selection"
      rule-id   = "1"
      rule-name = "1"
      object-locator = {
        schema-name = "%"
        table-name  = "%"
      }
      rule-action = "include"
    }]
  })
  target_endpoint_arn = aws_dms_s3_endpoint.app.endpoint_arn
  compute_config {
    replication_subnet_group_id = aws_dms_replication_subnet_group.app.replication_subnet_group_id
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_dms_replication_config.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: replication_instance_class
resource "aws_dms_replication_instance" "app" {
  replication_instance_id    = "tofu-data-movement-cohort-dms-replication-instance"
  replication_instance_class = "dms.t3.micro"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_dms_replication_instance.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: subnet_ids is a required list with a provider-enforced 2-item minimum (validate: "Attribute subnet_ids requires 2 item minimum, but config has only 1 declared"), the same MinItems gap aws_route53_resolver_firewall_rule's ip_address override fixes in the observability batch, and the schema itself does not say so
resource "aws_dms_replication_subnet_group" "app" {
  replication_subnet_group_id          = "tofu-data-movement-cohort-dms-replication-subnet-group"
  replication_subnet_group_description = "placeholder"
  subnet_ids                           = ["subnet-0123456789abcdef0", "subnet-0123456789abcdef1"]
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_dms_replication_subnet_group.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: migration_type is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected migration_type to be one of [...]"); replication_instance_arn, source_endpoint_arn and target_endpoint_arn are all required strings the provider validates are well-formed ARNs (validate: "is an invalid ARN") - overridden to this cohort's own aws_dms_replication_instance.app.replication_instance_arn, aws_dms_endpoint.app.endpoint_arn and aws_dms_s3_endpoint.app.endpoint_arn, the cross-resource reference issue #56 asks for; table_mappings is a required string the provider validates is well-formed JSON (validate: "contains an invalid JSON")
resource "aws_dms_replication_task" "app" {
  replication_task_id      = "tofu-data-movement-cohort-dms-replication-task"
  migration_type           = "full-load"
  replication_instance_arn = aws_dms_replication_instance.app.replication_instance_arn
  source_endpoint_arn      = aws_dms_endpoint.app.endpoint_arn
  table_mappings = jsonencode({
    rules = [{
      rule-type = "selection"
      rule-id   = "1"
      rule-name = "1"
      object-locator = {
        schema-name = "%"
        table-name  = "%"
      }
      rule-action = "include"
    }]
  })
  target_endpoint_arn = aws_dms_s3_endpoint.app.endpoint_arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_dms_replication_task.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: endpoint_type is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected endpoint_type to be one of [...]") - set to "target", pairing with aws_dms_endpoint.app's own "source" so aws_dms_replication_config and aws_dms_replication_task above have one endpoint of each type to reference
resource "aws_dms_s3_endpoint" "app" {
  endpoint_id             = "tofu-data-movement-cohort-dms-s3-endpoint"
  bucket_name             = "tofu-data-movement-cohort-dms-s3-endpoint"
  endpoint_type           = "target"
  service_access_role_arn = aws_iam_role.data-movement.arn
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_dms_s3_endpoint.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_transfer_connector" "app" {
  access_role = "placeholder"
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_transfer_connector.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: none
resource "aws_transfer_server" "app" {
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_transfer_server.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: doc example: home_directory_mappings.entry, home_directory_mappings.target
resource "aws_transfer_user" "app" {
  role      = aws_iam_role.data-movement.arn
  server_id = aws_transfer_server.app.id
  user_name = "tofu-data-movement-cohort-transfer-user"
  home_directory_mappings {
    entry  = "/test.pdf"
    target = "/bucket3/test-path/tftestuser.pdf"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_transfer_user.app"
  }
}

# Coverage: generated by estate-gen from hashicorp/aws 6.58.0 and the identity table (internal/live/identity/table.go).
# overrides: steps.type is a required string the schema does not constrain to an enum, but the provider validates it against a fixed set (validate: "expected type to be one of [...]"); DELETE needs no further delete_step_details block, the smaller of the five shapes
resource "aws_transfer_workflow" "app" {
  steps {
    type = "DELETE"
  }
  tags = {
    tofu-estate  = local.estate_tag
    tofu-address = "aws_transfer_workflow.app"
  }
}
