{
  "_comment": "Tracks real, popularity-weighted OSS Terraform/OpenTofu estates run through the five-stage crossing pipeline (cold deploy -> migrate -> test plan -> test apply -> drift and reconverge), same spirit as cohort-acceptance.json but for real-world estates rather than estate-gen's synthetic cohorts. A stage is 'pass', 'fail', or 'not_run' - not_run means the estate's script does not yet exercise that stage, which is itself a gap worth closing, not a silent pass. Updated by the orchestrator from each crossing's own verified run, not by an agent editing this file directly - the same discipline live/cohort-acceptance.json uses, so N concurrent crossings never fight over one JSON file's sort order.",
  "stage_order": ["cold_deploy", "migrate", "test_plan", "test_apply", "drift_reconverge"],
  "totals": {
    "estates": 5,
    "cold_deploy": { "pass": 4, "fail": 1, "not_run": 0 },
    "migrate": { "pass": 3, "fail": 1, "not_run": 1 },
    "test_plan": { "pass": 1, "fail": 3, "not_run": 1 },
    "test_apply": { "pass": 1, "fail": 0, "not_run": 4 },
    "drift_reconverge": { "pass": 0, "fail": 0, "not_run": 5 }
  },
  "estates": [
    {
      "dir": "live/e2e/reference-ec2-vpc",
      "source": "hand-written reference project: the plainest getting-started shape (VPC/subnet/SG/IGW/EC2)",
      "lane": "reference",
      "stages": {
        "cold_deploy": "pass",
        "migrate": "pass",
        "test_plan": "pass",
        "test_apply": "pass",
        "drift_reconverge": "not_run"
      },
      "notes": "Verified end-to-end 2026-08-17/18. Predates the drift-and-reconverge stage being added to the pipeline shape; needs a follow-up pass to add it, same as every other pre-existing corpus-* script."
    },
    {
      "dir": "live/e2e/corpus-lambda-simple",
      "source": "terraform-aws-modules/terraform-aws-lambda examples/simple (tag v8.8.1)",
      "lane": "terraform-popular",
      "stages": {
        "cold_deploy": "pass",
        "migrate": "pass",
        "test_plan": "fail",
        "test_apply": "not_run",
        "drift_reconverge": "not_run"
      },
      "notes": "Landed f6a9f899d0 (2026-08-18), which also fixed a real generalizing bug on the way: live-import's Ratify loop skipped every resource inside a module block (regressed behind closed issue #59's child-module admission, which named five other root-only walkers but never live-import). module.lambda_function.{aws_lambda_function,aws_iam_role,aws_cloudwatch_log_group} now stamp correctly - this affects every real module usage, not just this estate, since virtually all published Terraform modules nest their resources under a module block. test_plan blocked on a separate, still-open gap: two resource blocks (aws_lambda_function_url, aws_lambda_function_recursion_config) use count = var.enable_x ? 1 : 0 with the var false/unset, so stock OpenTofu creates zero instances of either, but choudoufu's type-admission check runs per declared block rather than per resolved instance and refuses the plan outright. A local_file resource is also refused (outside the live-markers subset). Filed as #303. Recommended next slot: a static count-is-zero evaluator feeding type admission - likely high-leverage, since count-gated optional-feature resources are an extremely common module idiom, not specific to this one."
    },
    {
      "dir": "live/e2e/corpus-rds-complete-postgres",
      "source": "terraform-aws-modules/terraform-aws-rds examples/complete-postgres (tag v7.2.1)",
      "lane": "terraform-popular",
      "stages": {
        "cold_deploy": "pass",
        "migrate": "pass",
        "test_plan": "fail",
        "test_apply": "not_run",
        "drift_reconverge": "not_run"
      },
      "notes": "Landed c239792018/47778a931a (2026-08-18), crossed by an agent whose worktree predated the lambda-simple crossing's live-import child-module fix (cec3c4b9b1); migrate was recorded fail against that stale base. Re-verified 2026-08-18 against current main (which has cec3c4b9b1) in an isolated worktree: live-import's dry run now reports '23 of 39 resource instance(s) are eligible for stamping (VERIFIED or DRIFTED)' (all 39 module-nested resources considered, none dropped as non-root), and a real -approve stamped them for real - '23 resource(s) newly stamped, 0 already stamped, 0 failed, 16 skipped' - confirmed independently by reading the primary aws_db_instance's tags straight through the AWS CLI (0 tofu-address tags before, 1 after). The 16 skipped are not a regression: 13 are untaggable by design (aws_route_table_association, aws_route, aws_security_group_rule, aws_iam_role_policy_attachment, random_id - no tags argument in the provider schema, expected) and 3 are #305's still-open unadmitted-type gap (aws_default_network_acl/aws_default_route_table/aws_default_security_group). test_plan remains fail, and is confirmed to fail for exactly the two already-filed, still-open reasons rather than anything new: a live-plan run against the now-really-migrated estate still refuses with 35 count-index-in-tag sites (7 of them aws_security_group_rule.ingress_with_cidr_blocks, #304) and 5 unadmitted-type sites (the three default-object adopters actually created plus aws_default_vpc/aws_vpn_gateway_attachment declared at count=0, #305) - identical counts to the original crossing. Note: the committed run.sh's own stage-2 assertions still expect the OLD pre-fix '0 eligible' shape and pass only because they're now acting as a stale control, not a real check - the script needs a follow-up pass to assert the real, current numbers instead. Two unrelated real floci gaps found and filed upstream on the fork: lex00/floci#51 (RDS cross-region backup replication), lex00/floci#52 (SecretsManager RotateSecret wrongly requires a Lambda ARN for RDS-managed rotation) - both worked around in the script with documented EMULATOR GAP deltas so stage 1 could stand up at all."
    },
    {
      "dir": "live/e2e/corpus-vpc-complete",
      "source": "terraform-aws-modules/terraform-aws-vpc examples/complete (tag v6.6.1), its flagship example",
      "lane": "terraform-popular",
      "stages": {
        "cold_deploy": "fail",
        "migrate": "not_run",
        "test_plan": "not_run",
        "test_apply": "not_run",
        "drift_reconverge": "not_run"
      },
      "notes": "Landed 9b75cae5ce (2026-08-18). Two real, distinct fixes landed on the way: (1) a floci gap where DescribeVpcEndpointServices always answered empty, blocking every VPC endpoint regardless of service - fixed on the fork (lex00/floci commit 83e44fe6, reconciled into the combined image the orchestrator is republishing) and (2) closed #303 outright (see that issue) via the new generically-derived blockHasNoInstances, hit here through the same terraform-aws-vpc default-object-adoption pattern #305 already named. cold_deploy still fails on floci gaps this crossing judged out of scope for the session rather than sprawling into: Redshift has no service in floci at all (CreateClusterSubnetGroup), plus CreateDhcpOptions, CreateCacheSubnetGroup (ElastiCache) and CreateCustomerGateway/CreateVpnGateway all return UnsupportedOperation with no implementation in floci's Java source. Stages 2-5 are implemented in the script (live-import adoption, empty replan with rendered-identity assertions, no-op apply, drift-and-reconverge on a subnet tag, BREAK=1 controls) but unexercised since stage 1 fails fast by design."
    },
    {
      "dir": "live/e2e/corpus-eks-basic",
      "source": "terraform-aws-modules/terraform-aws-eks examples/basic (tag v9.0.0)",
      "lane": "terraform-popular",
      "stages": {
        "cold_deploy": "pass",
        "migrate": "fail",
        "test_plan": "fail",
        "test_apply": "not_run",
        "drift_reconverge": "not_run"
      },
      "notes": "Landed 9717459fd3 (2026-08-18). Crossed by an agent whose worktree, like the original rds-complete-postgres crossing, predates the lambda-simple crossing's live-import child-module fix (cec3c4b9b1) - migrate is recorded fail here ('3 of 4 root-module resources stamped, 50 non-root instances not considered') because that's what the agent's own stale base observed. Given rds-complete-postgres's own re-verification confirmed cec3c4b9b1 generalizes cleanly to a different estate's module-nested resources, this row very likely also improves once re-verified against current main - flagged for the same re-verification treatment, not yet done. test_plan independently fails on unadmitted default_*/VPN-gateway types (overlapping #305's pattern), undeclared-record-store logical resources, and 7 correctly-conservative count-index refusals (element() over a list, which the checker cannot statically prove injective - not a defect). Two real, generalizing floci gaps found and fixed but NOT YET merged/published: EKS worker AMI discovery (AWS-owned AMI catalog lookup, owner 602401143452/801119661308, evaluated unconditionally by every terraform-aws-eks version) and AutoScalingGroup SuspendProcesses/ResumeProcesses (unimplemented, but the AWS provider calls SuspendProcesses unconditionally around ASG creation on default settings - blocks any aws_autoscaling_group apply, not just EKS's). Filed as lex00/floci#55, PR lex00/floci#56, pushed but not merged - the currently-pinned floci image predates both fixes."
    }
  ]
}
