[
  {
    "estate": "terralith-scale",
    "stage": "test_plan",
    "reason": "At SCALE=136 (10,069 resources) the post-migration plan is refused by the count-index rule, not merely non-empty: 'count.index is not available in resource arguments', on aws_iam_instance_profile.count_team_profile, whose count is 2*SCALE. The same estate at SCALE=1 plans clean, three times over on the same commit and the same emulator pin, so this is scale-dependent rather than a break in the stage. Two independent harnesses reproduce it - this crossing script and TestSlicingMatrixAgainstFloci at SLICE_SCALE=136 - so it is choudoufu's own behaviour and not a harness artifact. Acknowledged here so the scale-136 row can land with its provenance; the threshold and the mechanism are under investigation.",
    "issue": "#1051"
  },
  {
    "estate": "terralith-scale",
    "stage": "day2_remove",
    "reason": "choudoufu does not propose destroying aws_iam_instance_profile.team_0002_profile when its block is deleted. Neither sweep leg enumerates the type: the provider has no list resource for it, so it routes to the tagging leg, and #1045's emulator repin stopped floci answering GetResources for IAM to match real AWS. The stage's committed 'pass' was never a measurement - it was carried forward across an early abort by the merge #1069 fixed, and the last genuine pass is b8fbe8909d on 2026-08-31.",
    "issue": "#881"
  },
  {
    "estate": "terralith-scale",
    "stage": "day2_count",
    "reason": "At SCALE=128 (9,477 resources) this stage's STOCK ORACLE fails, not choudoufu: after stock's own scale-down apply reports exactly six destroys, all of them index [1], an `iam list-roles` query still finds the destroyed count_team[1] role. The oracle fixture uses its own `${PREFIX}o-` names, so this is not a collision with the greenfield estate that shares the account. Unverified hypothesis: floci's list-roles pagination over the ~4,200 roles greenfield leaves in GREEN at this scale. The same stage passes at scale 1 and passed at scales 10 and 50 on 2026-09-09/10, where the account holds far fewer roles.",
    "issue": "#1077"
  },
  {
    "estate": "corpus-eks-basic",
    "stage": "test_plan",
    "reason": "This stage's committed 'pass' was never a measurement on the current pin. It is from 3bca740259 on 2026-09-10 against sha256:9ec3fa64; f51d0c882b repinned the emulator on 2026-09-11, and from that day every run of this estate exited at migrate on the count #1497 fixes, so test_plan was not reached again until now. Reached, it fails twice on one commit with identical detail (17s and 18s): the Kubernetes sweep reads LIST_FAILED on all 28 kubernetes_* types under 'Warning: Kubernetes sweep unavailable - the cluster's API discovery failed', and the plan proposes creating module.eks.kubernetes_config_map.aws_auth[0] although discovery bound it ADOPTABLE to the live kube-system/aws-auth object in the same run. Not #326 or #313: the log confirms both are clear two lines before the assertion fires. Acknowledged here so #1497's re-measurement of migrate can land with this row's provenance visible; the sweep is #1527 and is a unit of its own.",
    "issue": "#1527"
  }
]
