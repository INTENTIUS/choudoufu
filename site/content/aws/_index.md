---
title: "AWS"
layout: hub
weight: 10
lead: "Every resource this fork manages carries two tags, `tofu-estate` and `tofu-address`, written on the create call. The tags are the identity the next plan binds to and the attribute your IAM conditions on. Everything else is stock OpenTofu."
---

Six pages, one question each. Each ends with where to dig.
