---
title: "terralith-scale"
weight: 50
---
<!-- Generated by `go run ./tools/gauntlet render` from tools/gauntlet/stages.go, live/gauntlet/estates.json and live/gauntlet.json. Do not edit; TestRenderedDocsAreCurrent holds this file to the tree. -->

tools/terralith-gen, generated at -scale 1 by this estate's own crossing script: a synthetic single-state terralith of stock Terraform - IAM-dominated with deliberate copy-paste duplication, a small ECS layer and a Route53 fan-out, expanded with count, for_each and a module-nested pod (issues #564 and #574). No upstream repository: the artifact is the generator, and "the same estate at 4x" is -scale 4 rather than a second fixture.

Set: core. Lane: reference.

Why it is in the core set: the one estate shaped like the thing the product is FOR - a single-state monolith a stranger would bring to an adoption (#546) - rather than a module example; every other core estate is a small published module, so a headline bar without this one does not read the product's own claim

**Not clear yet.**

| Stage | Verdict | Duration | Detail |
|---|---|---|---|
| Cold deploy | pass | 1m21s | stock terraform applied 79 resources at scale=1 from unmodified terralith-gen output (no live block, no choudoufu binary in this script at all), 34 objects enumerated live across IAM/Route53/ECS/EC2 with a BREAK control proving the enumeration has teeth, then stock destroy removed all 79 and left the prefix's account enumerated empty |
| Migrate | not run | - | this crossing script is deliberately choudoufu-free (issue #564): it proves the generator emits valid stock Terraform that applies and destroys, and every stage past cold_deploy needs the choudoufu binary this script does not invoke |
| Replan from nothing | not run | - | this crossing script is deliberately choudoufu-free (issue #564): it proves the generator emits valid stock Terraform that applies and destroys, and every stage past cold_deploy needs the choudoufu binary this script does not invoke |
| No-op apply | not run | - | this crossing script is deliberately choudoufu-free (issue #564): it proves the generator emits valid stock Terraform that applies and destroys, and every stage past cold_deploy needs the choudoufu binary this script does not invoke |
| Drift and reconverge | not run | - | this crossing script is deliberately choudoufu-free (issue #564): it proves the generator emits valid stock Terraform that applies and destroys, and every stage past cold_deploy needs the choudoufu binary this script does not invoke |
| Rename | not run | - | this crossing script is deliberately choudoufu-free (issue #564): it proves the generator emits valid stock Terraform that applies and destroys, and every stage past cold_deploy needs the choudoufu binary this script does not invoke |
| Remove a block | not run | - | this crossing script is deliberately choudoufu-free (issue #564): it proves the generator emits valid stock Terraform that applies and destroys, and every stage past cold_deploy needs the choudoufu binary this script does not invoke |
| Change count | not run | - | this crossing script is deliberately choudoufu-free (issue #564): it proves the generator emits valid stock Terraform that applies and destroys, and every stage past cold_deploy needs the choudoufu binary this script does not invoke |
| Replace with create_before_destroy | not run | - | this crossing script is deliberately choudoufu-free (issue #564): it proves the generator emits valid stock Terraform that applies and destroys, and every stage past cold_deploy needs the choudoufu binary this script does not invoke |
| Crash between create and destroy (planned) | not run |  |  |
| Teardown (planned) | not run |  |  |
| Plan, review, apply (planned) | not run |  |  |
| Greenfield apply | not run | - | this crossing script is deliberately choudoufu-free (issue #564): it proves the generator emits valid stock Terraform that applies and destroys, and every stage past cold_deploy needs the choudoufu binary this script does not invoke |
| Strict profile (not a headline stage) | not run | - | this crossing script is deliberately choudoufu-free (issue #564): it proves the generator emits valid stock Terraform that applies and destroys, and every stage past cold_deploy needs the choudoufu binary this script does not invoke |

Last run at commit `20faceacaa` on 2026-08-30T17:10:39Z, exit code 0, against emulator image `ghcr.io/lex00/floci@sha256:c55d74e13e96c8b132056677337dba0084bb0b427cb039be2dbf9a8b7efc0948`. Total run time 1m20.9s.
Oracle: stock terraform `1.15.8`, stock tofu `1.12.5`. **Stale**: the current pin is terraform `1.16.0`, tofu `1.12.6`.

## Reproduce it

```
go run ./tools/gauntlet run terralith-scale
```

Needs Docker (the emulator is pulled at the pinned digest), the AWS CLI, and a
stock `terraform` or `tofu` binary on PATH for the cold deploy. The script is
`live/e2e/terralith-scale/run.sh`; `BREAK=1` corrupts its assertions to show they are load-bearing.
