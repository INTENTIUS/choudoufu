{
  "name": "choudoufu-example-record-store-bucket",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The bucket a live estate's record_store \"s3\" writes into: versioning, a lifecycle rule that expires noncurrent versions and never current records, and public-access block. `just verify` asks the choudoufu binary whether the bucket is correct.",
  "scripts": {
    "build": "chant build src --lexicon aws -o templates/template.json",
    "lint": "chant lint src",
    "list": "chant list src",
    "plan": "chant run bucket-plan",
    "apply": "chant run bucket-apply"
  },
  "dependencies": {
    "@intentius/chant": "0.73.0",
    "@intentius/chant-lexicon-aws": "0.73.0"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
