# Per-run artifacts: manifests, transcripts, terraform working dirs, captured numbers.
runs/
# uv's virtualenv.
.venv/
# Terraform/OpenTofu working files that end up under a run dir anyway.
__pycache__/
