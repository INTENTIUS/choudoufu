Synthetic-by-emitter. Written by tlmig's real emitters (events.py, guard,
govern, receipt) end to end with the cloud faked at the guard boundary and
the receipt read from the 2026-09-03 carve-by-retag log, by choudoufu-e9.
Everything here is what the modules serialize, not a hand-typed sample; the
live request counts are placeholders, the reference numbers are the real
receipt. The first real events.jsonl comes from the beats.
