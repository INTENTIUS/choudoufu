# live-plan.yml - generated by examples/ci-pipelines/generate.ts. DO NOT EDIT.
# Regenerate with: CHANT_FORGE=forgejo npm run generate
# The Op this job runs is examples/ci-pipelines/src/live-plan.op.ts.

on:
  pull_request:
    branches:
      - main

env:
  CHANT_FORGE: forgejo
  AWS_REGION: '${{ vars.AWS_REGION }}'
  TF_VAR_aws_region: '${{ vars.AWS_REGION }}'

concurrency:
  group: live-plan
  cancel-in-progress: false

jobs:
  live-plan:
    runs-on: docker
    container: node:22
    env:
      AWS_ACCESS_KEY_ID: '${{ secrets.CHOUDOUFU_PLAN_ACCESS_KEY_ID }}'
      AWS_SECRET_ACCESS_KEY: '${{ secrets.CHOUDOUFU_PLAN_SECRET_ACCESS_KEY }}'
    steps:
      - uses: https://code.forgejo.org/actions/checkout@v4
      - run: curl -fsSL -o /tmp/gh_2.100.0_linux_amd64.tar.gz https://github.com/cli/cli/releases/download/v2.100.0/gh_2.100.0_linux_amd64.tar.gz && echo "e4d4bb4498e8d007abe545b6568926793ace1b6447da598294a610018cb164be  /tmp/gh_2.100.0_linux_amd64.tar.gz" | sha256sum -c - && tar -xzf /tmp/gh_2.100.0_linux_amd64.tar.gz -C /tmp && install /tmp/gh_2.100.0_linux_amd64/bin/gh /usr/local/bin/gh && gh --version
      - run: npm ci --no-audit --no-fund
      - run: curl -fsSL -o /tmp/choudoufu_v0.16.0_linux_amd64.tar.gz https://github.com/INTENTIUS/choudoufu/releases/download/v0.16.0/choudoufu_v0.16.0_linux_amd64.tar.gz && echo "1e9d540893c6f30fad47530614cf0b1935672e991167a175aca940ebe7b3c7ae  /tmp/choudoufu_v0.16.0_linux_amd64.tar.gz" | sha256sum -c - && tar -xzf /tmp/choudoufu_v0.16.0_linux_amd64.tar.gz -C /usr/local/bin choudoufu && choudoufu version
      - run: npx chant run live-plan
        env:
          GITHUB_TOKEN: '${{ github.token }}'
          GH_TOKEN: '${{ github.token }}'
